
## 2026-07-12 Anthropic/OpenAI 非流式强制上游 SSE

- Blame: OpenCode hy3 direct OK; zen Anthropic `stream=false` → 502 `stream truncated before DONE or finish_reason` because `handle_non_stream`/`handle_oa_non_stream` call `fetch_zen_stream` + `collect_stream_parts` while upstream body kept `stream:false` (JSON, not SSE). Anthropic/OpenAI `stream=true` on panda :4002 OK.
- Fix: force `attempt_body["stream"]=true` each non-stream collect attempt (client still gets aggregated non-stream JSON).

## 2026-07-15 22:30

目标：
从 ZenProxy/free-model-client-rs 源头提高 DeepSeek V4 Flash、Mimo V2.5、Hy3 的 ClaudeCode 工具兼容、首字、总耗时和稳定性，并完成生产部署、真实 CC Switch 验收、NewAPI 日志对账和交接。

已做：
提交 `a3fc5ca`、`2c29662`、`210f60c`；完成 Hy3 profile/forced-tool/cache 策略、非流式 SSE 聚合、reasoning alias、watchdog 和错误脱敏修复。本地 release build 后通过 GitHub 临时 release asset 部署 Panda，Panda 未编译。Windows CC Switch 创建三个临时 provider 和专用 NewAPI token，执行正式 189 项矩阵及部署后定向回归。

验证：
`free-model-client-rs` 172 unit + 140 golden；`zen-proxy-rs` 221 unit + 44 e2e。正式矩阵 `184/189`，工具执行 `185/189`。部署后定向首轮 `16/18`，Hy3 Grep stream-json 与 DeepSeek Edit text 重跑均通过。部署后 NewAPI/CC Switch `57/57` HTTP 200、0 错误；CC Switch FRT P95：DeepSeek 8.015s、Mimo 15.748s、Hy3 13.918s。

结论：
最终生产 hash 为 `1e5102df0d2f4ec9bd7cbb6fbae44134368ba48f1613a694df4becb6dfad41d7`，三实例和四个 health 端口正常。Mimo 请求级 cache-read 覆盖 95.65%；DeepSeek 36.84%、Hy3 40.00%，不能宣称三模型均达到 85%-95%。Hy3 cache breakpoint 因损害工具参数完整性保持禁用。

未完成：
仍需 24 小时真实流量冷/暖会话分层；Mimo 100s+ ClaudeCode 总耗时长尾需要继续定位；DeepSeek/Hy3 provider 缓存行为需要 A/B。

下一步：
临时 NewAPI token、CC Switch provider/备份、本地 key、GitHub release/assets 和 Panda 中间备份已清理；按顶层 `docs/CLAUDECODE_STABILITY_HANDOFF_2026-07-15.md` 继续 24 小时稳态验收。

# 2026-07 工作日志

## 2026-07-02 16:25

目标：
恢复 channel 69 生产可用性，更换失效 Webshare 代理池，把旧公开名 `deepseek-v4-flash-lite` 改回 `big-pickle` 并取消公开映射。

已做：
验证用户提供的新 Webshare 100 代理，100/100 可访问上游，100/100 出口为 `SG`；替换 panda `/etc/zen-proxy-rs/nodes-prod.json`。修改并部署 `zen-proxy-rs` public static model 为 `big-pickle`，移除旧 lite public alias；同步 NewAPI channel 69、models、abilities 和启用状态。

验证：
`free-model-client-rs` 通过 `cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`；`zen-proxy-rs` 通过同样三项。panda 4001/4002/4004/4000 health 均 `ok`。ZenProxy 直连 OpenAI chat/stream 三模型通过；NewAPI `/v1/models` 显示 `deepseek-v4-flash,big-pickle,mimo-v2.5` 且不显示旧 lite；NewAPI chat/completions 三模型 200，旧 lite 为 `No available channel`；NewAPI channel test stream 对 Mimo 和 `big-pickle` 成功，DeepSeek 单独重试成功但耗时约 104s。

结论：
生产代理认证 502 已恢复，channel 69 已使用新公开模型名。普通 NewAPI chat 路径可用；管理端 channel test 的短 `/v1/messages` 探针对 DeepSeek 仍有慢尾，不代表普通 chat 或 ClaudeCode 全局质量结论。

未完成：
未跑 10k/50k/100k/200k 半压测；未重新统计部署后真实 cache hit/TTFT 分布。

下一步：
观察 channel 69 真实流量 30-60 分钟，重点扫 proxy auth/502/429/lane saturated、DeepSeek 管理端探针慢尾，以及 `big-pickle`/Mimo 的 ClaudeCode stream 质量。

## 2026-07-02 21:10

目标：
按用户要求确认真实 ClaudeCode 必须走 `ClaudeCode -> cc-switch -> https://sub2api.closeapi.top`，不能用 Tailscale/panda 内网链路替代，并解释 cc-switch 与 NewAPI 模型名展示差异。

已做：
确认 Windows ClaudeCode `ANTHROPIC_BASE_URL=http://127.0.0.1:15721`，cc-switch 当前 provider 由用户切到 `closedmimo`，base URL 为 `https://sub2api.closeapi.top`。复查公网 DNS 已解析到 Cloudflare 公网 IP，443 可连。Cloudflare A/B 显示仅 `User-Agent: Python-urllib/3.12` 返回 403/1010；curl/no-UA/ClaudeCode-like/Mozilla UA 均到达 NewAPI 并返回正常鉴权层 401，因此 1010 是测试工具 UA 触发的 Cloudflare Browser Integrity Check，不是 ClaudeCode/cc-switch 普遍被挡。

验证：
使用 Windows official `claude.orig.exe`，经 cc-switch `127.0.0.1:15721` 和 `sub2api.closeapi.top` 串行 smoke。`deepseek-v4-flash` 9/9 通过，覆盖 Bash/WebFetch/WebSearch x text/json/stream-json；`mimo-v2.5` 9/9 通过，覆盖同一矩阵；`big-pickle` 3/3 通过，覆盖 Bash/WebFetch/WebSearch 的 stream-json。产物在 `/tmp/claudecode-ccswitch-smoke-runs/20260702-ccswitch-*-smoke/`，只保存脱敏哈希、状态和耗时。

对账：
cc-switch 同窗口全部 200，无错误行：DeepSeek `request_model=deepseek-v4-flash` -> `model=deepseek-v4-flash-free` 21 条，Mimo `request_model=mimo-v2.5` -> `model=mimo-v2.5-free` 21 条，big-pickle `request_model=big-pickle` -> `model=big-pickle` 7 条。NewAPI 最近 30 分钟 channel 69 只有 `type=2` 成功消费：`deepseek-v4-flash` 22 条、`mimo-v2.5` 28 条、`big-pickle` 8 条；聚合 cache_tokens 分别约 306,944、394,816、102,144。ZenProxy 同窗口未见 `unsupported V4 model`、`upstream_429`、`proxy authorization required`、`stream truncated`、`no proxy resources`、`lane is saturated` 或 panic；4001/4002/4004/4000 health 均 `status=ok`、`dispatch=100`、`dead=0`、`ratelimited=0`。

结论：
cc-switch 显示 `mimo-v2.5 -> mimo-v2.5-free` 是因为它展示 `request_model -> provider/upstream model`；NewAPI 展示 `mimo-v2.5` 是因为它记录 channel 69 的公开请求模型，内部由 ZenProxy 再映射到 `mimo-v2.5-free`。两边缓存读数可对上，说明不是路由错乱。Mimo 本地 `API Error: 400 unsupported V4 model` 未在本轮真实 cc-switch smoke 复现；NewAPI 偶发 `upstream provider rate limited` 在本轮窗口也未复现。

未完成：
本轮只做小流量 smoke 和对账，未启动 10k/50k/100k/200k x 50rpm 半压测，也未改 cache/session/affinity 代码。

下一步：
若继续 cache/TTFT 优化，先基于这条公网 cc-switch 链路做 20rpm 分桶校准，再升 50rpm；报告继续同时列 cache hit、TTFT/first_content/first_tool_emit P50-P99、prefix 稳定度和工具质量。

## 2026-07-02 21:35

目标：
按用户要求把 WSL 下 `free-model-client-rs` 与 `zen-proxy-rs` 的协同工作收束到一个项目入口，清理开发中间垃圾，并补完整交接文档。

已做：
创建 `/home/lenovo/zen-free-model-suite`，通过软链接聚合两个真实仓库和本轮 ClaudeCode cc-switch smoke 产物；新增 `README.md`、`docs/PROJECT_HANDOFF.md`、`docs/CLEANUP_AND_STRUCTURE.md`、`docs/OPERATING_RULES.md`。同步更新本仓库 `docs/README.md` 和 `zen-proxy-rs/docs/README.md`，把统一入口写入维护入口。

清理：
删除确定性中间产物和孤儿文件：`free-model-client-rs/.bun`、`.codex_tmp`、`~`、`tmpcc-zenprobe-wsl-*`、根目录 `north-mini-code`、`""`、`\`，以及 `zen-proxy-rs/.codex_tmp`、`tmp`、`target-1.86`。复查两个仓库内不再有 `*/__pycache__`、`.pytest_cache` 或 `*/target/tmp`。`test-records/runs/` 作为历史证据保留但加入忽略，不提交原始大日志。

Git 收口：
更新两个仓库 `.gitignore`，隔离 `.codex_tmp/`、本地缓存、临时运行目录和 `test-records/runs/`。`zen-proxy-rs/target-1.86` 是误跟踪的构建产物，已从 git 索引移除并保留忽略规则。两个仓库和聚合目录所有权已统一为 `lenovo:lenovo`。

结论：
当前完成的是非破坏性聚合，不移动两个 git 仓库本体，避免破坏脚本、systemd、cargo 和历史文档路径。统一目录可作为后续 AI/人工接手入口；真实代码和 git 历史仍留在原仓库。

下一步：
若要进一步做真正 mono-repo 迁移，必须先写迁移 RFC，审计 systemd/nginx/deploy/cargo/docs 路径，再在分支内执行；当前不建议直接搬仓库。
