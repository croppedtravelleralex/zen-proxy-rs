# 2026-06 工作记录

## 2026-06-09 20:55

目标：
按用户确认的质量目标推进 V4.106：降低 cc-switch 口径“新增输入”占比，但不裁上下文、不改提示词、不降低输出质量。

已查：
- 用户截图窗口 `2026-06-09 17:45-20:40 CST` 显示：新增输入约 `3.01 亿`、缓存命中约 `5.76 亿`、缓存命中率 `65.7%`。
- panda/ZenProxy 复算同窗口：NewAPI prompt/new input `308,496,934`，provider cache read `580,939,136`，cc-switch 风格命中率约 `65.32%`，和截图口径一致。
- 同窗口 10k-50k 输入桶 cache hit 约 `37.82%`，明显低于 50k+ 的 `96%+`，是新增输入占比偏高的主要结构性来源。
- 源码确认 V4.98 上游 session scope 默认用 `256KB` prefix；10k-50k 的 `messages` material 常小于 256KB，尾部追加会导致 session/project 改变。

已做：
- `src/zen/client.rs`：新增中等上下文稳定前缀 `ZEN_UPSTREAM_MEDIUM_SESSION_PREFIX_BYTES`，默认 `32KB`；大上下文仍使用 `ZEN_UPSTREAM_SESSION_PREFIX_BYTES` 默认 `256KB`。
- session scope 版本升级为 `large_prefix_v4106`，避免和旧 V4.98 分组混淆。
- 新增回归：中等上下文稳定前缀不变、尾部追加时 `x-opencode-session` 保持稳定；中等上下文前缀变化时 session 必须变化。
- 更新 `docs/03-roadmap.md`、`docs/04-improvement-backlog.md` 和 `docs/02-current-state.md`，写明质量硬门槛和线上验收。

验证：
- `cargo fmt -- --check` 通过。
- `CARGO_INCREMENTAL=0 cargo clippy --all-targets -- -D warnings` 通过。
- `CARGO_INCREMENTAL=0 cargo test` 通过：lib/main 122 条、kernel golden 112 条。

结论：
- V4.106 当前是 header/session 层 cache-friendly 优化，不改请求体、不改模型输入语义，预期质量不降。
- 尚未部署 panda；部署后才可判断 10k-50k cache hit、cc-switch 总命中率和真实首字是否改善。

## 2026-06-09 17:30

目标：
处理旧脏项，并将 V4.105 ClaudeCode true-stream/cache-hit 源码补丁部署到 panda。

已做：
- 清理 `free-model-client-rs` 旧脏项：删除临时 `err.log`、空 `out.jsonl` 和异常双引号 0 字节文件；恢复/确认 `scripts/panda_pressure_runner.py` 在 WSL 侧执行位，WSL Git 工作区干净。
- 在 `/home/lenovo/zen-proxy-rs` 构建 release，确认 `Cargo.toml` 通过 path 依赖 `/home/lenovo/free-model-client-rs`，已包含 `free-model-client-rs` commit `74001de`。
- 本地 stripped 二进制 SHA256：`a52f4d6add0a93fe0070a59c3a3ec9ee3b4bc0a9172047c7b3ec5855e67ff7e8`。
- 本机到 panda 的 Tailscale/SSH 文件传输继续抖动：`scp` 传 12MB 二进制和 3.3MB xz 包均被 reset；最终启动本机临时 HTTP 服务，按 64KB 小分片让 panda 主动 `curl` 拉取并重组，远端 xz hash `f022b189b18383ee2b10f74aeda1da21721f0868e43f2df74ddf121211c05028` 校验通过。
- 备份旧 V4.104 线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.20260609-172830.pre-v4105-08d9064600e6`。
- 安装新二进制并滚动重启 `zen-proxy-rs@1`、`@2`、`@3`。

验证：
- 三实例 `systemctl is-active` 均为 `active`，新 pid 分别为 `684958`、`685069`、`685141`。
- 线上 `/opt/zen-proxy-rs/zen-proxy-rs` hash 为 `a52f4d6add0a93fe0070a59c3a3ec9ee3b4bc0a9172047c7b3ec5855e67ff7e8`。
- panda 4000 `/health` HTTP 200，`status=ok`、`dead=0`、`ratelimited=0`；4000 `/v1/models` HTTP 200，只暴露 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- panda NewAPI OpenAI 非流式 `deepseek-v4-flash` 返回 `PONG`，HTTP 200，约 3.37s。
- panda NewAPI Anthropic 非流式 `deepseek-v4-flash` 返回 `PONG`，HTTP 200，约 3.20s。

结论：
- V4.105 已上线 panda，基础服务和 NewAPI 用户链路可用。
- 这次部署没有修改 NewAPI、ClaudeCode 或 cc-switch；仍是 ZenProxyRS/free-model-client-rs 源头修复。

待观察：
- 用真实 ClaudeCode 长会话观察 `anthropic_buffered` 是否降到窄场景，buffer-like 慢尾是否下降。
- 补 cc-switch/NewAPI/ZenProxy 三侧 cache hit 对齐报表，区分真未命中、字段未记录和统计口径差异。

## 2026-06-09 16:20

目标：
把 ClaudeCode 慢尾和 cache hit 低于预期的问题收敛为 V4.105，并先在源码层落地；本轮不改 NewAPI、ClaudeCode 或 cc-switch，不部署 panda。

已查：
- cc-switch 15:29 CST 后 `deepseek-v4-flash-free` 成功样本约 955 次，真实首字 P50 约 6.5s、P90 约 13.5s、P95 约 17.2s、P99 约 31.6s。
- 同窗口 `first_token_ms≈latency_ms` 的 buffer-like 请求约 290 次，P95 约 26.6s；progressive 真流式约 665 次，P95 约 13.5s。
- 100k+ 输入桶中，buffer-like 占比约 44%-47%，说明慢尾主要来自假流式/buffered，而不是所有大上下文都慢。
- 当日 cc-switch `deepseek-v4-flash-free` 成功流式约 6840 次，cache_read 命中约 4988 次，命中率约 72.9%；13:00 约 63.2%、14:00 约 59.8%、16:00 约 95.3%。
- 输入桶维度：`10k-50k` 命中约 9.9%，`50k-100k` 约 84.1%，`100k-200k` 约 98.1%，`200k+` 约 98.3%。
- 源码确认 `anthropic_buffered` 会先完整收完上游流再下发；`exact_output_literal` 检测包含 `只输出/只回复/reply exactly/output only`，普通 ClaudeCode 格式要求可能误触发。
- 源码确认 `ZenUsage` 未显式解析 DeepSeek 官方常见 `prompt_cache_hit_tokens/prompt_cache_miss_tokens`，存在 cache usage 低估或显示不完整风险。

已更新：
- `docs/03-roadmap.md`：新增 V4.105 ClaudeCode true-stream/cache-hit 专项到 Now/Next。
- `docs/04-improvement-backlog.md`：新增 P0 V4.105 待办，列出 buffered 误触发、cache usage 字段别名、三侧对齐报表和验收标准。
- `docs/02-current-state.md`：新增 P1.23 诊断记录。

已落地源码：
- `src/proxy/anthropic.rs`：`anthropic_buffered` 判定从 bool 改为 reason；新增 `buffer_reason` 日志；ClaudeCode 带 tools 的长会话、多行 Markdown/JSON/代码块 exact-output 不再进入 buffered。
- `src/zen/client.rs`：`prompt_cache_hit_tokens` 映射到通用 cache read 路径；`prompt_cache_miss_tokens` 进入 cache miss 观测；header alias 补 `x-prompt-cache-hit-tokens` 等兼容字段。
- `src/proxy/openai.rs` / `src/proxy/anthropic.rs`：下游 usage 统一通过 `ZenUsage::cache_read_tokens()` 透传 cache read。
- 新增单元测试：ClaudeCode buffered reason 4 条；provider cache signals 2 条。

验证：
- `cargo fmt -- --check` 通过。
- `CARGO_INCREMENTAL=0 cargo clippy --all-targets -- -D warnings` 通过。
- `CARGO_INCREMENTAL=0 cargo test` 通过，lib/main 120 条、kernel golden 112 条。

结论：
换 Clash 节点确实改善了 progressive 路径，但慢尾主要来自 `anthropic_buffered` 误触发。cache hit 不是全面失败：50k+ 尤其 100k+ 命中较高，用户看到的 60.6% 更可能被 10k-50k、旧窗口或统计口径拉低；仍需补字段别名和三侧对齐确认是否有漏记。

下一步：
部署 V4.105 到 panda，观察真实 ClaudeCode 长会话；补 cache hit 审计脚本或 sidecar 查询，按小时、输入桶、session、prefix hash、buffered/progressive 对齐 cc-switch/NewAPI/ZenProxy 三侧数据。

## 2026-06-06 01:00

目标：
修复 ClaudeCode 仍偶发 `API Error: Failed to parse JSON`、`stream truncated before DONE or finish_reason` 和流式中断的问题，同时避免牺牲正常输出质量。

已做：
- 复查 panda NewAPI channel 69 与 ZenProxy journal，确认该类客户端错误的服务端对应是 ClaudeCode Anthropic stream 上游截断，不是 30KB Write JSON 序列化溢出。
- 在 `src/proxy/anthropic.rs` 落地 V4.99 Stream Guard：真实 text/tool 输出前，上游 fetch/stream 截断可原地重试；60 秒无可转发内容触发重试；最多 3 次；最后一次仅在工具请求中 disabled thinking。
- 增加 Anthropic stream 错误结构化日志，记录 attempt、idle ping、上游事件数、text/reasoning/tool 计数、finish reason 和 prompt hash。
- 对 Anthropic 普通流式和 buffered huge-stream 的 `input_json_delta` 做 4KB 分片，保证拼接后 JSON 不变，降低大 Write 参数解析压力。
- 修复 ClaudeCode forced `tool_choice` 与 DeepSeek thinking 冲突：forced `tool_choice` 首跳禁用 thinking；`tool_choice=auto`、普通 tools auto 和 unknown client 保持默认 thinking。
- 更新 `docs/02-current-state.md` 与 `docs/05-ai-maintenance-playbook.md`。

验证：
- `free-model-client-rs`：`cargo fmt -- --check` 通过。
- `free-model-client-rs`：`cargo clippy --all-targets -- -D warnings` 通过。
- `free-model-client-rs`：`cargo test` 通过，库测试 89 条、kernel golden 100 条。
- `/home/lenovo/zen-proxy-rs` release 构建通过。
- panda 最终部署 stripped SHA256：`39dc0bb94092597a00518abf83e80f8c32a91e8c60682c169942bf16bf70017d`。
- 旧线上 hash `9d64728e5511f2b414d16f4f4dac27395dabb1abe3ae64c2cf9404ee4f31ba0e` 已备份到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-v499-forced-tool-20260606-005416-9d64728`。
- panda `zen-proxy-rs@1/@2/@3` active；4001/4002/4004/4000 `/health` 均 ok。
- panda NewAPI smoke：`/v1/models` HTTP 200；Anthropic stream PONG HTTP 200、`event:error=0`；Anthropic forced `tool_choice` 工具流 HTTP 200，`tool_use/input_json_delta` 存在，`Thinking mode does not support this tool_choice=0`。
- 最终部署后 channel 69 采样 13/13 成功、0 错误。

结论：
V4.99 Stream Guard 已部署到 panda，当前服务端已不再把“上游流截断且无真实输出”的失败直接裸透成 ClaudeCode 可见的半截 Anthropic 流。forced `tool_choice` 与 thinking 的上游协议冲突也已窄修，不影响普通 ClaudeCode tools auto 质量路径。

未完成：
- 需要用户真实 ClaudeCode 长会话继续观察 1-2 小时，重点看 `stream truncated`、`Failed to parse JSON`、`missing parameters` 是否复发。
- 部署前仍有 3 条非流式 300s/504 旧记录，属于 long non-stream 超时类问题，不是本轮 stream parse 修复范围。

下一步：
若仍出现 `Failed to parse JSON`，先按 V4.99 日志关键词 `ClaudeCode stream guard` 对齐 prompt hash；若转为非流式 300s/504，则单独设计 long non-stream watchdog/拒绝或转流式保护。

## 2026-06-04 15:49

目标：
把包含 `finish_reason` 透传、non-stream cache usage 透传和 ClaudeCode 中等工具历史压缩的最新 `zen-proxy-rs` release 部署到 panda，并做最小生产链路验收。

已做：
- 确认 `/home/lenovo/zen-proxy-rs` 通过 path 依赖引用 `../free-model-client-rs`。
- 在 WSL 本地构建 `zen-proxy-rs` release，并生成未 strip hash `9b68db105aaad2c1014899d00122accf3a21109a26054f68ce0d612f152b5839`。
- strip 产物后得到部署 hash `694036f6a130e8211b998a5b58eff36105fb48fb866ec57ebbb2c03ccfb5f3d7`。
- 通过 SSH 流式传输把新二进制上传到 panda，备份旧线上二进制 `0f6cdf6e5cd2dd1946a69707c97591cca865b47178ff63846f04bbdf283f2314` 到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-v46-20260604-153327-0f6cdf6e5cd2`。
- 替换 `/opt/zen-proxy-rs/zen-proxy-rs` 并重启 `zen-proxy-rs@1/@2/@3`。
- 用有效 `vip` 组 token 对 panda `8081` 做 `/v1/models`、OpenAI 非流式、Anthropic 流式、Anthropic 非流式探针 smoke。

验证：
- 三实例 `systemctl is-active` 均为 `active`。
- 4000/4001/4002/4004 `/health` 均返回 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- `/metrics` 记录 smoke 后 `status=200` 为 2、`status=5xx` 为 1、`stream=2`、`non_stream=1`、`model=deepseek-v4-flash` 为 3。
- `curl --noproxy '*' http://100.69.228.93:8081/v1/models` 返回 200，包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 返回 `PONG`，usage `prompt_tokens=87`、`completion_tokens=27`。
- Anthropic 流式 `deepseek-v4-flash` 返回 `PONG`，usage `input_tokens=87`、`output_tokens=27`。
- Anthropic 极短非流式探针 `reply PONG only` 仍可能返回 502：`upstream retry budget exhausted ... last_error=empty_output`；ZenProxy 日志显示其被归类为 `internal_claude_code_probe`，并在上游连续空输出时耗尽 provider retry。

结论：
- 这轮 release 已成功进入 panda 生产三实例，OpenAI 非流式和 Anthropic 流式真实链路可用。
- `finish_reason` 透传、cache usage 透传和中等工具历史压缩已不是“仅源码修复”，而是线上代码事实。
- 当前残留主要是 Anthropic 极短非流式 probe 的空输出兜底仍不完整；它会污染 smoke/管理探针，但不等于真实流式用户链路失败。

未完成：
- 用真实 ClaudeCode 长会话复验半截输出体感和 `finish_reason=max_tokens` 可见性。
- 在 NewAPI 展示层确认 cache 字段是否开始出现。
- 补 Anthropic `internal_claude_code_probe` 的 non-stream 本地兜底或更细分类，避免极短探针继续打出 502。

下一步：
继续做真实 ClaudeCode 长会话复验，并单独修 Anthropic 极短 non-stream probe 的空输出兜底。

## 2026-06-01 17:20

目标：
继续处理 panda NewAPI 下 ClaudeCode huge_context 空跑、300s timeout、context_drift 问题，只从 ZenProxy/free-model-client 源头修复。

已做：
- 构建并部署包含 latest-user final-anchor 的 `zen-proxy-rs` release 到 panda 三实例。
- 线上二进制从 `94872cd91a558ec431e176af5a1fa8f257219c9518df3f729e7e5645b5cbb937` 更新为 `3a27f2c7cda56119b32dfc42738b06f3f1e08155a0ff89c48daca6ddc8aed1d4`。
- 保留备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-final-anchor-20260601-94872cd`。
- 在 panda 本机通过 NewAPI `/v1/messages` 做约 1.0MB huge stream source-side smoke。

验证：
- 三实例 `/health` 均 200，池 `total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 返回 `OK`。
- huge stream `deepseek-v4-flash` 3/3 返回 `HUGE_OK`。
- huge stream `deepseek-v4-flash-lite` 3/3 返回 `HUGE_OK`。
- ZenProxy 日志确认 `source_client=claude-code`、`compacted_messages=1`、`appended_latest_user_anchor=true`。

结论：
source-side final-anchor 已在 panda 生产链路生效，旧的 “git diff / inspect current state” 语义漂移未在本次 panda 本机 huge smoke 中复现。

未完成：
- 这不是四客户端真实 dry run，不能替代 Windows ClaudeCode、WSL ClaudeCode、WSL Hermes、WSL OpenClaw 验收。
- 日志仍可见上游偶发空输出，当前由 buffered retry 兜底，正式 dry run 需要统计重试率和尾延迟。

下一步：
重新跑 panda-only dry run，并重点观察 ClaudeCode huge_context、lite huge_context、OpenClaw subagent、Hermes 慢路径和上游 502/524/stream decode 分类。

## 2026-06-01 19:45

目标：
执行 panda-only 四客户端 dry run，验证 final-anchor 部署后的真实客户端表现。

已做：
- WSL ClaudeCode dry 50。
- WSL Hermes dry 50。
- WSL OpenClaw dry 50。
- Windows ClaudeCode dry 50，使用 Windows 原生 Python/PowerShell 启动 runner，不再把 WSL interop 结果计入正式 dry run。
- 追加脱敏报告 `docs/reports/panda-dry-run-20260601.md`。

验证：
- 总轮次 200，API OK 200/200，semantic OK 186/200。
- Windows ClaudeCode：50/50 API ok，43/50 semantic ok；6 个 huge_context `context_drift`，1 个 `subagent_not_triggered`。
- WSL ClaudeCode：50/50 API ok，44/50 semantic ok；6 个 huge_context 全部 `context_drift`。
- WSL Hermes：50/50 API ok，50/50 semantic ok；P50/P90/P99 total 约 54.3s/69.5s/103.5s。
- WSL OpenClaw：50/50 API ok，49/50 semantic ok；1 个 lite long_context `context_drift`，subagent 5/5 observed。
- panda 三实例 dry run 后健康：`total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`。

结论：
链路层稳定性明显好于之前：没有认证/模型/API 层失败，没有 300s timeout，runner summary 未观察到 502/504/协议 400。full run 仍不能启动，阻塞点已从链路可用性转为 ClaudeCode huge_context 语义漂移、Hermes 慢路径和 Windows runner 路径问题。

未完成：
- ClaudeCode 真实客户端 huge_context 仍会读取 transcript、git 状态或继续旧任务，source-side final-anchor 还不够。
- Windows ClaudeCode runner 使用 `\\wsl.localhost` UNC 工作目录，CMD fallback 到 Windows 目录，影响 subagent 结果。
- Hermes 功能通过但延迟不达标。

下一步：
先修 ClaudeCode huge_context 的真实客户端 prompt/compactor 策略和 Windows runner 工作目录，再复跑 dry run；不要启动 4 x 500 full run。

## 2026-06-03 11:40

目标：
解决 panda NewAPI channel 69 测试报 `502 upstream returned no assistant content or tool call` 的问题，只从 ZenProxy/free-model-client 源头修复。

已做：
- 在 `free-model-client-rs` 修复空内容无工具健康探测：OpenAI/Anthropic 小探测短路返回本地 `ok`。
- 修正 ClaudeCode huge buffered stream 触发条件：只在修复前估算输入 >= 50k tokens 时启用，不再因 `max_tokens <= 512` 误伤小请求。
- 补充 kernel golden：OpenAI/Anthropic 空 stream probe 短路、ClaudeCode 小低 `max_tokens` 流式请求不进入 huge buffer retry。
- 构建 `zen-proxy-rs` release 并部署到 panda 三实例，保留线上备份。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy -D warnings`、`cargo test` 通过；库测试 61 条、kernel golden 62 条。
- panda 线上二进制 hash 更新为 `28b928472d2abc7be036cdc2796915865bd4a5b083ee2dd1dab46b1ddd0e2633`。
- panda 三个 ZenProxy systemd 实例为 `active`。
- 2026-06-03 11:26 NewAPI 日志出现 channel 69 成功消费记录：`/v1/messages`、`stream_status=ok`、`frt=76ms`。
- 有效 `vip` 组 token 下，panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- 有效 `vip` 组 token 下，OpenAI 非流式 `deepseek-v4-flash` 200，返回 `CHANNEL69_OPENAI_OK`。
- 有效 `vip` 组 token 下，Anthropic 流式 `deepseek-v4-flash` 200，返回 `CHANNEL69_ANTHROPIC_OK`。

结论：
channel 69 本身可用，当前核心问题已修复。此前继续报错的主要误区是把已删除/错误分组 token 当作验收 key，或把 `token_name=ds` 当作明文 API key。

未完成：
- 仍需重新跑 panda-only smoke/dry，验证四客户端真实链路是否还存在 huge_context 语义漂移、Hermes 慢路径、lite 长上下文漂移。
- `.codex_tmp/`、`configured`、`panda`、异常字符文件等历史临时项仍不应提交。

下一步：
只用有效 `vip` 组 token 进入 panda-only smoke/dry；dry run 通过前不启动 4 客户端 x 500 full run。

## 2026-06-03 12:18

目标：
继续修复 NewAPI 管理端“测渠道”仍偶发 `upstream returned no assistant content or tool call`，但普通调用可用的问题。

已做：
- 抓取 panda NewAPI 日志，确认管理端测渠道请求为 `/api/channel/test/69?model=deepseek-v4-flash&stream=true`，内部生成 OpenAI 流式小探测，转到 ZenProxy 后走 `/v1/messages`，`body_size` 约 112-114。
- 确认失败为偶发：11:46:40 一次测渠道失败，11:46:54 和 11:47:44 同接口又成功；普通调用后续多次成功。
- 在 `free-model-client-rs` 增加第二层 fallback：无工具、流式、单用户消息、`max_tokens <= 64` 的 `hi/hello/test/echo hi` 类测渠道探测，如果上游空输出，则返回本地 `ok`，不裸透空输出错误。
- 补充 OpenAI/Anthropic 两条 kernel golden，模拟 `echo hi` 上游空流。
- 构建并部署新的 `zen-proxy-rs` 到 panda，备份旧二进制。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy -D warnings`、`cargo test` 通过；库测试 61 条，kernel golden 64 条。
- panda 线上二进制从 `28b928472d2abc7be036cdc2796915865bd4a5b083ee2dd1dab46b1ddd0e2633` 更新为 `942e69091ef424d92a3e2f4d30d54ea708e46e88cbe782ede789f7b6d77849e1`。
- 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-channel-probe-fallback-20260603-120851-28b9284`。
- panda 4000/4001/4002/4004 `/health` 均 `status=ok`，池约 `total=90/91`、`dead=0`、`ratelimited=0`。
- panda NewAPI 有效 `vip` 组 token 下，`echo hi`、`max_tokens=16`、stream 探测 10/10 HTTP 200，`empty_error=0`。
- ZenProxy 日志确认这 10 轮上游实际为空流，但已被 `short channel-test probe received empty upstream; returning local ok` 兜底。

结论：
普通调用原本可用；管理端测渠道的剩余失败点是 NewAPI 自带极短探测碰到上游空流。当前已从源头兜住该请求形态。

未完成：
- 我不能用 API token 直接调用 NewAPI 管理端 `/api/channel/test/69`，该接口要求网页登录态；最终 UI 按钮结果仍需用户点击一次确认。

下一步：
用户在 NewAPI 管理端再点 channel 69 测试后，继续看 5 分钟 NewAPI/ZenProxy 日志，确认不再出现该错误。

## 2026-06-03 19:30

目标：
排查 panda CPU 突然爆满/短尖峰来源，避免无用探活和空输出重试继续放大；同时补源码层非流式渠道探针兜底。

已做：
- 备份 panda crontab 和 `/usr/local/bin/check-socks5-proxies.sh`。
- 将旧 socks5 每 5 分钟探活 cron 禁用，旧脚本会因为单一 `httpbin.org/ip` 失败而批量 `docker restart`。
- 替换为安全版 socks5 探活脚本：`flock`、3 个探测端点、2/3 成功判定、连续失败阈值 3、单轮最多重启 1 个、重启冷却 30 分钟、跳过新启动容器。
- 确认当前生产 NewAPI/ZenProxy channel 69 不使用这些本地 socks5 容器，ZenProxy 使用 `/opt/zen-proxy-rs/nodes.json` 里的 Webshare 节点。
- 在源码层补非流式渠道探针兜底：`echo hi`/`hi`/`hello`/`test` 类极短无工具非流式探针，只有在上游连续空输出后才返回本地 `ok`；普通请求仍返回结构化空输出错误。
- 同步 `docs/02-current-state.md` 和 `docs/05-ai-maintenance-playbook.md`，明确该源码补丁尚未部署到 panda。

验证：
- panda 19:00 后没有新的 socks5 Docker restart/task-delete 事件；`socks5-check.log` 最后一条为 18:59:46 手动安全探活，4 个容器均 `OK successes=3/3`。
- panda 19:27 load average 为 `0.12/0.11/0.16`。
- ZenProxy 4000 `/health` 正常，nginx 后端采样确认流量分到 `4001/4002/4004` 三实例。
- ZenProxy `/metrics` 当前有历史 `5xx=2`、`retry_budget_exhausted=2`、`client_gone=1`，但并发和 RPM 很低：`active_concurrency=1`、`rpm=2`。
- 本机 Windows Rust 工具链验证通过：`cargo fmt --check`、`cargo test`；结果为库测试 61 条、kernel golden 70 条、doc tests 0 条。

结论：
CPU 持续占用并不高，主要风险是短尖峰：旧 socks5 cron 每 5 分钟因误判批量重启容器是明确放大源，已经禁用并替换为安全脚本。ZenProxy 三实例在跑，当前负载正常。源码层非流式渠道探针补丁已验证，但尚未进入 panda 线上二进制。

未完成：
- 非流式渠道探针源码补丁未部署到 panda：panda 没有 Rust 工具链，本机 WSL rustup 下载 stable toolchain 卡住后已中止；未在生产机强行编译。
- 还未实现更完整的“连续空输出短冷却/熔断”机制；当前只修了渠道探针形态。
- 还未重新跑 NewAPI 管理端真实测渠道按钮，因为该接口需要网页登录态。

下一步：
先准备可控 Linux release 构建环境，再部署非流式渠道探针补丁；随后点 NewAPI channel 69 管理端测试并观察 5 分钟 NewAPI/ZenProxy 日志。若还出现空输出尖峰，再做 per-client/model/request-shape 的短窗口空输出冷却。

## 2026-06-03 20:31

目标：
部署非流式渠道探针兜底，并排查 70k 输入、cache 近乎为 0、ClaudeCode CLI Markdown/表格/代码块/列表格式错乱。

已做：
- 在 `free-model-client-rs` 收窄 body/toolset 客户端识别：普通正文出现 OpenClaw/Hermes 不再作为客户端身份信号；`web_fetch`/`web_search` 不再单独触发 OpenClaw；OpenClaw 仍由 `subagents`、`sessions_*`、`memory_*`、`openclaw*` 等专属工具识别。
- 在 `zen-proxy-rs` 外层 `src/v4/provider.rs` 做同样收窄，避免外层先把 ClaudeCode 请求标成 OpenClaw 后传入内核。
- 新增回归：ClaudeCode + `Task/TodoWrite` + `web_fetch/web_search` 必须识别为 ClaudeCode；普通 OpenClaw/Hermes 文本引用不能覆盖 ClaudeCode 工具信号；Web-only 工具不能识别为 OpenClaw。
- 构建 Linux release，strip 后上传 panda；因 Windows/WSL scp 走 SFTP 很慢，最终使用 gzip + `scp -O` 上传，远端解压后校验 hash。
- 部署到 panda 三实例并滚动重启。

验证：
- `free-model-client-rs`：`cargo fmt -- --check`、`cargo test` 通过；库测试 64/64，kernel golden 70/70，doc tests 0。
- `zen-proxy-rs`：`cargo fmt -- --check`、`cargo test` 通过；主单元测试 129/129，e2e 26/26。
- release hash：未 strip `7ff411a5ae72aeb267044cdbb4aa09b7522891f56a3cb7924153cbbd6ce97f4c`；strip 部署 hash `e47e2c89d7c0c497daa2cd49a9d135a8b695928e951f81a22630b206a0e2ab51`。
- panda 旧 hash：`cdafae89c2f7f3ec7d4b453dd14555c41e9456f5a0d5730456b80ed64fe3c19f`。
- panda 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-profile-format-20260603-202631-cdafae8`。
- panda 新 pid：`zen-proxy-rs@1=365354`、`@2=365378`、`@3=365392`；4001/4002/4004 和 4000 `/health` 均 `status=ok`，dispatch=90、dead=0、ratelimited=0。
- panda `/v1/models` 200，只暴露 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- 受控 `Task + web_fetch` `/v1/messages` 请求进入新 pid 后日志为 `source_client=claude-code`，证明格式误伤的核心识别路径已修正。

结论：
- 70k/60k 输入不是 NewAPI 随机行为，主要来自默认流式 compactor：超过约 80k tokens 后压到约 60k tokens；部署前 ClaudeCode 被误识别为 OpenClaw 时，会错过 ClaudeCode huge-context 的更激进压缩策略。
- cache 近乎为 0 是因为当前上游 usage 基本不返回 provider cache 字段；ZenProxy 当前只转发 `cache_creation_input_tokens`、`cache_read_input_tokens` 或 `cached_tokens`，不伪造缓存命中。
- ClaudeCode CLI 格式错乱的一个明确源头是 profile 误伤：一旦被判成 OpenClaw/Hermes，流式内容会走 Markdown 修复/脱敏路径，不再逐字保留 Markdown 和空白。该识别误伤已修复并部署。

未完成：
- 还没有拿 Windows ClaudeCode 真实终端重新跑格式全集；若 raw SSE 正确但终端仍乱，再归类为 CLI 渲染问题。
- 受控小请求由于上游慢路径 60 秒超时，未作为模型质量验收，只用于 ingress profile 验证。
- 未重新跑四客户端 dry/full 压测。

下一步：
让 Windows ClaudeCode 真实跑一轮 Markdown 格式全集，同时抓 ZenProxy raw SSE 和新 pid `source_client`；若 `source_client=claude-code` 且 raw SSE 正确，则不再从 ZenProxy 修改格式。

## 2026-06-03 21:05

目标：
对 WSL ClaudeCode、WSL Hermes、WSL OpenClaw 做小样本对比，验证 profile 区分是否正确，并收集后续 per-client 适配数据。

已做：
- 从 panda NewAPI token 表选择有效 `vip` 组 token id=38/name=ds，仅写入本地临时 `/tmp/panda_newapi_key.env`，未写入仓库，未输出明文 key。
- 跑 `panda_pressure_runner.py --mode preflight --clients wsl-claudecode,wsl-hermes,wsl-openclaw --models deepseek-v4-flash`。
- 分别跑 WSL Hermes smoke、WSL OpenClaw smoke；三客户端混合 smoke 因 WSL ClaudeCode 连续 timeout 被中止，保留 partial raw results。
- 追加报告 `docs/reports/client-profile-smoke-20260603.md`。

验证：
- preflight 通过：models 200；chat 200；返回 `OK`。
- WSL ClaudeCode partial：2/2 API 路径进入 CLI，但 2/2 `client_timeout`，每条约 90s；runner 只捕获到 system init。
- WSL Hermes smoke：5/5 API ok，5/5 semantic ok；P50 33.7s，P90 44.0s；工具 2/2。
- WSL OpenClaw smoke：5/5 API ok，0/5 semantic ok；错误为 semantic mismatch、tool runtime、context drift、subagent not triggered；输出前缀集中为 `HEARTBEAT_OK`，stderr 有 local secrets gateway 1006 warning。
- ZenProxy 20:50 后识别分布：`claude-code=133`、`hermes=13`、`openclaw=5`。
- ZenProxy metrics：162 个请求均为 `200/success`，`5xx=0`、`timeout=0`、`429=0`，dispatch=90、dead=0、ratelimited=0。

结论：
三类客户端已经能被 ZenProxy 正确区分。当前问题不再是 OpenClaw/Hermes 适配打到 ClaudeCode 上，而是：
1. ClaudeCode 链路存在 CLI timeout，需要单独抓 raw SSE 和 CLI 输出。
2. Hermes 可用但慢，需要分段耗时。
3. OpenClaw API 通但本地 agent/gateway/提示形态不合格，需要先修 OpenClaw harness/config，再谈模型适配。

下一步：
先修 OpenClaw 本地 secrets gateway/config/agent harness 的 `HEARTBEAT_OK` 偏移；同时为 ClaudeCode 单独做 Markdown 格式全集和 timeout 复现。

## 2026-06-03 21:31

目标：
继续复测 WSL ClaudeCode / Hermes / OpenClaw，收集 raw API、CLI、ZenProxy ingress 三侧证据，判断 ClaudeCode 格式错乱和 timeout 是否仍来自 ZenProxy profile 误伤。

已做：
- 通过 panda NewAPI 重新跑 preflight，`/v1/models` 与最小 chat 均通过。
- 跑三客户端 smoke：`20260603-210902`。
- 发现该轮 WSL ClaudeCode 是 root 进程，5/5 被 ClaudeCode 拒绝 `--dangerously-skip-permissions`，该段只作为 runner 环境错误记录。
- 切换到 WSL `lenovo` 用户重跑 WSL ClaudeCode：`20260603-211511`，short prompt 90s timeout，stdout 只到 system init。
- 直接 raw POST panda NewAPI：`/v1/chat/completions` 与 `/v1/messages` 的 stream/non-stream 均能返回 `OK`。
- 用 `x-fmc-client=claude-code` raw `/v1/messages` 跑 Markdown 格式检查，返回表格、代码块、引用块、列表均完整。
- 统计 21:08 后 ZenProxy ingress body 分布，确认三类客户端被正确分开。
- 确认本地无残留 runner/claude/openclaw/hermes 进程；ZenProxy active_concurrency 后续自然回到 0。

验证：
- raw `/v1/messages`：非流式约 2.8s，流式约 3.7s，均返回 `OK`。
- raw Markdown `/v1/messages`：约 31.5s 返回完整 Markdown，`has_table/has_fence/has_quote/has_list` 均为 true。
- 21:08 后 ZenProxy ingress：`claude-code=182`、`hermes=20`、`openclaw=5`。
- ClaudeCode body size：min 118，p50 144533，max 463984；46 个请求超过 200KB。
- Hermes body size：p50 479541，20 个里 14 个超过 200KB。
- OpenClaw body size：5 个均在 50KB-200KB。
- 空目录 ClaudeCode short prompt 仍会产生约 291KB-305KB 请求，估算 56k-59k tokens；还出现约 467KB / 72k tokens 请求；`max_tokens=32000` 被 ZenProxy cap 到 1024。
- 空目录测试中出现 body 342 非流式请求，多次 `non-stream upstream returned empty output` retry。

结论：
- profile 分离已经生效，ClaudeCode 新请求没有被 OpenClaw/Hermes 适配覆盖。
- ZenProxy raw 响应没有破坏 Markdown；若 ClaudeCode 终端仍显示错乱，优先排查 CLI 渲染、流式事件消费、插件/skills/工具 schema 造成的大上下文请求形态。
- ClaudeCode timeout 不是 panda NewAPI/ZenProxy `/v1/messages` 基础链路不可用，而是 ClaudeCode CLI 实际发送的请求远大于表面 prompt，并伴随小非流式空输出重试。
- Hermes 可用但慢，且 tool_read 有 90s timeout。
- OpenClaw API 通但语义不通，仍集中为 `HEARTBEAT_OK` 和 local secrets gateway 1006 warning。

下一步：
专项抓 ClaudeCode 完整 raw SSE 与终端渲染；拆分 ClaudeCode 300KB+ 请求体来源；识别 body 342 非流式空输出请求用途并在 ZenProxy/free-model-client-rs 源头适配；OpenClaw 先修 gateway/harness 后再做模型适配。

## 2026-06-03 22:14

目标：
拆分 ClaudeCode 300KB+ 请求体来源，识别 body 342 非流式空输出请求用途，并检查 Windows ClaudeCode CLI 是否存在输入 token 限制或链路混淆。

已做：
- 读取当前维护文档、backlog 和 21:31 复测记录，确认本轮只做证据采集和待办编排，不改运行代码。
- 拉取 panda 21:35-21:50 ZenProxy 日志并统计 `source_client/body_size/context_action/stream`。
- 阅读 `free-model-client-rs` 的流式 compactor、流式输出 cap、非流式空输出 retry、短探针 fallback 代码。
- 检查 Windows ClaudeCode CLI 版本、路径、环境变量、`settings.json`、插件配置和 cc-switch 监听进程。

## 2026-06-04 16:40

目标：
检查 NewAPI 为什么读不到/不显示 cache，并定位 ClaudeCode 使用中的“输出被截断”到底属于哪一层。

已做：
- 复查维护文档、`src/proxy/openai.rs`、`src/proxy/anthropic.rs`、`src/protocol/translate.rs`、`src/zen/client.rs`、`src/client_profile.rs`、`src/proxy/markdown.rs`。
- 确认非流式 cache 丢失的源头：OpenAI 非流式响应未带 `cache_*`，Anthropic 非流式正文/工具调用把 `cache_*` 写死为 `0`。
- 修复 OpenAI/Anthropic 非流式正文和工具调用两条分支的 cache usage 透传。
- 新增 kernel golden 4 条回归：OpenAI/Anthropic 非流式正文、工具调用均检查 `cache_creation_input_tokens/cache_read_input_tokens` 保留。
- 读取 ClaudeCode 终端样本，确认 `… +N lines (ctrl+o to expand)` 是客户端工具输出折叠，不是 ZenProxy 截断正文。
- 对照源码确认当前流式输出 cap：估算 prompt `>=50k/100k/200k` 时，流式 `max_tokens` 会分别被 cap 到 `1024/768/512`。

验证：
- WSL `lenovo` 用户下执行 `cargo fmt -- --check` 通过。
- WSL `lenovo` 用户下执行 `cargo test -q` 通过：库测试 71 条、kernel golden 82 条。
- WSL `lenovo` 用户下执行 `cargo clippy --all-targets -- -D warnings` 通过。

结论：
- NewAPI cache 不可见至少有一部分是源头问题，现已在源码层修复；但还未部署到 panda，线上不应视为已恢复。
- ClaudeCode 的“输出截断感”当前最明确的源头是服务端流式输出 cap，不是 `ctrl+o` 这类客户端折叠展示，也不是 Markdown fence 修复。

未完成：
- 本轮没有部署到 panda，也还没有拿真实 NewAPI 线上记录复验 cache 展示。
- 还没有调整 ClaudeCode 大流式 1024/768/512 硬 cap；当前只完成根因定位，没有改策略。

下一步：
- 如继续推进，先部署 cache usage 修复到 panda，再用真实 NewAPI 日志确认显示是否恢复。
- 对 ClaudeCode 长会话补 `requested_max_tokens/effective_max_tokens` 观测，再决定是否把流式 cap 改为可配置或按 profile 分桶。

## 2026-06-04 16:55

目标：
继续排查 ClaudeCode 使用 ZenProxy deepseek 时“原本 3 段只输出 1.5 段”的真实原因，不能再用客户端折叠展示解释。

已做：
- 读取 panda 最近两小时 ZenProxy 日志和 NewAPI `logs` 表，按 channel 69 统计 prompt/completion tokens。
- 确认 14:56-14:58 多条 ClaudeCode 工程请求为 26k-40k prompt tokens，并未全部触发 50k+ 输出 cap；大量样本 completion tokens 只有 100-700。
- 确认同窗口存在长输出成功样本：`prompt_tokens=11906`、`completion_tokens=6985`、耗时约 82s，说明模型/链路不是整体不能长输出。
- 确认源码已解析上游 `finish_reason`，但 OpenAI/Anthropic 响应此前固定写成 `stop/end_turn`，会隐藏上游 `length`。
- 确认近期 ClaudeCode 工程请求常见 `last_user_tokens=3`、最大旧消息约 26k、工具 schema 6k-7k，旧工具历史会淹没当前短指令。
- 修复 OpenAI/Anthropic 非流式、流式和 buffered 流式的 `finish_reason/stop_reason` 透传。
- 增加 ClaudeCode 中等工具历史压缩：消息很多、消息 token 24k+、单条旧输出 12k+、最新用户指令 <=1024 tokens 时触发。

验证：
- 新增 OpenAI 非流式/流式 `finish_reason=length` 回归。
- 新增 Anthropic 非流式/流式 `length -> max_tokens` 回归。
- 新增 ClaudeCode 中等工具历史折叠回归。
- WSL `lenovo` 用户下 `cargo fmt -- --check` 通过。
- WSL `lenovo` 用户下 `cargo test -q` 通过：库测试 71 条、kernel golden 87 条。
- WSL `lenovo` 用户下 `cargo clippy --all-targets -- -D warnings` 通过。

结论：
- 用户描述的半截输出不是客户端 `ctrl+o` 折叠能解释的。
- 当前最明确的两个源头是：上游提前结束原因被吞掉；中等 ClaudeCode 工具历史未压缩导致当前短指令被旧输出淹没。
- 50k+ 输出 cap 仍是风险，但不是这批样本的唯一原因。

未完成：
- 本轮仍未部署到 panda。
- 部署后必须用真实 ClaudeCode 工程会话复验 stop_reason、completion tokens 和体感输出完整度。

下一步：
- 部署包含 cache usage、finish_reason 透传和中等工具历史压缩的二进制到 panda。
- 部署后抓 NewAPI 和 ZenProxy 日志，重点看是否出现 `stop_reason=max_tokens`、completion tokens 是否上升、半截输出是否减少。
- 读取 cc-switch SQLite DB 的表结构、provider endpoint、proxy config 和最近 Claude 请求日志；API key/token 均未写入仓库，报告中不记录明文。
- 更新 `docs/04-improvement-backlog.md` 和 `docs/05-ai-maintenance-playbook.md`。

验证：
- 21:35-21:50 ZenProxy ingress 共 38 条：`claude-code=2`、`hermes=36`。
- 21:44 ClaudeCode 两条请求：`body_size=472161/474175`，`context_action=pass`，`stream=true`；free-model-client-rs 日志显示只做输出 cap：`prompt_tokens=72826/73017`，`max_tokens=32000 -> 1024`。
- 21:37-21:48 Hermes 多条请求：`before_tokens≈104k-113k`，压缩后 `after_tokens≈88k-97k`，并将 `max_tokens=32000 -> 768`；这与 NewAPI 截图里 88k-90k input tokens 对得上。
- body 342 线索：21:28:16 出现 `source_client=claude-code`、`stream=false`、`body_size=342` 的 `/v1/messages`，随后 `non-stream upstream returned empty output` retry 三次；21:28:38 又开始下一轮非流式空输出 retry。
- Windows ClaudeCode：版本 `2.1.138`；当前 `ANTHROPIC_BASE_URL=http://127.0.0.1:15721`；端口 15721 由 `D:\SelfMadeTool\CCSwitch-v3.16.0\cc-switch.exe` 监听。
- Windows ClaudeCode settings：启用 `CLAUDE_CODE_EFFORT_LEVEL=max`、`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`、`ENABLE_TOOL_SEARCH=true`，并启用 `glm-plan-bug`、`glm-plan-usage` 插件；本机还安装了 context7、playwright、security-guidance、typescript-lsp、everything-claude-code 等插件。
- cc-switch proxy config：Claude proxy 开启，监听 `127.0.0.1:15721`，日志开启，`max_retries=6`，stream first-byte timeout 90s，non-stream timeout 600s。
- cc-switch 最近 Claude 请求 provider 为 `closedeepseek -> https://sub2api.closeapi.top`；`LocalNewapi -> http://127.0.0.1:4000/v1` 存在但不是最近记录里的 provider。
- cc-switch 最近 20 条 Claude 日志出现 `input_tokens=76728`、多条约 40k，也有 20/27/166/174 等小请求；未发现 cc-switch 层存在 90k 输入 token 墙字段。

结论：
- NewAPI 里看到 70k-90k input tokens 不是已确认的输入墙。至少本轮有两类来源：Hermes/默认流式 compactor 将 104k+ 压到 88k-97k；ClaudeCode 72k-73k 则是未压缩输入，只有输出 max_tokens 被 cap。
- Windows ClaudeCode 最近并非直接打 panda NewAPI channel 69，而是先打 cc-switch；cc-switch 最近又路由到 closeapi 直连 provider。因此 Windows 本机使用日志和 panda NewAPI channel 69 截图不能直接合并归因。
- ClaudeCode 300KB+ 请求体大概率由 ClaudeCode 自带系统提示、工具 schema、plugins/skills、agent 信息、历史上下文和模型别名共同组成；当前还缺少脱敏 request-shape 采样，不能精确给出每项占比。
- body 342 非流式请求基本可以定位为 ClaudeCode 内部小请求或探测/摘要类请求，但当前日志没有保存脱敏形态，不能确认具体用途。

下一步：
实现脱敏 request-shape 采样和小非流式请求分类；为 ClaudeCode profile 输出 `system/messages/tools/largest_message` token 分布；确认 Windows cc-switch 当前 active provider 后，再分别做 `Windows ClaudeCode -> cc-switch` 和 `panda NewAPI channel 69 -> ZenProxy` 两条链路的独立验收。

## 2026-06-03 22:54

目标：
落地第一阶段脱敏 request-shape 采样和 ClaudeCode 小非流式空输出请求分类，先增强源头观测，不调整 compactor 阈值，不新增普通请求 fallback。

已做：
- 在 `src/protocol/translate.rs` 新增 `RequestShape` 和 `ShortNonStreamRequestKind`。
- 在 OpenAI/Anthropic 入口统一记录脱敏 shape：`system_tokens/messages_tokens/tools_tokens/tool_count/message_count/largest_message_tokens/last_user_tokens/estimated_total_tokens/stream/max_tokens/tool_choice_present/prompt_hash/source_client/profile_source`。
- `prompt_hash` 使用消息增量 hash，不再为了 hash 拼出完整 prompt 字符串，降低大上下文请求的额外分配。
- 在非流式空输出 retry 日志增加 `short_request_kind/prompt_hash/prompt_tokens/message_count/max_tokens/source_client`。
- 保持现有 fallback 边界：只有 `hi/hello/test/echo hi` 等 channel-test probe 在上游连续空输出后返回本地 `ok`；普通短请求不被误短路。
- 补单元测试和 kernel golden，覆盖不泄露原文、工具 schema token 计数、channel-test 分类、普通短请求分类、ClaudeCode 小非流式非探针不返回本地 `ok`。
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo fmt -- --check`：通过。
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo clippy --all-targets -- -D warnings`：通过。
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo test`：通过，库测试 69 条、kernel golden 71 条、doc tests 0 条。

结论：
- 第一阶段源码已完成：下次看到 `body_size=342` 或 300KB+ ClaudeCode 请求，可以用脱敏 shape 和 `short_request_kind` 做归因，不再依赖原始 prompt。
- 当前改动没有部署到 panda；线上日志暂时不会出现这些新字段。
- 当前没有改变 compactor 阈值，也没有对 `internal_claude_code_probe` 自动返回本地 `ok`，避免误伤真实用户短请求。

未完成：
- 部署到 panda 后复测真实 ClaudeCode/Hermes/OpenClaw 请求，确认 shape 字段可采集。
- 真实样本确认后，再决定是否给 ClaudeCode 内部小探测加安全 fallback 或短冷却。
- Windows ClaudeCode 仍需按独立链路 `ClaudeCode -> cc-switch(15721) -> provider` 做 raw/CLI 对照，不和 panda channel 69 统计混用。

下一步：
提交前复查 git diff 和未跟踪文件；如要上线，先构建 Linux release，上传 panda 灰度部署，再从 NewAPI/ZenProxy 日志验证新 shape 字段。

## 2026-06-03 23:45

目标：
将 request-shape 观测改动随 `zen-proxy-rs` release 部署到 panda，并验证 NewAPI -> ZenProxy -> free-model-client-rs 链路可用。

已做：
- 确认 `zen-proxy-rs` 通过 path dependency 嵌入本地 `../free-model-client-rs`。
- 本地构建 `zen-proxy-rs` release，并生成 stripped 部署包。
- 上传时普通 `scp` 在 tailnet 上中断，远端只留下 3MB 残片；已删除残片，改用 gzip + rsync `--partial --inplace` 续传成功。
- 备份旧二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-request-shape-20260603-232757-e47e2c8`。
- 替换 `/opt/zen-proxy-rs/zen-proxy-rs`，滚动重启 `zen-proxy-rs@1/@2/@3`。
- 用 panda Postgres 中 token id `38`/name `ds`/group `vip` 做本机 NewAPI smoke；未输出明文 key。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy --all-targets -- -D warnings`、`cargo test` 通过；库测试 69 条、kernel golden 71 条。
- `zen-proxy-rs`：`cargo test` 通过，单元测试 129 条、e2e 26 条；`cargo build --release` 通过。
- 本地未 strip release hash：`1e77a544b5c444b4d626b14327862259cfe1b26221ffbd6f87778c1afc321376`。
- 部署 stripped hash：`28b25370925835bb33aa4142208a5a20f0cf4dcb74ad3ae74c3808d3c2761e2b`。
- 旧线上 hash：`e47e2c89d7c0c497daa2cd49a9d135a8b695928e951f81a22630b206a0e2ab51`。
- 三实例新 pid：4001=`462903`、4002=`462918`、4004=`462981`；三实例和 nginx 均 active，`/health` 200。
- `/metrics`：`zen_proxy_requests_total{status="200"} 6`，`5xx=0`，`timeout=0`，`active_concurrency=0`，dead=0，ratelimited=0。
- panda NewAPI 8081 smoke：HTTP 200，返回 `OK`，总耗时约 4.01s。
- NewAPI 最近日志：id `109472`，channel id `69`，token id `38`，token name `ds`，model `deepseek-v4-flash`，non-stream，use_time `4`，prompt tokens `93`，completion tokens `47`。
- ZenProxy 日志已出现新字段：小非流式样本 `short_request_kind="internal_claude_code_probe"`，并在 retry 日志里带 `prompt_hash/prompt_tokens/message_count/max_tokens`；真实 ClaudeCode 787KB 流式样本记录 `message_count=703`、`system_tokens=5773`、`messages_tokens=78859`、`tools_tokens=12700`、`tool_count=10`、`estimated_total_tokens=97332`。

结论：
- request-shape 观测已在线上生效，能拆出 ClaudeCode 大请求里的 system/messages/tools 占比。
- `body_size=342` 同类小非流式请求现在可通过 `short_request_kind` 归因；当前仍只是观测，不自动本地 `ok`。
- 之前 WSL 侧 502 是本机 proxy 环境误走代理导致，请求没有到 ZenProxy；清空 proxy env 或在 panda 本机测试即可避免误判。

未完成：
- 继续观察更多真实 `internal_claude_code_probe` 样本，判断 retry 是否经常成功、是否需要短冷却或安全 fallback。
- 若后续要从本机 WSL 直测 panda，必须清空 `http_proxy/https_proxy/ALL_PROXY` 并使用 `--noproxy "*"`.

下一步：
跑一小段真实 ClaudeCode/WSL/Hermes/OpenClaw 混合 smoke，基于新 shape 字段分析请求构成和首字/重试关系。

## 2026-06-04 00:12

目标：
排查用户反馈的 “ClaudeCode CLI 用 deepseek-v4-flash 一直反复做”，并解释 NewAPI 中看到 200k+ prompt tokens 记录是否代表上下文未截断。

已做：
- 拉取 panda 当前时间、服务状态、NewAPI channel 69 最近日志和 ZenProxy 三实例 journal。
- 对齐 NewAPI 记录与 ZenProxy `v4 ingress request`、`compacted streaming anthropic context before upstream`、`desensitized request shape before upstream` 日志。
- 阅读 `src/proxy/anthropic.rs`、`src/proxy/openai.rs` 和 `src/protocol/translate.rs`，确认流式/非流式 compactor 边界。
- 检查 panda 当前 CPU/负载和 `/metrics`，确认排查时不是服务器 CPU 打满。

验证：
- 2026-06-03 23:01-23:46 panda channel 69 出现一串 ClaudeCode 大流式请求：body 从约 674KB 增到 788KB，`before_tokens` 约 97k-110k，流式 compactor 后 `after_tokens` 约 66k-79k，`max_tokens` 被 cap 到 1024 或 768。
- 部署 request-shape 后的真实样本显示：`message_count` 从 670 增到 705，`tools_tokens=12700`，`tool_count=10`，`last_user_tokens` 多数只有 36-96，`estimated_total_tokens` 约 96k-98k。
- NewAPI channel 69 最近窗口共 61 条：57 条流式、4 条非流式；53 条 prompt tokens 在 70k-90k，2 条 prompt tokens 超过 200k，2 条 prompt tokens 为 0 且内容是 `status_code=500, upstream returned no assistant content or tool call`。
- 200k+ 两条不是常规流式轮次：id `109370` 为非流式 213248 prompt tokens，id `109461` 为非流式 225416 prompt tokens。23:39:05 对应 ZenProxy 日志显示 `stream_seen_by_zenproxy=false`、`max_tokens=Some(1024)`、`message_count=701`、`largest_message_tokens=26563`、`estimated_total_tokens=128229`。
- 23:38:49 流式请求先出现 NewAPI id `109459`，prompt tokens 记 0，内容为 `upstream returned no assistant content or tool call`；随后 23:39:05 出现非流式大请求 id `109461` 并成功返回。
- 代码确认：`compact_stream_context_with_policy` 只在 `stream_requested=true` 时调用；非流式路径只调用 `non_stream_output_policy` cap 输出 tokens，不压输入上下文。
- panda 排查时负载约 `0.12/0.13/0.10`，ZenProxy `/metrics` 为 `5xx=0`、`timeout=0`、active concurrency 0，问题不是当前服务器 CPU 满载。

结论：
- “200k 记录”不是证明流式路径已经完整吃下 200k 上下文，而是 ClaudeCode 在流式空输出后重发了一条非流式大请求；非流式路径目前没压输入，所以 NewAPI 账面会看到 200k+。
- “一直反复做”的直接证据是同一会话持续追加历史：`message_count` 增长到 700+，最新用户请求只有几十 tokens；当前 compactor 主要压单条大文本，无法折叠大量旧短消息和工具历史，因此 active anchor 仍可能被旧任务轨迹淹没。
- 这不是 profile 误伤导致的 OpenClaw/Hermes 策略打到 ClaudeCode；日志中 `source_client=ClaudeCode`、`profile_source=Header`。

下一步：
从源头修 ClaudeCode huge-context 策略：增加旧短轮次历史折叠/裁剪、给大非流式 fallback 加输入保护、统计 `stream empty -> non-stream fallback` 链路。修复前不要把 70k-90k 或 200k NewAPI 数字当成稳定上下文质量验收。

## 2026-06-04 00:24

目标：
阅读用户提供的 ClaudeCode CLI 实际输出片段，确认 “一直反复做” 是否只是体感，还是可由日志文本证明。

已做：
- 读取用户贴文 `pasted-text.txt`，不复述其中本地 Bearer token。
- 对贴文做机械计数，统计重复动作和错误形态。
- 拉取 panda NewAPI channel 69 最新记录和 00:00 后 ZenProxy 日志，确认贴文末尾后 panda 侧只有一次小 `channel_test`，没有新的大上下文轮次。

验证：
- 贴文中同一目标被重复执行：`开始导出` 出现 11 次，`检查` 16 次，`清理` 7 次，`重启` 10 次。
- 运行状态反复恶化：任务从 `running=23` 增长到 `running=53/74/96/117`，说明模型没有执行 single-flight/幂等保护，重复提交了导出任务。
- QCE/NapCat 状态处理失控：`502` 出现 29 次，`timeout` 出现 29 次，`Interrupted` 出现 7 次，`Invalid tool parameters` 2 次，`API Error: Failed to parse JSON` 1 次。
- 贴文显示模型在扫码成功/即将成功后仍继续执行重启/杀进程命令，导致登录态被破坏；这属于工具执行计划失控，不是单纯 API 不通。
- panda 00:03 仅有小非流式 `channel_test` 请求，`short_request_kind="channel_test"`，上游连续空输出后由探针 fallback 处理；贴文的大量导出/重启循环主要对应更早 23 点段的 ClaudeCode 大上下文链路。

结论：
- 用户贴文直接证实：当前问题是 “长会话上下文漂移 + 工具执行幂等缺失 + 上游空输出/非流式 fallback 膨胀” 叠加，不是简单首字慢或单次请求失败。
- 仅靠保留 200k 上下文不会解决，反而可能保留更多旧错误动作；必须在 ZenProxy/free-model-client-rs 源头对 ClaudeCode 长会话做旧工具历史折叠、最新目标置顶和大非流式 fallback 输入保护。

下一步：
实现 ClaudeCode huge-session compactor，而不是继续扩大上下文窗口：折叠旧 Bash 输出和旧工具结果、保留最新用户目标和最近有效状态、压制非流式大 fallback 对旧历史的二次放大。

## 2026-06-04 00:36

目标：
从源头解决 ClaudeCode deepseek 长会话反复执行旧任务、非流式 200k+ fallback 放大旧历史的问题。

已做：
- 在 `src/protocol/translate.rs` 新增 `compact_claude_code_huge_session_context`。
- huge-session compactor 先执行原有大文本 compactor，再在消息数过多或压缩后仍过大时折叠旧短轮次历史。
- 折叠策略保留系统消息、最近 48 条消息、最新用户 anchor 和少量脱敏状态信号；旧 Bash/工具输出不会整段复制。
- Anthropic/OpenAI 两个入口都接入 ClaudeCode huge-session compactor，覆盖 stream 和 non-stream。
- 非流式输出 cap 改为使用压缩前原始 prompt token 口径，避免大 fallback 压缩后误放宽输出上限。
- 新增回归测试：700 条短历史必须折叠；Anthropic 非流式大 fallback 进上游前必须压缩。
- 构建 `zen-proxy-rs` release，部署到 panda 三实例。

验证：
- `free-model-client-rs`：`cargo fmt -- --check` 通过；`cargo clippy --all-targets -- -D warnings` 通过；`cargo test` 通过，库测试 69 条、kernel golden 73 条。
- `zen-proxy-rs`：`cargo test` 通过，主单测 129 条、e2e 26 条；`cargo build --release` 通过。
- 本地未 strip release hash：`408fa673aef439e849ca9e24d41576810c122faa71724581ce8067e10c04fc80`。
- 部署 stripped hash：`96b954a81978e9348f26341d68626d0a98682c6971611d7802a0850ef771d815`。
- 旧线上 hash：`28b25370925835bb33aa4142208a5a20f0cf4dcb74ad3ae74c3808d3c2761e2b`。
- 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-huge-session-20260604-003040-28b2537`。
- 新 pid：4001=`498728`、4002=`498733`、4004=`498734`；三实例和 nginx active，4000/4001/4002/4004 `/health` 均 200。
- panda 非流式 smoke：517KB 请求、`before_tokens=123371`，线上日志显示 `compacted non-stream anthropic context before upstream`，压到 `after_tokens=9139`、`message_count=51`；NewAPI id `109585` 为 HTTP 200，`prompt_tokens=5647`。
- panda 流式 smoke：522KB 请求、`before_tokens=124597`，线上日志显示 `compacted streaming anthropic context before upstream`，exact-anchor 后 shape 为 `message_count=1`、`estimated_total_tokens=51`；NewAPI id `109593` 为 HTTP 200，`prompt_tokens=125`。

结论：
- 源头修复已部署：ClaudeCode 长会话不再只压单条大文本，700+ 旧短消息会被折叠；非流式大 fallback 也会压输入，不再让 NewAPI 看到同类 200k+ 脏历史。
- 这仍需真实 Windows/WSL ClaudeCode 长会话复测，因为 smoke 证明压缩链路生效，但不能完全等同于真实导出/扫码任务不会再被模型搞乱。

下一步：
用真实 ClaudeCode 长会话复测用户贴文中的导出/扫码/等待外部动作场景；重点验收不重复提交导出、不在用户扫码后继续重启/杀进程、不再出现 200k+ 非流式 fallback 放大旧历史。

## 2026-06-04 10:55

目标：
按严格验收复测 panda NewAPI channel 69，并修复短 non-stream smoke 仍可能 502 的问题。

已做：
- 重新运行 `free-model-client-rs` 的 `cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`。
- 重新运行 `zen-proxy-rs` 的 `cargo test` 和 `cargo build --release`。
- 在 panda 本机用有效 NewAPI token 测 `/v1/models`、OpenAI `/v1/chat/completions`、Anthropic `/v1/messages`，覆盖两个 deepseek 模型、stream/non-stream。
- 发现并修复：极短 non-stream smoke 经 NewAPI 转为 ClaudeCode/Anthropic 小请求后，上游连续空输出会在 `internal_claude_code_probe` 分类下裸透 502。
- 新增 `short_no_tool_empty_fallback_text`，只对显式测试探针兜底；普通 ClaudeCode 短输入仍不兜底。
- 部署新 stripped 二进制到 panda 三实例。

验证：
- 修复前：panda 本机 NewAPI 短 non-stream `deepseek-v4-flash` 返回 502，错误为 `upstream retry budget exhausted ... last_error=empty_output`。
- 修复后 `free-model-client-rs` 测试通过：库测试 70 条，kernel golden 75 条。
- 修复后 `zen-proxy-rs` 测试通过：主单测 129 条，e2e 26 条；release build 通过。
- 新部署 hash：`0f1d7a36fdc7142e1acd9670301e7277ca6805e47899490958a2c390c619cea5`。
- 旧线上 hash：`96b954a81978e9348f26341d68626d0a98682c6971611d7802a0850ef771d815`。
- 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-strict-smoke-20260604-105132-96b954a`。
- panda 本机 NewAPI `/v1/models` 返回 200，模型数 8，包含 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- 两个 deepseek 模型的 OpenAI/Anthropic、stream/non-stream 共 8 条 smoke 全部 HTTP 200，内容摘要均为 `PASS`。
- 耗时：non-stream 总耗时约 4.8-5.5s；stream 首内容约 2.0-2.3s。
- 线上日志显示短探针仍会先尝试上游，连续空输出后走本地兜底，不再裸透 502。
- WSL 到 `100.69.228.93:8081/v1/models` 直连返回 200；Windows `curl --noproxy '*'` 直连返回 200。
- Windows `Invoke-RestMethod` 走环境变量 `HTTP_PROXY=http://127.0.0.1:7897` 时仍返回 502，这是 Windows 代理路径问题，不是 panda 本机链路问题。

结论：
- 本轮严格验收暴露的 deepseek channel 69 短 non-stream 502 已从源头修复并部署。
- NewAPI -> ZenProxy -> free-model-client-rs 对两个 deepseek 模型的最小 OpenAI/Anthropic、stream/non-stream 链路已通过。
- Windows 客户端后续验收必须显式确认 no-proxy/Tailscale bypass，否则可能把本机代理 502 误判为 ZenProxy 失败。

## 2026-06-04 11:14

目标：
解释 NewAPI 日志中偶发 `status_code=500, upstream returned no assistant content or tool call` 的含义，并把问题记录为后续待办；同时检查 `deepseek-v4-flash` web search 能力边界。

已做：
- 对齐 panda NewAPI 日志和 `zen-proxy-rs@1/@2/@3` journal。
- 阅读 `free-model-client-rs` 的空上游错误、Anthropic stream/non-stream、ClaudeCode huge buffered stream 和输出 cap 代码。
- 统计近 4 小时 NewAPI `deepseek-v4-flash` 成功/失败样本。
- 搜索项目文档和源码中的 `web_search/web_fetch` 相关逻辑。
- 回写 `docs/04-improvement-backlog.md` 和 `docs/05-ai-maintenance-playbook.md`。

验证：
- 近 4 小时 `deepseek-v4-flash` 成功消费 133 条；空 assistant 错误事件 4 次，分别在 10:43、10:58、11:03、11:04。
- 失败样本主要是 ClaudeCode Anthropic `/v1/messages` 流式请求，`source_client=ClaudeCode`，大上下文加工具 schema。
- 失败样本的 `max_tokens=32000` 被 cap 到 768 或 1024；当前 ClaudeCode huge buffered retry 只覆盖 `max_tokens <= 512`，因此这些请求绕过 buffered retry。
- 代码确认：普通流式分支在上游结束后若 `text` 为空且 `tool_calls` 为空，会发送 `upstream returned no assistant content or tool call`。
- 后续非流式 fallback 有成功样本，说明不是 channel 69 整体不可用。
- 现有文档显示：OpenClaw `web_fetch` 小矩阵曾通过；Hermes web 用例命令成功但返回 `WEB_FAIL`，仍需拆分是未触发工具、工具执行失败、工具结果未被使用还是模型判断错误。

结论：
- 该 500 的根因是大 ClaudeCode 流式请求偶发上游空输出且未进入 buffered retry；NewAPI 只是记录了这个错误。
- `deepseek-v4-flash-free` 不能被视为“原生支持联网搜索”的模型；它最多通过 tools/function calling 请求外部搜索工具，真正联网动作必须由客户端/MCP/后端执行。
- 本仓库当前职责是协议规范化和工具调用转发，不自带搜索引擎；web search 用不了时必须按三段链路验收，而不是只问模型是否支持。

未完成：
- 扩大 ClaudeCode huge stream buffered retry 覆盖 768/1024 cap 桶。
- 给普通流式空输出补更细结构化日志。
- 为 ClaudeCode/Hermes/OpenClaw 分别做 web 工具定义、tool call、工具结果使用的最小验收。

下一步：
先修 ClaudeCode 大流式空 assistant 500；随后用 panda NewAPI 对三客户端 web 工具链路做脱敏采样和分段验收。

## 2026-06-04 12:24

目标：
执行 ClaudeCode 大流式空 assistant 500 修复，并把 ClaudeCode/Hermes/OpenClaw/web-search 的真实边界测清楚。

已做：
- 修复 `src/proxy/anthropic.rs`：移除 `handle_stream` 内部多余的 `max_tokens <= 512` 二次门槛，让外层判定的大 ClaudeCode 流式请求直接进入 buffered retry。
- 新增 kernel golden：`claude_code_huge_stream_uses_buffer_retry_after_1024_output_cap`，覆盖 `max_tokens=32000 -> 1024`、第一次上游空输出、第二次 retry 成功。
- 构建 `zen-proxy-rs` release，上传并部署到 panda 三实例。
- 用有效 panda NewAPI key/base URL 做 preflight、Windows ClaudeCode、Hermes、OpenClaw、直连 web tool 和 Windows ClaudeCode WebSearch 对照。
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy -D warnings`、`cargo test` 通过；库测试 71 条、kernel golden 76 条。
- `zen-proxy-rs`：主单测 129 条、e2e 26 条、release build 通过。
- 本地未 strip release hash：`bcedd35fa53e16eecbad76725aef6ae62aa625290ba129976229e0543017fded`。
- panda stripped hash：`7a8f4e5dc99e8ccf1aaf6562519d8353dc4ba5205e5e55f521c265b0760ed66e`；旧线上 hash `117b3cbfaf058fbbeb258f98542afc09a097e763359f34d174414b47dfd11aff` 已备份。
- panda health：`zen-proxy-rs@1/@2/@3` active；`/health` 返回 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`。
- NewAPI preflight：`/v1/models` 200，模型数 8；最小 `deepseek-v4-flash` chat 200，返回 `OK`。
- Windows ClaudeCode 显式 panda base/key：5/5 通过，P50 约 4.3s，P90 约 7.8s，tool 2/2。
- WSL Hermes：5/5 通过，P50 约 34.7s，P90 约 38.9s，tool 2/2。
- WSL OpenClaw：API 5/5 通，但 semantic 0/5，输出固定 `HEARTBEAT_OK`，stderr 为 local secrets gateway `1006 abnormal closure`。
- WSL ClaudeCode：当前 launcher 实际启动 `/root/.clawgod/cli.cjs`，不是有效 ClaudeCode；测试进程挂住后已清理。
- 直连 web tools：清空 WSL proxy env 后，Anthropic 和 OpenAI 两种协议均返回 `web_search` tool call。
- Windows ClaudeCode WebSearch：`--tools WebSearch,WebFetch` 下 `tools_seen=[]`，模型只输出普通文本伪 function call；`--tools default` 只有 `Bash/Edit/PowerShell/Read`。

结论：
- `upstream returned no assistant content or tool call` 的 768/1024 大流式绕过 retry bug 已从源头修复并部署。
- panda NewAPI -> ZenProxy -> free-model-client-rs 链路当前可用；Windows ClaudeCode 和 Hermes 能通过显式 panda base/key 工作。
- WSL ClaudeCode 不是有效客户端；OpenClaw 当前是本地 gateway/harness 问题；ClaudeCode WebSearch 不可用是客户端没有注册 WebSearch/WebFetch，不是 ZenProxy 不会转发工具调用。

未完成：
- 观察新 hash 后 NewAPI 是否还出现同类空 assistant 500。
- 修复 WSL ClaudeCode launcher 或换成真实 ClaudeCode CLI。
- 修复 OpenClaw local secrets gateway / agent harness 的 `HEARTBEAT_OK` 问题。
- 若要 ClaudeCode 真实 web search，需要接入可执行 WebSearch/WebFetch 的 MCP 或使用 Bash/PowerShell/curl 类工具。

下一步：
先处理客户端侧两个阻塞：WSL ClaudeCode 可执行文件和 OpenClaw gateway/harness；随后重跑四客户端 smoke，再进入 dry/full 压测。

## 2026-06-04 14:10

目标：
纠正 ClaudeCode WebSearch 误判，并修复 ZenProxy/free-model-client-rs 路径下工具名不匹配导致的 WebSearch/Task 不执行问题。

已做：
- 用户截图证明：Windows ClaudeCode 在官方 Claude 模型路径下可以真实执行 `WebSearch/WebFetch`。此前“ClaudeCode 没注册 WebSearch/WebFetch”的说法只适用于当时那次受控 ZenProxy 样本，不能作为 ClaudeCode 能力结论。
- 定位到 free-model-client-rs 出口问题：上游可能返回 `web_search/task` 等小写或下划线工具名，旧代码在 OpenAI/Anthropic stream/non-stream 出口原样返回；ClaudeCode 注册工具名是 `WebSearch/Task`，名称不一致时不会执行工具或 subagent。
- 在 `src/synthesis/tool.rs` 新增 tool call name canonicalization：按原始请求 `tools` 表用去符号、小写归一匹配，把 `web_search -> WebSearch`、`task -> Task` 回填成客户端注册名。
- 在 `src/proxy/openai.rs`、`src/proxy/anthropic.rs` 的 stream/non-stream/buffered stream 工具调用出口接入 canonicalization。
- 新增 kernel golden：
  - `claude_code_anthropic_stream_canonicalizes_web_tool_name`
  - `claude_code_anthropic_nonstream_canonicalizes_task_tool_name`
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- 先跑新增测试，确认旧行为失败：返回 `web_search/task`，断言 `WebSearch/Task` 失败。
- 修复后新增测试通过。
- `free-model-client-rs`：
  - `cargo fmt -- --check` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。
  - `cargo test` 通过：库测试 71 条、kernel golden 78 条。
- `zen-proxy-rs` path 依赖重新编译后：
  - `cargo test` 通过：主单测 129 条、e2e 26 条。
  - `cargo build --release` 通过。

部署：
- 用户确认“部署”后，将 `/home/lenovo/zen-proxy-rs/target/release/zen-proxy-rs` 重新 strip、上传到 panda，并安装到 `/opt/zen-proxy-rs/zen-proxy-rs`。
- 远端 stripped hash：`0f6cdf6e5cd2dd1946a69707c97591cca865b47178ff63846f04bbdf283f2314`。
- 备份路径：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-tool-canon-20260604-142705-0f6cdf6`。
- `zen-proxy-rs@1/@2/@3` 重启后均为 `active`。
- `/health`：4000/4001/4002/4004 均返回 `status=ok`，dispatch 90、dead 0、ratelimited 0、upstream backoff false。
- `/metrics`：`zen_proxy_requests_total{status="5xx"} 0`，`active_concurrency 0`。

线上 smoke：
- 直连 panda ZenProxy `/v1/messages`，`x-fmc-client=claude-code`，工具表只注册 `WebSearch`：HTTP 200，`tool_use_names=WebSearch`，`stop_reason=tool_use`。
- 强制 `tool_choice=WebSearch` 的 smoke 返回上游 400：`Thinking mode does not support this tool_choice`，因此真实 smoke 改为 auto tool。该 400 是上游 DeepSeek 限制，不是部署失败。
- 直连 panda ZenProxy `/v1/messages`，工具表只注册 `Task`：HTTP 200，`tool_use_names=Task`，`stop_reason=tool_use`；期间上游空输出重试后成功。

结论：
- 这次不是 ClaudeCode 不能 WebSearch；是 ZenProxy/free-model-client-rs 对非 Claude 上游返回的工具名没有回填为 ClaudeCode 注册名。
- 官方 Claude 能跑，是因为它原生按 ClaudeCode 期望返回 `WebSearch/WebFetch`。DeepSeek/free 上游更容易返回 `web_search/task` 或伪调用，ZenProxy 必须做协议保真适配。
- 修复已部署到 panda，直连 ZenProxy 层的 WebSearch/Task 工具名 canonicalization 已验收通过。

未完成：
- 通过用户本机 Windows ClaudeCode -> NewAPI -> ZenProxy 的完整交互再跑一轮真实 WebSearch/WebFetch。
- 如果模型仍输出普通文本伪函数调用，而不是 tool call，下一步再做“伪函数调用文本 -> 受控 tool_use 修复”的风险评估；当前未实现该高风险转换。

## 2026-06-04 17:01

目标：
执行输出限制、缓存/截断、客户端适配分流的剩余源头改动；用 subagent 检查“为什么要输出限制、能不能不加限制”，并统一把结论落到源码和维护文档。

已做：
- 拉起两个 `gpt-5.5/xhigh` subagent：A 检查输出限制必要性，B 检查 ClaudeCode/Hermes/OpenClaw 识别与格式误伤边界。第三个 subagent 因线程 agent 数上限未创建，本地接手缓存/截断/websearch 归因。
- 修复 WSL rustup 半安装状态：`stable-x86_64-unknown-linux-gnu` 重新安装成功；后续验证使用 WSL 原生路径和 `/tmp/free-model-client-rs-target`。
- 新增 `ClientProfile::effective_for_model`：识别 profile 和有效策略 profile 分离，日志保留真实 `source_client`，同时记录 `effective_client`。
- 模型策略分流：`deepseek-v4-flash/deepseek-v4-flash-free` 不再应用 Hermes/OpenClaw 兼容策略，只保留 ClaudeCode 策略；`deepseek-v4-flash-lite/big-pickle` 不再应用 ClaudeCode 策略，只保留 Hermes/OpenClaw 策略。
- 调整流式输出策略：非流式 guard 保留；流式显式 `max_tokens` 不再因输入达到 50k/100k/200k 被硬降到 1024/768/512。
- 收窄 ClaudeCode Anthropic buffered stream：`max_tokens <= 512`、50k+ 输入且 `max_tokens <= 2048`、exact-output fallback 仍可 retry；20k/32k 长输出直接流式返回，避免完整缓冲后一次性回放。
- 增加 `reply PONG only` 短 smoke 兜底，只在无工具、`max_tokens <= 64` 且上游连续空输出时生效。
- 回写 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- 定向测试通过：
  - `stream_output_policy_preserves_explicit_max_tokens_by_prompt_size`
  - `deepseek_flash_*`
  - `deepseek_flash_lite_*`
- `kernel_golden` 通过：90/90。
- 最终完整验证通过：
  - `cargo fmt -- --check`
  - `cargo clippy --all-targets -- -D warnings`
  - `cargo test`：库测试 77 条、kernel golden 90 条、doc tests 0 条。

结论：
- 该轮临时结论是保留非流式 guard、流式尊重显式大输出；17:55 后已被最新决策覆盖，当前维护文档以“输出限制完全取消、真实 panda policy 验收待跑”为准。
- ClaudeCode 格式问题的主要风险仍是 profile 误判或模型策略误套；本轮已把真实识别和有效策略拆开，避免 Hermes/OpenClaw 适配继续污染 flash 的 ClaudeCode 使用。

未完成：
- 尚未把本轮 free-model-client-rs 改动构建进 `zen-proxy-rs` release 并部署到 panda。
- 尚未用用户侧 Windows ClaudeCode -> NewAPI -> ZenProxy 完整链路复验长输出、WebSearch/WebFetch、cache 展示和 `finish_reason`。
- OpenClaw local gateway/harness `HEARTBEAT_OK` 阻塞仍未解决。

下一步：
如需要上线，先在 `zen-proxy-rs` path dependency 重新 build release，部署 panda 三实例；随后跑最小 smoke：models、OpenAI/Anthropic PONG、ClaudeCode 长流式 20k/32k、WebSearch/Task 工具名、NewAPI cache/finish_reason 展示。

## 2026-06-04 18:01

目标：
按最新要求彻底取消输出限制；flash/free 只保留 ClaudeCode 深度适配并取消输入墙；lite/big-pickle 只保留 Hermes/OpenClaw 适配；补 cache 四态和 provider usage 信号；补 policy harness；处理 ZenProxy 外层 compactor 是否仍压 flash 输入。

已做：
- 新建分支 `codex/v47-client-split-cache-harness`。
- `free-model-client-rs`：输出策略改为 `Option<u64>` 原样透传；缺省 `max_tokens` 不再补 1024/2048；显式 `max_tokens` 不再按输入桶 cap。
- `free-model-client-rs`：`deepseek-v4-flash/deepseek-v4-flash-free` 取消 Hermes/OpenClaw effective policy，只保留 ClaudeCode；`deepseek-v4-flash-lite/big-pickle` 取消 ClaudeCode effective policy，只保留 Hermes/OpenClaw。
- `free-model-client-rs`：flash/free 输入只做 request-shape 观测，不再走内核 compactor。
- `free-model-client-rs`：新增 cache 观测四态 `attempted/accepted/rejected/ignored`，并记录 provider response/header/body usage 信号。
- `scripts/panda_pressure_runner.py`：新增 `policy-smoke` / `policy-dry`，覆盖 input/output wall、provider header/body usage、cache 四态和 lite effective profile。
- `zen-proxy-rs`：取消 non-stream output guard 的改写/拒绝；外层 V4 context compactor 对 flash/free 改为只 warn/pass，不 compact、不 token-target reject；lite 仍保留 compactor。
- `zen-proxy-rs` e2e：新增 flash 大输入不压缩测试，保留 lite 大输入会 compact 的测试；mock upstream body limit 调整为 8MB，避免测试 harness 自己误报 413。
- 维护文档同步：current state、roadmap、backlog、playbook、panda 压测方案、客户端策略方案已写入最新边界和剩余验收。

验证：
- `free-model-client-rs` 已通过：`python3 -B -m unittest discover -s tests -p test_panda_pressure_runner.py`，4/4。
- `zen-proxy-rs` 已通过：`cargo test -q v4::context::tests`，12/12；`cargo test --test e2e_integration -- --test-threads=1 --nocapture`，27/27。
- subagent Rawls 独立确认外层风险真实存在，并复跑 flash pass-through、lite compactor、nonstream guard 相关测试通过。

结论：
- 本轮源码层已经取消输出墙；flash/free 的输入墙已从 `free-model-client-rs` 和 `zen-proxy-rs` 外层同时取消。
- 这不是全局关闭 compactor：lite/big-pickle 仍保留大上下文压缩保护。
- 真实 panda `policy-smoke/policy-dry` 尚未执行，不能写成生产已验证；输出限制取消后的上游 413、超时、空输出、延迟和成本风险必须靠 panda policy/dry 数据确认。

未完成：
- 还未部署本轮 v47 到 panda。
- 还未跑真实 panda `policy-smoke/policy-dry`。
- 还未跑四客户端 dry/full 压测。
- OpenClaw local gateway/harness `HEARTBEAT_OK` 和 WSL ClaudeCode launcher 问题仍是后续正式压测阻塞。

下一步：
先完成本地全量验证、git diff 审查和提交；如用户批准部署，再构建 `zen-proxy-rs` release 部署 panda，并先跑 `policy-smoke/policy-dry`，通过后再进入四客户端 dry/full。

## 2026-06-04 18:58

目标：
排查用户反馈的 ClaudeCode 反复声称“上下文被压缩/我看不到上一轮指令”的问题；确认 v47 是否已解决，若未部署则部署到 panda 并做最小验证。

已做：
- 通过用户截图定位到模型回复中直接引用了 `[free-model-client-rs context compactor: folded stale ClaudeCode tool/session history]`，确认这不是用户手动 compact，而是旧版 `free-model-client-rs` 自动 ClaudeCode compactor 注入的折叠提示。
- 本地代码确认 v47 已在 `deepseek-v4-flash/deepseek-v4-flash-free` 路径调用 `model_disables_input_compaction`，只做 `observe_context`，不再调用 `compact_claude_code_huge_session_context`；ZenProxy 外层也对 flash/free 只 warn/pass。
- 远端部署前日志确认旧线上仍在压缩和 cap：18:19-18:21 仍有 `compacted streaming anthropic context before upstream` 与 `capped streaming anthropic max_tokens ... effective_max_tokens=512`，模型为 `deepseek-v4-flash-free`。
- 构建 `/home/lenovo/zen-proxy-rs` release，依赖本地 `free-model-client-rs` v47 commit `619f2f0` 和 `zen-proxy-rs` commit `21feba2`。
- Windows `scp` 大文件上传在 8-12MB 处卡住，改为本地 strip 后 512KB base64 分片上传；远端文件 hash 校验通过。
- 部署 panda `/opt/zen-proxy-rs/zen-proxy-rs`，旧 hash `694036f6a130e8211b998a5b58eff36105fb48fb866ec57ebbb2c03ccfb5f3d7`，新 stripped hash `99424602ce7c076671579abf48ca0d27367ac126e514efe4403d902d5caecd78`，备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-v47-20260604-185423-694036f`。
- 滚动重启 `zen-proxy-rs@1/@2/@3`，新 pid 为 `1093754/1093766/1093777`。

验证：
- 三实例 `/health` 均返回 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 短请求返回 `V47_SHORT_OK`，HTTP 200，约 3.9s。
- OpenAI 非流式 `deepseek-v4-flash` 大上下文请求：361 条消息、请求体约 560KB、`max_tokens=32000`，返回 `V47_NO_COMPACTOR_OK`，HTTP 200，约 7.0s，NewAPI usage 约 `prompt_tokens=97836`。
- 部署后日志 grep `compacted .*context|capped .*max_tokens|context compactor|effective_max_tokens` 无命中。
- 大请求日志显示 `context_action=pass`、`effective_body_size=560709`、`max_tokens=Some(32000)`，证明 flash/free 线上不再被 ZenProxy 外层或 free-model-client-rs 内核折叠，也不再 cap 输出。

结论：
- 用户看到的“被压缩过了/不记得上一轮具体内容”是旧线上自动 compactor 的真实副作用，不是用户手动触发 compact。
- v47 已部署并通过手工 smoke 证明 flash/free 这条链路不再注入 `context compactor` 折叠块，`max_tokens=32000` 已原样透传。
- cache 本轮大请求记录为 `cache_observation="rejected"` 且 `provider_body_cached_tokens=Some(0)`，说明 provider 返回 usage/cache 信号但未命中缓存。

未完成：
- 真实 panda `policy-smoke/policy-dry` 仍未跑。
- 四客户端 dry/full 压测仍未跑。
- 用户本机 Windows ClaudeCode 真实长会话还需要继续观察是否还有模型自身短视或上游空输出问题；若日志不再出现 compactor/cap，则下一步应看上游质量、cache、工具调用和 ClaudeCode 客户端状态。

## 2026-06-05 00:58

目标：
落地 V4.98 cache-friendly session，尽量提高长会话 provider cache 命中机会，同时不降低输入大小、不裁剪上下文、不影响 ClaudeCode 输出质量。

已做：
- 复核 panda 真实数据：最近约 70 分钟 `deepseek-v4-flash` `/v1/messages` 77/77 流式，prompt tokens P50 约 326k、P90 约 331k，cache hits 0，`frt` P50 约 3.8s、P90 约 5.1s，总耗时 P50 约 14s、P90 约 39s。
- 确认 330k 不是输入 token 墙：ZenProxy 线上日志为 `context_action=pass`，`effective_body_size=body_size`；free-model-client-rs `messages_tokens` 连续增长到 331k+。
- 确认 cache 不是完全不可用：8 小时窗口内存在 `prompt_tokens=286975/cache_tokens=462592`、`prompt_tokens=439847/cache_tokens=439808` 等命中。
- 定位 cache 命中差异：完全相同 `prompt_hash` 的重试样本能命中；长会话尾部持续增长的样本不命中。
- 写入失败证明：旧逻辑下，同一 1.2MB 前缀只追加尾部对话会导致 `x-opencode-session` 变化。
- 修复 `src/zen/client.rs`：大请求 session scope 改为 `large_prefix_v498`，使用稳定前缀 hash + tools hash + tool_choice hash；保留模型、api key hash、时间桶隔离。
- 扩展 `RequestShape` 和日志：新增 `prefix_4k_hash/prefix_32k_hash/prefix_128k_hash/prefix_256k_hash/cache_material_bytes`，同时出现在 request-shape 和 cache observation 日志。
- 新增回归：大前缀稳定只追加尾部时，session 与 prefix hash 保持稳定；前缀变化时 prefix hash 变化。
- 同步 `docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

当前验证：
- `cargo test -q opencode_session` 通过：4/4。
- `cargo test -q request_shape_prefix_hashes_stay_stable_when_large_tail_grows` 通过：1/1。
- `cargo fmt -- --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- `cargo test` 通过：库测试 83 条、kernel golden 95 条、doc tests 0 条。
- `zen-proxy-rs` 集成验证通过：`cargo test -q v4::context::tests` 12/12，`cargo build --release` 成功，确认 V4.98 path dependency 能编进最终二进制。

结论：
- V4.98 是 cache/session 友好优化，不是上下文裁剪策略。
- 该改动不修改请求正文、消息顺序、`max_tokens` 或用户提示词，因此理论上不应降低回答质量。
- 真实 cache 命中提升需要部署 panda 后用同一 ClaudeCode 长会话 A/B 观察，不能只凭源码测试下生产结论。

未完成：
- 尚未部署 panda。
- 尚未做部署前后 cache hit rate、`frt`、总耗时、工具错误和回答质量对比。

## 2026-06-05 09:18

目标：
检查 V4.98 是否存在明显问题，并部署到 panda。

已做：
- 重新执行上线前检查：`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`、`zen-proxy-rs` `v4::context::tests`、`cargo build --release` 均通过。
- 构建并 strip `zen-proxy-rs` release；部署包 SHA256 为 `566e1c519056a4d2ee95697803d0e8bff9db40dc706c81ab753d70405edfb224`。
- 通过 Windows SSH 部署到 panda `/opt/zen-proxy-rs/zen-proxy-rs`，旧线上 V47 hash `99424602ce7c076671579abf48ca0d27367ac126e514efe4403d902d5caecd78` 备份为 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-v498-20260605-091813-9942460`。
- 滚动重启 `zen-proxy-rs@1/@2/@3`。

验证：
- `zen-proxy-rs@1/@2/@3` 与 nginx 均为 active。
- 4001/4002/4004/4000 `/health` 均为 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- 4000 `/v1/models` 返回两个公开模型：`deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- panda NewAPI 8081 `/v1/models` 200，包含两个 deepseek 模型。
- NewAPI exact smoke `reply pong only` 返回 `PONG`；真实中文短问答返回 200。
- NewAPI 真实英文短问答返回 502 `upstream returned no assistant content or tool call`；用 V47 备份二进制在 4011 临时实例同 prompt 对照也返回 502，证明该问题不是 V4.98 引入。
- 部署后日志已出现 V4.98 `prefix_4k_hash/prefix_32k_hash/prefix_128k_hash/prefix_256k_hash/cache_material_bytes` 和 `cache_observation` 字段。

结论：
- V4.98 已部署到 panda，基础服务、模型列表和最小链路可用。
- 当前仍存在既有上游空输出/节点质量问题，尤其是真实短问答可能在 60s 重试预算后 502；这不是 V4.98 的新增回归，但会影响后续体验和压测。
- V4.98 的真实收益仍必须通过同一 ClaudeCode 长会话部署前后窗口对比来判断，重点看 prefix hash 稳定性、cache tokens、`frt`、总耗时和工具错误。

未完成：
- 尚未跑同一 ClaudeCode 长会话的 V4.98 cache A/B。
- 尚未继续修复/隔离既有上游空输出问题。

## 2026-06-05 10:35

目标：
落地 V4.99 reasoning-aware output guard，修复 V4.98 后仍存在的短/中非流式 reasoning-only 空正文 502，同时不回滚 cache-friendly session、不恢复输入墙/输出墙、不全局禁用 ClaudeCode 主会话 thinking。

已做：
- 深入复核 `src/client_profile.rs`、`src/proxy/openai.rs`、`src/proxy/anthropic.rs`、`src/proxy/mod.rs`、`src/zen/client.rs` 和相关 golden tests。
- 确认当前根因链：上游返回 `reasoning_content` 但 `content/tool_calls` 为空，且常见 `finish_reason=length`；旧逻辑只看正文/工具调用，因此返回 `upstream returned no assistant content or tool call`。
- 新增共享输出分类：`valid/empty_output/reasoning_only/reasoning_only_length`。
- OpenAI/Anthropic 非流式路径遇到 `reasoning_only_length` 时只重试一次 `thinking: disabled`；普通空输出仍按原空输出保护，不伪造答案。
- 流式路径增加 reasoning-only 分类日志和错误文本；流已开始后不做不安全重试。
- ClaudeCode Anthropic buffered stream 触发条件收窄：不再仅因 `max_tokens<=512` 进入 huge buffered，避免小流式请求被拖进大上下文保护路径。
- 新增 `set_thinking_disabled_if_absent`，不会覆盖客户端显式传入的 thinking。
- 新增/更新 golden tests：OpenAI/Anthropic 非流式 reasoning-only-length disabled retry、小流式低预算直接 disabled 而非 buffered retry、普通小非流式非探针仍返回分类 502。
- 同步 `docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`。

验证：
- `cargo fmt` 已执行。
- `CARGO_INCREMENTAL=0 cargo test` 通过：库测试 83 条、kernel golden 97 条、doc tests 0 条。
- `CARGO_INCREMENTAL=0 cargo clippy --all-targets -- -D warnings` 通过。

结论：
- V4.99 源码已落地，目标是把 reasoning-only 低预算空正文从裸 502 改为一次精准 disabled retry 或清晰分类错误。
- 该改动不影响 V4.98 大请求 session/cache 前缀策略，不裁剪 flash/free 输入，不限制输出。
- ClaudeCode 大流式主会话、工具请求和长上下文仍不会默认 `thinking: disabled`。

未完成：
- 尚未部署 panda。
- 尚未通过 panda NewAPI 验证真实短非流式/小流式 reasoning-only 502 是否下降。
- 尚未把本仓库细分类同步到 ZenProxy `/metrics`。

## 2026-06-05 10:52

目标：
部署 V4.99 reasoning-aware output guard 到 panda，并完成最小线上验收。

已做：
- 确认 `zen-proxy-rs` 通过 `free-model-client-rs = { path = "../free-model-client-rs" }` 嵌入当前内核；`free-model-client-rs` HEAD 为 `93c5890`，`zen-proxy-rs` HEAD 为 `21feba2`。
- WSL/Linux 侧执行 `CARGO_INCREMENTAL=0 cargo test -q v4::context::tests`，12/12 通过。
- WSL/Linux 侧执行 `CARGO_INCREMENTAL=0 cargo build --release`，并 strip 生成部署包。
- 部署包 SHA256：`8f8513c418c40704bd50c8ce73f27696fdc9fbb1aa75290f2829cedd9eb9e2f2`。
- 上传到 panda `/tmp/zen-proxy-rs.v499`，远端 hash 校验一致。
- 备份旧 V4.98 线上二进制：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-v499-20260605-104718-566e1c5`。
- 替换 `/opt/zen-proxy-rs/zen-proxy-rs` 并滚动重启 `zen-proxy-rs@1/@2/@3`。
- 记录一次操作细节：前两次远端多行命令被 PowerShell 引用规则污染，第一次只完成 hash 校验，第二次意外执行了旧二进制并因端口占用退出；确认线上 hash 未变化后，改用 stdin 脚本完成备份和替换。

验证：
- `zen-proxy-rs@1/@2/@3` 与 nginx 均 active。
- 4001/4002/4004/4000 `/health` 均为 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- 线上 `/opt/zen-proxy-rs/zen-proxy-rs` SHA256 为 `8f8513c418c40704bd50c8ce73f27696fdc9fbb1aa75290f2829cedd9eb9e2f2`。
- 4000 `/v1/models` 返回 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- panda NewAPI 8081 `/v1/models` 200，包含两个 deepseek 模型。
- panda NewAPI OpenAI 非流式短问答 200，约 2.03s，返回 `2+2 equals 4.`。
- panda NewAPI Anthropic 流式 exact prompt 返回 `STREAM_OK` 且无 error。
- panda NewAPI Anthropic 非 exact 小流式返回正常 greeting 且无 error；日志显示该请求 `protocol="anthropic"`，未因 `max_tokens=64` 进入 `anthropic_buffered`。
- 部署后日志出现 V4.99 `applied upstream thinking policy`、`thinking_policy="low_budget_probe_disabled"`、request shape 与 provider cache observation 字段。
- 部署后最小观察窗口内未见 `empty_output_class`、`upstream returned no assistant`、`stream error`、`retry budget`、`client_gone`。

结论：
- V4.99 已部署到 panda，基础服务和 NewAPI 目标链路可用。
- 直连 4000 chat 使用 `sk-dev` 或 NewAPI 用户 key 会返回 `invalid proxy api key`，说明生产直连 ZenProxy key 与 NewAPI 用户 key 不同；用户目标链路是 `NewAPI -> ZenProxy`，本轮按 NewAPI key 完成验收。
- exact-output literal 小流式仍会进入 buffered，这是设计内行为；非 exact 小流式已验证不会仅因低 `max_tokens` 进入 buffered。

未完成：
- 尚未跑 `policy-smoke/policy-dry`。
- 尚未用真实 ClaudeCode 长会话观察 V4.99 后的 reasoning-only 502、cache、工具调用和输出格式。

## 2026-06-05 14:50

目标：
实现独立 NewAPI 使用日志导出 sidecar，按 `user_id + time range` 导出脱敏日志和简要分析包；不修改 NewAPI，不进入 ZenProxy/free-model-client-rs 主链路。

已做：
- 新增独立 Rust crate：`tools/newapi-usage-exporter/`。
- 实现 SQLite 只读 schema 自动识别，支持常见 NewAPI 日志表和字段名。
- 实现 CLI：`serve`、`export`、`cleanup`。
- 实现 HTTP API：`GET /health`、`POST /v1/usage-export`、`GET /v1/usage-export/{id}`、`GET /v1/usage-export/{id}/download`、`DELETE /v1/usage-export/{id}`。
- 导出 zip 包包含 `usage_logs.csv`、`usage_summary.json`、`brief_analysis.md`、`ai_analysis_guide.md`、`data_dictionary.md`。
- 增加 30 天导出文件保留和后台清理；单次导出最大 31 天。
- 增加可选 `NEWAPI_USAGE_EXPORTER_ADMIN_TOKEN`，支持 Bearer 或 `x-api-key` 保护导出 API。
- 修复 SQLite 整数列导出为字符串时可能为空的问题，避免 `user_id/channel_id` 等字段丢失。
- 同步 `docs/08-newapi-usage-exporter.md`、维护入口、current-state、roadmap、backlog 和 playbook。

验证：
- `cargo fmt --manifest-path tools/newapi-usage-exporter/Cargo.toml -- --check` 通过。
- `cargo clippy --manifest-path tools/newapi-usage-exporter/Cargo.toml --all-targets -- -D warnings` 通过。
- `cargo test --manifest-path tools/newapi-usage-exporter/Cargo.toml` 通过：4 条测试。
- 临时 SQLite 端到端 smoke 通过：CLI `export`、HTTP `create/get/download/delete` 均成功。

结论：
- 第一版 NewAPI 使用日志导出 sidecar 已可本地编译和测试。
- 当前只支持 SQLite 只读 NewAPI 日志库；生产接入前必须确认 panda NewAPI 的真实数据库类型、schema 和只读连接方式。

未完成：
- 尚未接入 panda NewAPI 真实只读库或快照。
- 尚未实现 MySQL/Postgres adapter。
- 尚未做真实用户时间段导出的端到端验收。

## 2026-06-05 15:30

目标：
在 panda 上检查 NewAPI 真实数据库，并执行 NewAPI 使用日志导出器测试。

已做：
- 确认 panda NewAPI 运行在 Docker：`new-api` 容器，Postgres 容器 `new-api-postgres`，Redis 容器 `new-api-redis`。
- 确认 NewAPI DB 为 Postgres，数据库 `new-api`，用户 `newapi`，日志表 `logs`。
- 确认 `logs` 表包含敏感字段 `content/ip/other/request_id/upstream_request_id`，导出器不得选择这些字段。
- 先用安全字段抽取到 SQLite 快照做回归测试，随后补 Postgres 直连 adapter。
- 新增 `NEWAPI_USAGE_DATABASE_URL` / `NEWAPI_USAGE_POSTGRES_DSN` 配置，支持 Postgres URL/DSN。
- 修复 CLI 在 Tokio runtime 内调用同步 Postgres 客户端导致的 panic：CLI `export/cleanup` 也改为 `spawn_blocking`。
- 更新文档：工具 README、`docs/08-newapi-usage-exporter.md`、current-state、roadmap、backlog、playbook。
- panda `/tmp` 临时测试脚本、临时二进制和临时导出包已清理。

验证：
- 本地 `cargo fmt --check`、`cargo test`、`cargo clippy --all-targets -- -D warnings` 通过。
- panda 安全 SQLite 快照测试：
  - 用户 2，31 天 97,438 行，导出 1.47 秒，峰值 RSS 约 100MB。
  - HTTP create/get/download/delete 通过。
  - 超过 31 天拒绝通过。
  - 未授权 HTTP 401 通过。
- panda Postgres 直连测试：
  - 用户 1，2026-06-05 当天 865 行，导出 0.05 秒，峰值 RSS 约 7MB，HTTP smoke 通过。
  - 用户 2，2026-05-06 至 2026-06-06 共 97,438 行，导出 1.17 秒，峰值 RSS 约 150MB。
  - 导出 CSV header 不包含 `content/ip/request_id`。

结论：
- NewAPI 使用日志导出器已经可以直连 panda Postgres 做真实只读导出。
- 现阶段不再需要 SQLite 快照作为常规路径；快照只保留为降级/离线测试方案。

未完成：
- 尚未部署为长期服务。
- 尚未创建专用只读 DB 用户；本轮测试复用了 NewAPI 容器 DSN 的脱敏变体。
- NewAPI `type` 数字目前只做粗分类：`2` 为 ok，其他类型暂为粗错误类；需要后续补精确映射。
- MySQL adapter 未实现。

## 2026-06-05 16:00

目标：
补齐“一句话导出”能力并部署到 panda，使 AI 可以通过一句 `导出用户xxx从xx年月日~xx年月日的数据并做简要分析` 完成导出。

已做：
- 新增自然语言 instruction parser，支持中文日期 `2026年6月5日` 和 ISO 日期 `2026-06-05`。
- 日期范围按自然语言习惯处理：结束日期包含当天，例如 `2026年6月5日~2026年6月5日` 实际导出 `[2026-06-05 00:00, 2026-06-06 00:00)`。
- CLI 支持：`export --instruction '导出用户1从2026年6月5日~2026年6月5日的数据并做简要分析'`。
- HTTP 支持：`POST /v1/usage-export/instruction`。
- panda 部署：
  - 二进制 `/opt/newapi-usage-exporter/newapi-usage-exporter`。
  - systemd 服务 `newapi-usage-exporter.service`，已 enabled 且 active。
  - helper `/usr/local/bin/newapi-usage-export`。
  - runner `/usr/local/bin/newapi-usage-exporter-run` 动态读取 NewAPI 容器 `SQL_DSN` 并替换为当前 Postgres 容器 IP。
  - 导出目录 `/var/lib/newapi-usage-exporter/exports`。
  - 本地 API `http://127.0.0.1:8098`。
- 同步工具 README、`docs/08-newapi-usage-exporter.md`、current-state、roadmap、backlog、playbook。

验证：
- 本地 `cargo fmt --check`、`cargo test`、`cargo clippy --all-targets -- -D warnings` 通过；测试数 6。
- panda `/health` 返回 `ok`。
- helper 验收通过：`newapi-usage-export '导出用户1从2026年6月5日~2026年6月5日的数据并做简要分析'` 生成 zip。
- HTTP instruction 验收通过：`POST /v1/usage-export/instruction` 可创建、下载并删除导出。
- 验收临时脚本、临时 JSON、临时 zip 已清理。

结论：
- 当前已经达到“一句话让 AI 导出用户某时间段数据并做简要分析”的可用状态。

未完成：
- 还没有创建专用只读 DB 用户。
- 还没有审计日志。
- NewAPI `type` 数字仍需精确映射。
- MySQL adapter 未实现。

## 2026-06-05 16:10

目标：
完成一句话导出收尾验证、panda 复测和历史脏文件清理。

已做：
- 清理历史未跟踪临时项：`.codex_tmp/`、`configured`、`panda`、异常双引号文件、旧报告临时文件和 Python cache。
- 恢复 `scripts/panda_pressure_runner.py` 的旧脏状态，未纳入本轮提交。
- panda 复测 `newapi-usage-exporter.service` active，`/health` 返回 `ok`。
- panda helper 复测通过：`导出用户1从2026年6月5日~2026年6月5日的数据并做简要分析` 当前导出 `row_count=1011`。
- 本次 smoke 生成的导出目录已删除，避免留下临时产物。

验证：
- `cargo fmt --manifest-path tools/newapi-usage-exporter/Cargo.toml -- --check` 通过。
- `cargo test --manifest-path tools/newapi-usage-exporter/Cargo.toml` 通过：6 条测试。
- `cargo clippy --manifest-path tools/newapi-usage-exporter/Cargo.toml --all-targets -- -D warnings` 通过。
- `cargo build --release --manifest-path tools/newapi-usage-exporter/Cargo.toml` 通过，release hash `0e25db3eac288190befe003de8999577c71bbd308cd65c57db9ce09c4ea9b169`。

结论：
- 当前可以通过一句话 instruction 完成 NewAPI 用户时间范围导出和简要分析包生成。
- 工作区历史脏文件已清理，剩余改动只属于本轮 NewAPI usage exporter 和维护文档。

未完成：
- 专用只读 DB 用户、导出审计日志、NewAPI `type` 精确映射和 MySQL adapter 仍为后续项。

## 2026-06-05 20:30

目标：
排查 ClaudeCode 使用中“莫名无输出直接停止”、`/context` 检测大量 NewAPI 报错的问题，并从 free-model-client-rs 源头修复。

已做：
- 查询 panda NewAPI 日志和 ZenProxy/free-model-client-rs 脱敏日志，确认失败集中在 `deepseek-v4-flash`、channel 69、Anthropic `/v1/messages?beta=true`、非流式 502。
- 失败样本主要是 ClaudeCode 内部低预算探针：`stream=false`、`max_tokens=1/16`、`message_count=1`、`tool_count=1`、小工具 schema；上游常返回 `reasoning_only_length`，正文和 tool call 均为空。
- 本地新增窄保护：仅 ClaudeCode、非流式、`max_tokens<=32`、工具数 1-2、无显式 `tool_choice`、小上下文时，第一次上游请求前禁用 thinking，并把上游 `max_tokens` 最小抬到 64。
- 普通工具调用、长上下文、Hermes/OpenClaw、无工具普通短请求不走该策略，也不返回本地假答案。
- 新增 OpenAI/Anthropic 两入口 golden 测试，断言低预算工具探针会 `thinking={"type":"disabled"}` 且上游 `max_tokens=64`。
- 同步 `docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- `cargo fmt -- --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- `cargo test` 通过：库测试 83 条、kernel golden 99 条、doc tests 0 条。

结论：
- 根因不是 NewAPI 解析 JSON 本身，也不是 flash/free 输入被压缩；而是 ClaudeCode `/context` 等内部低预算工具探针给 DeepSeek 的输出预算太小，上游把预算消耗在 reasoning，最终被空输出保护转成 502，客户端表现为 `Failed to parse JSON` 或无输出停止。
- 本地源码已修复并验证；尚未部署 panda，部署后需要用真实 ClaudeCode `/context` 复验 502 是否下降。

未完成：
- 尚未构建并部署到 panda 三实例。
- 尚未用真实 Windows ClaudeCode `/context` 和 NewAPI 日志做部署后验收。

## 2026-06-05 21:00

目标：
把 ClaudeCode 低预算工具探针修复部署到 panda，解决 `/context` 等内部探针导致的 NewAPI 502。

已做：
- 在 `/home/lenovo/zen-proxy-rs` 构建 release，确认依赖当前 `../free-model-client-rs` 补丁。
- Windows/WSL `scp` 上传不稳定，改为本机临时 HTTP 文件服务 + panda 主动 `curl` 拉取 gzip；部署后已清理本地 HTTP 服务和 `/tmp` 临时文件。
- 备份旧线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-low-budget-probe-20260605-205234-8f8513c`。
- 安装新 stripped 二进制到 `/opt/zen-proxy-rs/zen-proxy-rs`，线上 SHA256 为 `369e45062f870f8460ebf4d52f06bda30d94fe0f4459cf8cdebbc4829fe3316d`。
- 重启 `zen-proxy-rs@1/@2/@3`。
- 同步 current-state、roadmap、backlog、playbook。

验证：
- 本地 release 构建通过；未 strip SHA256 为 `5732beb2c6cc7b9092ae7d9dfe580fd69d48f602b8cb16c859e9beb5f2022f67`。
- 4001/4002/4004/4000 `/health` 均 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`。
- 线上二进制包含 `low_budget_tool_probe_disabled` 和 `raised ClaudeCode low-budget tool probe max_tokens before upstream`。
- panda NewAPI Anthropic `/v1/messages?beta=true` 低预算工具探针：
  - `max_tokens=1`：HTTP 200，约 2.31s，`stop_reason=tool_use`。
  - `max_tokens=16`：HTTP 200，约 2.13s，`stop_reason=tool_use`。
- ZenProxy 日志确认命中策略：`requested_max_tokens=Some(1/16)`、`effective_max_tokens=Some(64)`、`thinking_policy="low_budget_tool_probe_disabled"`。
- 部署后近 10 分钟 channel 69 无错误记录；ZenProxy 近 5 分钟未见 `upstream returned no assistant content`、`stream truncated`、`retry budget exhausted`。

结论：
- 这次批量非流式 502 的复现路径已在 panda 上通过。
- 线上已部署修复，等待用户真实 ClaudeCode `/context` 和日常调用长窗口观察。

未完成：
- 历史单条 `stream truncated before DONE or finish_reason` 不属于本次低预算工具探针批量故障；若复发需单独排查。
- 尚未 commit 本轮源码、测试和文档改动。

## 2026-06-05 22:40

目标：
排查用户看到的 NewAPI 红行是否与上一轮低预算 `/context` 502 同因，并修复 ClaudeCode 流式 60 秒左右无内容导致的 `client_gone` 风险。

已做：
- 复查截图时段 NewAPI 记录，确认红行样本是 `type=2` 成功消费、`stream=true`、约 50k prompt tokens、`completion=0`、`use_time≈64s`，`other.stream_status.end_reason=client_gone`，不是上一轮非流式低预算工具探针 502。
- 对照 ZenProxy 日志，同窗口未见 `upstream returned no assistant content`、`stream truncated`、`retry budget exhausted`；判断更像下游客户端/NewAPI/cc-switch 在真实内容或工具调用长时间未到达时断开。
- 在 `src/proxy/anthropic.rs` 增加 ClaudeCode Anthropic SSE idle ping：等待上游事件超过 15 秒，或上游只有 reasoning/usage 等不可转发事件且下游 15 秒无活动时，发送 `event: ping` / `data: {"type":"ping"}`。
- 该策略仅对 ClaudeCode Anthropic 流式生效，不对 Hermes/OpenClaw/OpenAI 生效，不伪造内容，不改变 prompt、thinking、工具调用或 `max_tokens`。
- 新增 golden：`claude_code_anthropic_stream_sends_idle_ping_before_delayed_content`，模拟上游 16 秒后才发内容，断言先发 ping，再完整输出真实内容和 `message_stop`。
- 本地构建 `zen-proxy-rs` release，传输过程中遇到本机到 panda 的 Tailscale/SSH 大文件 reset；最终在 Tailscale 恢复后通过 xz 压缩包上传并部署。

验证：
- `free-model-client-rs` 本地验证通过：`CARGO_INCREMENTAL=0 cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`；库测试 83 条、kernel golden 100 条。
- 新增 delayed-stream 单测单独通过，耗时约 16 秒。
- `/home/lenovo/zen-proxy-rs` release 构建通过；未 strip SHA256：`00ffe54a9b8b9ab09a5fda5c55cf68ebc06825dbece2dabbe6e888bc2bd2f300`。
- panda 部署：旧 hash `369e45062f870f8460ebf4d52f06bda30d94fe0f4459cf8cdebbc4829fe3316d` 备份到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-stream-idle-ping-20260605-223420-369e450`；新 stripped hash `be6b859576169d2cc710ed2c079a125c50d0a51a0d744abbd3668ba1e030e793`。
- `zen-proxy-rs@1/@2/@3` active；4001/4002/4004/4000 `/health` 均 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`。
- 线上二进制包含 `sent ClaudeCode stream idle ping while upstream produced no forwardable output` 和 `sent ClaudeCode stream idle ping while waiting for upstream event`。
- panda 本机 NewAPI token id `38`/name `ds`/group `vip` 下，`/v1/models` 200 且包含两个 deepseek 模型；Anthropic `/v1/messages?beta=true` + `x-fmc-client=claude-code` 流式 smoke HTTP 200，starttransfer 约 1.39s，总耗时约 1.92s，响应按 SSE 分片输出目标 marker，无 error。

结论：
- 上一轮非流式低预算工具探针 502 和这轮 stream `client_gone` 不是同一个问题。
- 本轮已在 panda 上部署协议级 idle ping 保活，理论上可降低“上游长时间没有可转发内容导致下游空闲断开”的红行。
- 该修复不代表 first content 变快，也不解决上游真实空输出；如果后续仍出现 60 秒左右 `client_gone`，需要加 first-content watchdog 或按请求形态做 retry/降级。

未完成：
- 尚未用用户真实 ClaudeCode 长会话观察部署后 `client_gone` 是否下降。
- 尚未 commit 本轮源码、测试和文档改动。

## 2026-06-06 11:40

目标：
修复 NewAPI channel 69 日志中 `reasoning_content in the thinking mode must be passed back` 的 provider 400/500，并避免 public error 泄露 `opencode zen` 内部反代标识。

已做：
- 查询 panda NewAPI 最近 24 小时日志，确认同类错误 16 条：8 条 stream 被 NewAPI 记为 500，8 条 non-stream 记为 400，均为 `deepseek-v4-flash`、`/v1/messages`、channel 69。
- 对齐 ZenProxy 日志，确认失败样本是 ClaudeCode Anthropic 请求转 OpenAI-compatible 上游，`thinking_policy=keep_default`，工具数 8-33，provider 在 fetch 阶段直接返回 400。
- 新增 `UpstreamErrorKind` 和 public/private 错误映射：`AppError::upstream` 不再返回 `opencode zen {status}: {body}`；public response 只给稳定 `type/code/message`，`Retry-After` 继续保留。
- 新增 `provider_missing_reasoning_content` 一次性 disabled-thinking 重试：覆盖 OpenAI/Anthropic 非流式、OpenAI 流式、ClaudeCode Anthropic 流式和 buffered huge-stream。
- 新增 golden：Anthropic non-stream missing reasoning_content 重试、Anthropic stream missing reasoning_content 重试、上游错误 public message 不泄露内部 proxy label。
- 同步 `docs/02-current-state.md` 和 `docs/05-ai-maintenance-playbook.md`。

验证：
- 先跑新增测试确认红灯：3 条新增用例失败，证明旧实现会裸 400/stream error 且泄露 `opencode`。
- 修复后 targeted 新增测试全部通过。
- 完整验证通过：`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`；库测试 89 条、kernel golden 103 条、doc tests 0 条。

结论：
- 本地源码已解决该 provider 400 的源头兜底和 public error 外泄问题。
- 该修复不是全局禁用 ClaudeCode tools auto thinking；只有 provider 明确拒绝当前请求缺 `reasoning_content` 时才重试一次 disabled thinking。

未完成：
- 尚未构建并部署到 panda。
- 部署后需复查 channel 69 是否仍出现 `provider_missing_reasoning_content` 或 `opencode zen`。

## 2026-06-06 12:50

目标：
将 `provider_missing_reasoning_content` 源头修复部署到 panda，并完成最小生产 smoke。

已做：
- 在 `/home/lenovo/zen-proxy-rs` 构建 release；构建时重新编译本地 path 依赖 `/home/lenovo/free-model-client-rs` commit `68bf5383f7c8915f0950a6864134b77dd51a1214`。
- 生成 stripped 二进制，sha256 为 `d5b7558c9f8f9fc7ea6faa802634dba85435868f1e338a4830f77079c3a1fc8e`。
- 因 Tailscale/SSH 大文件传输多次 reset/timeout，改用 64KB 分片上传 xz 包到 panda，远端 xz hash 校验通过。
- 备份旧线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.20260606-124001.pre-68bf538`，旧 hash 为 `39dc0bb94092597a00518abf83e80f8c32a91e8c60682c169942bf16bf70017d`。
- 替换 `/opt/zen-proxy-rs/zen-proxy-rs` 并重启 `zen-proxy-rs@1`、`zen-proxy-rs@2`、`zen-proxy-rs@3`。

验收：
- `systemctl is-active zen-proxy-rs@1/@2/@3` 均为 `active`，新 pid 为 `2423982`、`2424013`、`2424021`。
- `4001`、`4002`、`4004`、`4000` 的 `/health` 均返回 `status=ok`。
- ZenProxy `/v1/models` 返回 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- ZenProxy 直连 OpenAI `/v1/chat/completions` 和 Anthropic `/v1/messages` 均返回 `pong`。
- panda NewAPI 8081 经 OpenAI `/v1/chat/completions` 和 Anthropic `/v1/messages` 调用均返回 `pong`。
- 部署后 10 分钟窗口内，ZenProxy/NewAPI 日志未见新的 `reasoning_content`、`opencode zen`、`upstream returned no assistant`、`stream error`、`parse JSON`、`status_code=400/500`；仅有两条由部署 smoke 使用错误 dev key 造成的 `proxy authentication failed`。

结论：
- panda 当前已运行包含 `reasoning_content` 源头兜底和 public error 脱敏的版本。
- 该部署验证了最小链路正常；仍需用户真实 ClaudeCode 长会话继续观察 channel 69 是否再次出现同类 provider 错误。

## 2026-06-06 16:15

目标：
让 NewAPI 页面里的 FRT 更接近真实首个模型输出 token，而不是被空协议帧、`message_start`、空 role delta 或 pre-first ping 提前打点。

已做：
- 新增配置 `FREE_MODEL_TRUE_FIRST_TOKEN_FRT`，默认开启。
- OpenAI stream：默认不再先发空 `role=assistant/content=""` delta；等真实文本 delta 或真实 tool call 准备好时，再连同 role delta 一起发送。
- Anthropic stream：默认不再先发 `message_start`；等真实文本 delta、真实 tool_use 或 fallback 文本准备好时，再连同必要协议帧一起发送。
- ClaudeCode Anthropic pre-first ping：默认不再在真实内容/tool call 前发送，避免 NewAPI FRT 被 ping 污染；`FREE_MODEL_TRUE_FIRST_TOKEN_FRT=0` 可恢复旧保活口径。
- 保留 legacy 行为测试，确保需要回退时仍可通过配置恢复旧 pre-first ping。
- 同步 `.env.example`、README 和 AI 维护手册。

验证：
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo fmt -- --check` 通过。
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo clippy --all-targets -- -D warnings` 通过。
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo test` 通过：库测试 89 条、kernel golden 104 条、doc tests 0 条。

结论：
- 源码层已经把普通流式 NewAPI FRT 从“协议首包口径”改成“真实首个可转发内容/tool call 口径”。
- huge buffered stream 仍是特殊路径：该路径本身先收集再输出，NewAPI FRT 会接近收集完成时间，不是实时首 token；后续如果该路径影响体验，需要单独改为非 buffered guard。

未完成：
- 尚未构建并部署到 panda。
- 部署后需要通过 panda NewAPI 真实流式请求复查 FRT 是否从 `0.0s/0.4s` 回到真实首字耗时。

## 2026-06-06 16:30

目标：
将 `FREE_MODEL_TRUE_FIRST_TOKEN_FRT` 部署到 panda，并确认 NewAPI 页面口径能看到真实首 token 级 FRT。

已做：
- 在 `/home/lenovo/zen-proxy-rs` 构建 release，包含本地 path 依赖 `/home/lenovo/free-model-client-rs` 的 true-first-token FRT 改动。
- 生成 stripped 二进制，sha256 为 `4233b08cdeb7bf18c76d7528d837c38e938c72e6f6ead13fe3c3d4b018aaefe3`。
- 通过 Windows `scp/ssh panda` 上传 xz 包；WSL 侧 SSH/scp 同时段超时，未继续用 WSL 传输。
- 备份旧线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.20260606-162635.pre-true-frt-34c2f7d`，旧 hash 为 `34c2f7dbaeb6518672c1e2eef30fcad5985634ba520a4aaa34285f98682421f3`。
- 替换 `/opt/zen-proxy-rs/zen-proxy-rs`，重启 `zen-proxy-rs@1/@2/@3`。

验证：
- `zen-proxy-rs@1/@2/@3` 均为 `active`。
- 线上二进制 hash 为 `4233b08cdeb7bf18c76d7528d837c38e938c72e6f6ead13fe3c3d4b018aaefe3`。
- panda NewAPI OpenAI stream smoke：HTTP 200，curl `starttransfer=2.113s`，NewAPI 日志 `frt=2112`，返回目标 marker。
- panda NewAPI Anthropic stream smoke：HTTP 200，curl `starttransfer=1.991s`，NewAPI 日志 `frt=1990`，返回目标 marker。
- 最近 1 分钟 NewAPI 未见新的 `status_code=502/503/504`、`lane is saturated`、`Failed to parse JSON`。

结论：
- panda 当前已运行 true-first-token FRT 版本。
- NewAPI 里我这两条测试记录已经不再显示 `0.0s/0.4s` 假首包，而是约 2s 的真实首个可转发内容时间。
- 16:27 左右有一批 `bad response status code 502, body: <html>`，发生在部署重启和用户并发调用窗口；16:28 smoke 后最近 1 分钟没有继续出现。

未完成：
- 后续需要用用户真实 ClaudeCode 长会话继续观察，大上下文场景的 FRT 会按真实模型首 token 增长，不会再被空协议帧压低。

## 2026-06-08 12:20

目标：
分析 2026-06-08 channel 69 大上下文慢首字和 cache 观测不稳定问题，并在不裁剪输入、不限制输出、不改 NewAPI 的前提下，从 free-model-client-rs 源头降低 ClaudeCode 大上下文慢失败长尾。

已做：
- 复查 panda NewAPI 与 ZenProxy 日志，确认 `deepseek-v4-flash` 成功流式调用约 2783 条，NewAPI FRT P50 约 4.1s、P90 约 8.7s、P99 约 35.9s。
- 定位慢首字样本 `id=136957`：prompt `173751`、completion `218`、FRT `47123ms`、cache `270976`；第一次上游 fetch 在约 42 秒后返回 provider `520`，第二次重试后 cache accepted 并成功。
- 新增 ClaudeCode Anthropic stream 大上下文首包 fetch 超时保护：仅在 `source_client=ClaudeCode`、stream、真实 text/tool 输出前、估算 token 超过门槛时触发，超时后走既有 Stream Guard 重试。
- 新增配置：`FREE_MODEL_CLAUDE_CODE_STREAM_INITIAL_FETCH_TIMEOUT_SECS` 默认 `30`、`FREE_MODEL_CLAUDE_CODE_STREAM_SLOW_GUARD_MIN_INPUT_TOKENS` 默认 `150000`、`FREE_MODEL_CLAUDE_CODE_STREAM_NO_FORWARDABLE_RETRY_SECS` 默认 `45`。
- 新增 `ClaudeCode stream guard completion summary` 日志，记录 attempts、retry、first upstream/event/reasoning/content/tool-call 耗时、cache usage、prompt hash、tokens 和 guard 配置，便于后续区分 cache miss、上游慢思考、上游慢失败和本地重试。
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo fmt -- --check` 通过。
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo clippy --all-targets -- -D warnings` 通过。
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo test` 通过：库测试 94 条、kernel golden 105 条。
- 新增 golden `claude_code_anthropic_stream_retries_slow_initial_fetch_before_output` 覆盖首包慢失败主动重试，断言第一次慢 fetch 被超时切断后第二次请求成功。

结论：
- 2026-06-08 观测到的 180k 左右慢首字主样本不是 cache miss、输入 token 墙或本地池耗尽，而是上游慢失败后重试。
- 本轮源码修复会把“真实输出前长时间卡在首次上游 fetch”的请求更早交给 Stream Guard 重试，目标是降低 P99 长尾；不会把 ping 当首字，不伪造内容，不影响 Hermes/OpenClaw。

未完成：
- 尚未部署到 panda，不能宣称线上 FRT 已改善。
- 部署后需要复查 `ClaudeCode stream guard completion summary`、NewAPI FRT 分位、cache observation 四态和 150k+ 桶失败率。

下一步：
- 如批准部署，按生产流程在本机/WSL 构建 `zen-proxy-rs` release，上传 panda，重启三实例，并跑 `/health`、`/v1/models`、OpenAI/Anthropic smoke 与 150k+ ClaudeCode 小样本验收。

## 2026-06-08 14:08

目标：
把 2026-06-08 P1.21 部署列为显式待办，并分析 12:20 后用户继续产生的 channel 69 调用，判断是否属于上一轮已解决问题或出现新问题。

已做：
- 查询 panda NewAPI Postgres `logs` 表，窗口为 2026-06-08 12:20:00 至约 14:10:53 CST，channel 69。
- 统计到 2730 条调用：`type=2 stream=true` 共 2722 条，`type=2 stream=false` 共 7 条，`type=5 stream=true` 共 1 条。
- 按 `stream_status` 细分：`ok/eof` 2682 条，`error/client_gone` 40 条。
- `ok/eof` 流式 NewAPI FRT 分位：P50 约 4.9s，P90 约 8.4s，P95 约 12.3s，P99 约 25.6s，最大约 122.9s；FRT >=15s 共 101 条，>=30s 共 20 条。
- `ok/eof` cache 命中行约 2551/2682，约 95.12%；`150k-220k` 桶 cache 命中约 99.04%，`220k-350k` 桶约 98.50%。
- 查唯一错误 id `142345`：NewAPI 记录 `status_code=500, upstream returned no assistant content or tool call (class=reasoning_only)`；ZenProxy 日志显示 `finish_reason=Some("stop")`、`reasoning_chars=87`、`content_chars=0`。
- 查 124s FRT 样本 id `141071/141081`：NewAPI 为 `client_gone/context canceled`，prompt 约 53k-55k，completion 2，cache 0；ZenProxy 日志显示上游持续产生 reasoning/空事件，60 秒无可转发内容后 Stream Guard 重试。
- 进一步统计 `stream_status.end_reason=client_gone`：共 40 条，其中 38 条 `completion=0` 且 NewAPI FRT 为 `-1000`，use_time P50 约 59s、P90 约 64s、最大 125s。

结论：
- 12:20 后数据大部分正常，cache 命中并不差；主要不稳定点不是 cache 全局失效。
- 上一轮 P1.21 的“首包 fetch 慢失败/520”修复能覆盖一类上游 fetch 卡死或慢失败，但不能完全覆盖本轮新暴露的 `reasoning_only/no-forwardable`：上游已经开始返回事件，但长期没有真实 text/tool。
- 线上当前仍未部署 P1.21，因此 60 秒 no-forwardable retry 仍是旧口径；部署后默认会降到 45 秒，但仅靠它仍可能把两轮等待压到约 90 秒，不能从根上解决所有 `client_gone`。

未完成：
- P1.21 仍未部署 panda。
- 需要新增或调整 ClaudeCode stream first-content/no-forwardable watchdog，覆盖 `reasoning_only + stop + content/tool 为空` 和 58-65 秒无 FRT `client_gone`，同时不能伪造内容或污染 NewAPI 真实首字。

下一步：
- 先部署 P1.21 获取 `ClaudeCode stream guard completion summary` 细粒度数据；随后根据真实 summary 决定是否把 no-forwardable retry 从 45 秒进一步下调，或单独增加 reasoning-only disabled-thinking fallback。

## 2026-06-08 15:20

目标：
把 P1.21 ClaudeCode 大上下文首包保护平滑部署到 panda，用户正在使用时避免整条链路中断。

已做：
- 在 `/home/lenovo/zen-proxy-rs` 构建 release，并补齐 `KernelConfig` 新字段的两个 ZenProxy 初始化点。
- 验证 `zen-proxy-rs`：`cargo fmt -- --check`、`cargo build --release`、`cargo test` 均通过；135 个 unit tests 和 27 个 e2e tests 通过。
- 生成第一版 stripped 部署二进制，SHA256 为 `5c33046808ae5ccb2ff39083a31d95b683c605288a0c42dbaa7209636490f73d`；压缩包 SHA256 为 `52572631810a8949ddf24c399378171635257af33960fa33fbf2b2e5149d2605`。
- 传输时本机到 panda 的 HTTP/SSH 大包通道多次 reset，最终用 64KB 小块独立 SSH 写入 `/tmp/*.part`，整体 hash 校验通过后才解压部署。
- 备份旧线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-p121-20260608-151426-dfd52e3489e6`。
- 原子替换 `/opt/zen-proxy-rs/zen-proxy-rs`，按 `zen-proxy-rs@1`、`@2`、`@3` 滚动重启；每个实例重启后立即做 `/health`。
- 部署后发现 ZenProxy 主调用路径还在硬编码 P1.21 三个 guard 默认值，虽然当前默认可用，但后续调 `FREE_MODEL_CLAUDE_CODE_STREAM_*` 不会生效；随后把 `src/v4/provider.rs` 改为读取 `Config` 中的 env 字段，重新构建、测试并二次滚动部署。
- 二次 env-wired stripped SHA256 为 `a771174350bf6701c97b7deed1bbf4deecd995463c5cfb27ff4b4e6c7c440f6b`，压缩包 SHA256 为 `85728a9c3970fa869bd9dc618ee20c4c04b0cf5d7ca97517b91d148a44381b4e`；第一版中间二进制备份到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-p121-envwired-20260608-153746-5c33046808ae`。
- 清理本地临时 HTTP 服务。

验证：
- `zen-proxy-rs@1/@2/@3` 均为 active，新 pid 为 `4031850`、`4031873`、`4031941`。
- 线上 `/opt/zen-proxy-rs/zen-proxy-rs` 最终 hash 为 `a771174350bf6701c97b7deed1bbf4deecd995463c5cfb27ff4b4e6c7c440f6b`，三个实例进程 exe 均指向该文件。
- 4001/4002/4004/4000 `/health` 均返回 `status=ok`，`dispatch=90`、`dead=0`、`ratelimited=0`。
- 4001/4002/4004/4000 `/v1/models` 均返回 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- Anthropic/ClaudeCode 最小流式 smoke：HTTP 200，最终 curl `starttransfer=1.663s`，返回 `pong`。
- 部署后日志已出现 `ClaudeCode stream guard completion summary`，真实 ClaudeCode 样本包括 140k-220k 输入、`tool_calls`、`cache_observation=accepted`，主链路在持续成功。
- 部署后最近窗口未见 panic、bind failed、lane saturated、no proxy resources、stream decoding error。

结论：
- P1.21 已在 panda 上运行，且 ClaudeCode/Anthropic 主链路可用。
- OpenAI-compatible 极短流式 smoke 仍可返回 `upstream returned no assistant content or tool call (class=reasoning_only_length)`；这不是本轮首包 fetch 慢失败修复的覆盖面，列为 OpenAI 短流式/上游 reasoning-only 残留。
- 不能只凭最小 smoke 断言 FRT 已全局改善；接下来应看 150k+ 真实 ClaudeCode 样本里的 `attempts_used/retry_count/first_content_ms/cache_observation` 和 NewAPI FRT 分位。

## 2026-06-08 16:05

目标：
补算 2026-06-08 12:20-14:10 channel 69 调用的带宽、CPU 和内存压力。

已做：
- 从 ZenProxy journal 解析 `v4 ingress request body_size`，窗口为 2026-06-08 12:20:00 至 14:11:00。
- 解析到约 2733 条 `deepseek-v4-flash` 入站请求体记录，总请求体 `2,595,973,451 bytes`，约 `2475.71 MiB`。
- 请求体分布：平均约 949,862 bytes，P50 约 952,547 bytes，P90 约 1,397,668 bytes，P95 约 1,738,543 bytes，P99 约 1,890,721 bytes，最大约 1,982,268 bytes；无单请求超过 2MiB。
- NewAPI token 汇总同窗口约 2730 条：prompt tokens `430,642,614`，completion tokens `1,207,425`，总 tokens `431,850,039`；输出 token 远小于输入 token。
- 当前 16:03 资源观测：panda load average `0.71/0.52/0.38`；内存总量约 3.7GiB，used 1.9GiB，available 1.8GiB，swap used 715MiB。
- 当前进程观测：三个 `zen-proxy-rs` 进程 RSS 约 171-181MiB，合计约 526MiB；CPU 各约 6.5%-6.6%，合计约 20%。NewAPI 容器约 190.8MiB、CPU 约 8.39%；Postgres 约 160.2MiB。
- 5 秒网络采样：`eth0` 约 RX 14.6Mbps、TX 6.4Mbps；docker bridge 约 5.27Mbps 双向。

结论：
- 本窗口真实请求体入站约 2.48GiB，摊到 111 分钟约 3.1Mbps 单链路；如果按 client->NewAPI 入站和 ZenProxy->upstream 出站两条公网方向粗算，物理外网请求体流量约 5GiB，均值约 6.2Mbps，未计 TLS/SSE/header/隐藏 reasoning。
- 响应正文按 completion tokens 估算只有几 MB 到几十 MB 级，带宽主要不是输出，而是每轮 0.5-2MiB 的长上下文请求体反复上传。
- 当前 panda 不是 CPU 打满状态，ZenProxy CPU 和内存都可接受；真正长尾仍主要来自上游无可转发内容、client_gone 和 provider 行为。
- 但若按 1 vCPU/2GiB 机器部署，三 ZenProxy 实例加 NewAPI/Postgres/Redis/Docker 会偏紧：ZenProxy 三实例约 526MiB，NewAPI+Postgres+Redis 约 354MiB，再加系统和 page cache，2GiB 会更依赖 swap。更稳妥是减少实例数、外置数据库或保留 3-4GiB 内存。

未完成：
- 没有历史 CPU/内存监控留存，只能给当前快照和按请求体/token 的带宽估算。
- 没有精确记录 upstream response bytes 和 hidden reasoning bytes；后续若要精准算带宽，需要在 ZenProxy 或 sidecar 增加 response byte counters。

## 2026-06-08 17:10

目标：
落地 V4.101 质量保全低延迟优化，在不裁剪输入、不限制输出、不默认禁用 thinking、不注入隐藏提示词的前提下降低 ClaudeCode 真实首 token / first tool 长尾。

已做：
- `free-model-client-rs`：ClaudeCode Anthropic stream 增加完整 JSON 工具调用提前释放。只有工具参数可解析为完整 JSON 后才发送 `tool_use`，并跳过最终重复发送；新增 `first_tool_emit_ms`、`emitted_tool_call_count` 观测。
- `free-model-client-rs`：no-forwardable retry 从固定等待改为按输入 token 桶自适应：`<50k=10s`、`50k-100k=14s`、`100k-200k=22s`、`200k-400k=32s`、`400k+=45s`，且只在真实 text/tool 尚未下发前生效。
- `zen-proxy-rs`：大流式 affinity key 加入稳定 messages 前缀 hash、tools hash、tool_choice hash，提升 cache accepted 节点复用机会；只记录 hash，不记录 prompt 原文。
- `zen-proxy-rs`：tool-heavy lane 阈值从 `tools>=16/tool_markers>=12` 下调到 `tools>=8/tool_markers>=6`，让中等 ClaudeCode 工具流更早隔离。
- 新增 golden/unit 覆盖：split tool delta 完整后只发一次、early tool 后 text block index 不冲突、no-forwardable 自适应桶、stable prefix affinity、8 tools lane。
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- 已通过 targeted：ClaudeCode Anthropic stream、tool delta、split tool、tool-after-text index、no-forwardable adaptive、ZenProxy affinity 和 medium tool lane。
- `free-model-client-rs` 完整验证通过：`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`；库测试 95 条、kernel golden 107 条、doc tests 0 条。
- `zen-proxy-rs` 完整验证通过：`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`；unit tests 137 条、e2e tests 27 条。
- 尚未部署 panda，不能写成线上效果。

结论：
- V4.101 源码阶段已完成核心低延迟改造。预期收益主要来自工具调用更早可执行、50k-200k no-forwardable 长尾更早换路、长会话 cache 亲和更稳，以及中等工具流不再挤普通流式 lane。

未完成：
- 尚未构建 release、部署 panda 和做 30-60 分钟真实数据对比。

下一步：
- 构建 `zen-proxy-rs` release，滚动部署 panda，并按 V4.101 P0 验收标准观察 NewAPI FRT、`first_tool_emit_ms`、cache observation、502/503/504 和工具重复/缺参错误。

## 2026-06-08 22:40

目标：
将 V4.101 质量保全低延迟优化部署到 panda，并完成最小线上验收。

已做：
- 在 WSL 本地构建 `zen-proxy-rs` release，strip 后二进制 hash 为 `149dd2f65c8b33228498bcc1f2e94f6742e1e1a5417592c0eb6921e7cc7deb49`。
- panda SSH/Tailscale 文件传输多次 reset；最终采用 32KB base64 分块覆盖上传，远端合并后 xz hash 和二进制 hash 均校验通过。
- 备份旧线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.20260608-222704.pre-v4101`。
- 滚动重启 `zen-proxy-rs@1/@2/@3`，三实例新 pid 分别为 `76140`、`76778`、`76786`。

验证：
- 三实例 `systemctl is-active` 均为 `active`，线上 `/opt/zen-proxy-rs/zen-proxy-rs` hash 为 `149dd2f65c8b33228498bcc1f2e94f6742e1e1a5417592c0eb6921e7cc7deb49`。
- panda 本机 `4001/4002/4004 /health` 均通过；外部 `100.69.228.93:4000 /health` 和 `/v1/models` 通过。
- ZenProxy 直连 OpenAI stream 返回 `V4101_OPENAI_OK`；ZenProxy 直连 Anthropic ClaudeCode stream 返回 `V4101_ANTHROPIC_OK`；NewAPI -> ZenProxy OpenAI stream 返回 `V4101_NEWAPI_OK`。
- 部署后日志已出现 V4.101 新字段 `first_tool_emit_ms` 和 `emitted_tool_call_count`，并能看到自适应 `no_forwardable_retry_after_secs=14/32`；部署窗口 grep 未发现新的 `lane is saturated`、`no proxy resources`、`upstream returned no assistant` 或 50x 关键错误。

结论：
- V4.101 已上线 panda，最小链路可用。
- 这次部署只证明“代码已生效、链路可生成、关键错误未立即复发”；还不能证明 30-60 分钟分位目标已达成。

待观察：
- 继续看真实 ClaudeCode 长会话的 `first_tool_emit_ms`、`first_tool_call_ms`、`first_content_ms`、`attempts_used`、`cache_observation`、NewAPI FRT、工具重复/缺参错误。

## 2026-06-09 11:25

目标：
修复用户反馈的 Windows ClaudeCode 经 panda NewAPI / ZenProxy 使用 `deepseek-v4-flash` 时出现大量 `Invalid tool parameters`、缺参数、工具循环和偶发无回复问题，并先用本地真实 ClaudeCode 验证，不盲目改客户端侧。

已做：
- 在 `src/proxy/anthropic.rs` 增加 ClaudeCode 工具参数完整性门控：只有 JSON 完整、工具名非空、必填字段齐全的 tool_use 才下发。
- 对空 `{}` 或缺必填字段工具调用增加窄修复：优先从最新用户明确指令补 `Bash.command`、`Write.file_path/content`、`Read.file_path`、`ToolSearch.query` 等；补不齐则拒绝下发。
- 对缺参路径增加 disabled-thinking retry：只在 ClaudeCode 工具参数不完整时触发，不全局禁用 thinking。
- 增加重复 repaired 工具调用保护：如果历史中已有同一 assistant tool_call 和对应 tool_result，不再补出同一工具，避免 Bash/Read/Write 循环。
- 增加 ClaudeCode 文件工具坏路径保护：`Read/Write/Edit/MultiEdit/Notebook*` 收到 `file_path="\\\\"`、`"/"`、`"."` 这类明显非文件路径时，优先从用户明确路径修复；修不了就拒绝下发。
- 新增/更新回归测试，覆盖 split tool arguments、空参数拒绝、disabled retry 恢复、重复 repaired Bash 防循环、坏 root file_path 修复/拒绝。
- 构建 `zen-proxy-rs` release，最终 stripped hash `ebe41572fe76a5f99783ba5e4308e164368415b00277432cd9829e60ecc651dd`，滚动部署到 panda 三实例；旧版备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.20260609-111046.pre-v4102-tool-input-guard`。

验证：
- 本地验证通过：`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`；lib/main 110 条，kernel golden 112 条。
- panda 三实例 `zen-proxy-rs@1/@2/@3` active，`4001/4002/4004 /health` 均 `status=ok`，池 `dispatch=90/dead=0/ratelimited=0`。
- panda NewAPI smoke：OpenAI stream 和 Anthropic stream 均 HTTP 200，有真实文本，无 error event。
- Windows ClaudeCode 真实 Bash/Write/Read 用例通过：文件创建成功，返回 `CLEAN_TOOL_OK`，无 `Invalid tool parameters`、无 `Failed to parse JSON`、无工具错误、无 `file_path="\\\\"`。
- Windows ClaudeCode 小矩阵通过：ToolSearch 触发并成功；Task 通过 `Agent`/`local_agent` 路径启动 subagent 并返回；Markdown 输出成功。
- NewAPI 近 30 分钟 channel 69 只有正常消费日志：18 条带 usage 的 deepseek-v4-flash 调用，`completion=0` 为 0，use_time 平均约 4.06s、最大 6s，FRT 平均约 3892ms、最大 6338ms，cache read/create 仍为 0。

结论：
- V4.102 已上线 panda，并解决了当前可复现的 ClaudeCode 工具缺参/坏路径/重复补参问题。
- 这次修复仍保持源头适配：没有改 ClaudeCode 配置，没有裁剪输入，没有恢复输出限制，没有全局禁用 thinking。
- Task/subagent 验收不能只 grep `Task`，ClaudeCode 当前会把上游工具名显示为 `Agent` 并产生 `task_started/local_agent` 事件。

未完成：
- 还不是 24 小时稳定性结论；需要继续观察用户真实长会话是否复发 `Invalid tool parameters`、`Failed to parse JSON`、502/503、坏路径和重复工具。
- cache 仍为 0，需要继续按 V4.98 prefix hash 和 provider cache 信号排查。
- 23k-41k 工具请求 FRT 仍在数秒级，没有达到全局 2-3s latency 目标。

下一步：
- 继续观察真实调用数据；若复发，先查 V4.102 日志字段和 raw SSE，再决定是否继续扩大门控规则。

## 2026-06-09 13:50

目标：
将 V4.104 ClaudeCode progressive tool streaming 与质量回退部署到 panda，并完成最小线上验收。

已做：
- 复用本地已验证源码构建 `zen-proxy-rs` release；strip 后二进制 SHA256 为 `08d9064600e66097ab45bbe97290bf5e7015174a15adbe27dc5fcf8261c2ed9f`。
- 本机到 panda 的 SSH/Tailscale 大文件传输持续抖动；先后尝试 `scp`、Taildrop、HTTP 单文件拉取，最终采用 xz 压缩后 128KB HTTP 小分片拉取，远端重组后 xz hash 和最终二进制 hash 均校验通过。
- 备份旧线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.20260609-134330.pre-v4104-ebe41572fe76a5f99783ba5e4308e164368415b00277432cd9829e60ecc651dd`。
- 安装新二进制并滚动重启 `zen-proxy-rs@1/@2/@3`。

验证：
- 三实例 `systemctl is-active` 均为 `active`；线上 `/opt/zen-proxy-rs/zen-proxy-rs` hash 为 `08d9064600e66097ab45bbe97290bf5e7015174a15adbe27dc5fcf8261c2ed9f`。
- `4001/4002/4004/4000 /health` 均返回 `status=ok`，`dead=0`、`ratelimited=0`。
- ZenProxy 直连 `/v1/models` HTTP 200，只暴露 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- ZenProxy OpenAI 非流式 `PONG` HTTP 200，耗时约 2.06s；ZenProxy Anthropic 非流式 `PONG` HTTP 200，耗时约 1.78s。
- panda NewAPI `/v1/models` HTTP 200；NewAPI OpenAI 非流式 `PONG` HTTP 200，耗时约 1.90s；NewAPI Anthropic 非流式 `PONG` HTTP 200，耗时约 1.72s。
- ClaudeCode Anthropic forced `Bash` tool stream HTTP 200，SSE 中存在 `content_block_start tool_use`、完整 `input_json_delta`、`content_block_stop`、`message_stop`。
- 部署后日志窗口未扫到 `Invalid tool parameters`、`Failed to parse JSON`、`summary is required`、`provider_missing_reasoning_content` 或 panic。

结论：
- V4.104 已上线 panda，基础生成链路和关键 ClaudeCode 工具流路径通过。
- 这次修复继续保持源头适配：不改 ClaudeCode 客户端、不裁剪输入、不恢复输出限制、不注入隐藏提示词、不全局禁用 thinking。

未完成：
- 还不是 24 小时稳定性结论；需要继续看真实 ClaudeCode 长会话里的 FRT 分位、`first_tool_call_ms/first_tool_emit_ms` 差值、工具失败、输出格式和缓存命中。
- 日志仍可见短非流式 `reasoning_only_length` 警告，按既有上游空输出/低预算探针继续分类跟踪，不作为 V4.104 部署失败结论。

下一步：
- 观察真实 Windows/WSL ClaudeCode 长会话；若复发，优先抓 raw SSE 和 `ClaudeCode stream guard completion summary`，不要先回到客户端侧适配。

## 2026-06-09 21:52

目标：
将 V4.106 质量保全 cache-friendly 中等上下文 session 优化部署到 panda，并完成最小线上验收。

已做：
- 复用已构建并校验的 stripped release，安装到 panda `/opt/zen-proxy-rs/zen-proxy-rs`。
- 备份旧线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.20260609-213725.pre-v4106-a52f4d6add0a`。
- 滚动重启 `zen-proxy-rs@1/@2/@3`。
- 确认 channel 69 当前名称 `Zenproxyrs4.3`，状态启用，group 为 `vip,ds`，base URL 为 `http://172.17.0.1:4000`。

验证：
- 线上二进制 hash 为 `b401c9463e29788e67aaecbe53c02b8743b2e25970e135e767410df9d4e0edab`。
- `zen-proxy-rs@1/@2/@3` 均为 `active`。
- 4001/4002/4004/4000 `/health` 均返回 `status=ok`，`dead=0`、`ratelimited=0`。
- 4000 `/v1/models` 返回 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- ZenProxy 直连 OpenAI 非流式和 Anthropic 非流式 `PONG` smoke 均 HTTP 200。
- panda NewAPI -> channel 69 的 OpenAI/Anthropic `NEWAPI_PONG` smoke 通过。
- 部署后短窗口未见 `panic`、`Invalid tool parameters`、`Failed to parse JSON`、`lane is saturated`、`no proxy resources`、`stream truncated` 或 `provider_missing_reasoning_content`。
- 部署后日志可见大量 `reasoning_only_length` 首跳空输出和 disabled-thinking retry：这不是资源池故障，但说明上游仍会在短/中请求上先吐 reasoning-only，需要继续看最终是否裸透。
- NewAPI 部署后窗口约 1194 条 deepseek 记录中，type=5 错误 6 条：4 条 `upstream returned no assistant content or tool call (class=reasoning_only)`、1 条 `stream error: error decoding response body`、1 条 `upstream provider error (status=503)`。
- 部署后日志已经出现 `provider cache usage observation`，早期解析到 `accepted=1218`、`rejected=724`；token 口径 `cache_read=159,576,192`、`cache_miss=5,763,765`，hit rate 约 `96.51%`。该窗口混有 warm-up、短请求和用户流量，不能直接当作最终 10k-50k 桶验收结论。

结论：
- V4.106 已部署到 panda，ZenProxy 直连和 NewAPI 外层最小链路均可用。
- 本轮改动仍只影响上游 `x-opencode-session/project` header 分组，不改请求正文、不裁剪上下文、不改提示词、不限制输出。
- 当前不能宣称“零错误”：资源池/工具/崩溃类没有冒头，但 reasoning-only 和少量 NewAPI 500 仍在，需要继续观察。

未完成：
- 还没有 15-30 分钟以上真实流量统计，不能宣称 cache hit 已达到 70%/75% 目标。
- 仍需计算部署后 cc-switch 口径 cache hit、10k-50k 输入桶命中率、accepted/rejected 的 first real token 分位、工具错误和 Markdown/工具质量回归。

下一步：
- 等真实 ClaudeCode 流量累积后，按 NewAPI/ZenProxy/cc-switch 三侧口径复算 cache hit 和首字分位；若 10k-50k 桶仍低于 60%，继续查 prefix 稳定性、session scope 和 provider cache 行为。

## 2026-06-10 11:35

目标：
补 V4.107 cache usage/affinity 口径对齐，降低“provider cache observation 很高但 NewAPI/cc-switch 显示偏低”的统计偏差，并提升中等上下文的节点亲和连续性。

已做：
- `free-model-client-rs`：Anthropic stream final `message_delta.usage` 增加上游真实 `input_tokens`，避免最终 usage 帧只有 output/cache 时下游缺少输入口径。
- `zen-proxy-rs`：流式 usage 合并改为字段存在才更新，后续缺字段帧不再覆盖已有 `input_tokens/cache_read`。
- `zen-proxy-rs`：统一识别 DeepSeek `prompt_cache_hit_tokens`，映射到 `cached_tokens/cache_read_input_tokens`。
- `zen-proxy-rs`：affinity key 对 32KB+ 流式请求生效，移除 body bucket；中等上下文使用 32KB 稳定前缀，大上下文使用 256KB 稳定前缀。
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- `free-model-client-rs`：`cargo fmt -- --check` 通过；`cargo test` 通过，库测试 122 条、kernel golden 112 条；`cargo clippy --all-targets -- -D warnings` 通过。
- `zen-proxy-rs`：`cargo fmt -- --check` 通过；`cargo test` 通过，unit 142 条、e2e 27 条；`cargo clippy --all-targets -- -D warnings` 通过。
- `zen-proxy-rs` release build 通过，stripped hash `e3001320300b37e8daf05266e7c1899652df8f42729a8f029db4c8602d4cd3c5`。
- 2026-06-10 11:28 CST 已滚动部署 panda 三实例，旧版备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.20260610-112855.pre-v4107-b401c9463e29`。
- 部署后 4001/4002/4004/4000 `/health` 通过，`/v1/models` 只返回两个 deepseek 模型。
- ZenProxy 直连 OpenAI/Anthropic 最小 `PONG` smoke 通过；panda NewAPI -> channel 69 OpenAI/Anthropic 最小 `PONG` smoke 通过。
- 部署后约 8 分钟 NewAPI channel 69 记录 1367 条，最终错误 0；流式 20 条，200k+ 桶 18 条。早期 NewAPI 口径 cache 加权：整体约 48.30%，200k+ 桶约 51.33%。

结论：
- V4.107 已上线 panda，最小链路可用，早期最终错误为 0。
- 这是统计/路由亲和补丁，不改变请求正文、prompt、上下文、输出、thinking，也不修改 NewAPI/ClaudeCode/ccswitch。
- 早期 cache 窗口仍处于 warm-up，不能宣称已达到 75% 目标；需要看 15-30 分钟以上真实流量并按输入桶拆分。

未完成：
- 尚未完成 15-30 分钟稳定窗口统计，不能宣称线上 cache hit 或首字已改善。
- 部署后服务日志仍可见 `reasoning_only_length` 首跳警告，这是上游首跳 reasoning-only 后重试路径；当前 NewAPI 聚合未见最终错误，需要继续看是否裸透。

下一步：
- 按 15-30 分钟真实窗口复算 cache hit、FRT/use_time、错误率和工具质量；如果大上下文仍低命中，继续查 session reset、provider/account cache、prefix 稳定性和统计口径。

## 2026-06-10 16:56

目标：
部署 V4.109 ClaudeCode Anthropic 非流式 no-forwardable guard，降低 `/v1/messages stream=false` 在 reasoning-only/空输出场景下拖到 NewAPI 300s 后表现为 `API Error: Failed to parse JSON` 的概率。

已做：
- 本地已构建 `zen-proxy-rs` release，并用 xz 压缩后通过临时 GitHub 仓库 `croppedtravelleralex/new` 中转到 panda。
- panda 下载后校验 xz SHA256 `af53be47528f907b0f1c752e680ebf7fec34635d408a8281f93291190dc4f171`，解压后二进制 SHA256 `67c435b5a02cc1d1ba9839110f01d017aa1acbff14a258f258171e328957b31b`。
- 备份旧线上二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.$ts.pre-v4109-nonstream-guard`，安装新二进制并滚动重启 `zen-proxy-rs@1/@2/@3`。
- 中转仓已强推为空提交；GitHub contents API 返回空列表，raw 包地址返回 404。

验证：
- `zen-proxy-rs@1/@2/@3` 均为 `active`。
- 4001/4002/4004/4000 `/health` 均返回 `status=ok`，`dead=0`、`ratelimited=0`。
- 4000 `/v1/models` 返回 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- panda NewAPI 使用可见 deepseek 的 `ds` token 验证：OpenAI 非流式 `只输出 OK` HTTP 200，总耗时约 1.63s；Anthropic 非流式 forced `echo_tool` HTTP 200，总耗时约 2.46s，返回 `tool_use`。
- 部署后最近 60 秒 NewAPI channel 69 错误为 0；最近 1 分钟 ZenProxy journal 未见 WARN/ERROR。

结论：
- V4.109 已上线 panda，关键 Anthropic 非流式工具 smoke 通过。
- 日志确认新 guard 路径已生效，可看到 `ClaudeCode non-stream guard retrying after reasoning-only/no-forwardable upstream output`。
- 本轮仍只改 ZenProxy/free-model-client-rs 源头侧，不改 NewAPI、ClaudeCode、cc-switch，不裁剪上下文，不限制输出。

残留风险：
- 滚动重启 08:46-08:48 UTC 期间 NewAPI 出现大量 `status_code=503, no proxy resources available`，说明 `/health` 不能代表资源池可接流量；下一次部署要等 active/dispatch 池预热阈值满足后再重启下一实例。
- 08:48 发起的 3 条非流式请求在 08:52 以 `upstream retry budget exhausted ... empty_output` 结束；它们发生在部署/预热窗口，需要继续观察稳定窗口是否复发。

下一步：
- 观察真实 ClaudeCode 长会话，重点看 `Failed to parse JSON`、非流式 300s、`empty_output`、工具调用完整率和 NewAPI channel 69 错误率。
- 补部署脚本的 readiness gate：实例 `/health` 通过后，还要要求资源池可用数量达标，再切下一个实例。

## 2026-06-10 18:18

目标：
响应“先解决然后统一部署”的要求，收口 V4.109 后的剩余错误根因，先在本地完成 V4.110 修复、提交、验证和打包，不提前动 panda 生产。

已做：
- `free-model-client-rs` 已提交 `76fa9f4 fix claude code nonstream no-forwardable retry`：ClaudeCode Anthropic 非流式 no-tool 请求在 no-forwardable/reasoning-only 失败后允许一次 disabled-thinking 恢复重试；不是全局禁用 thinking。
- `zen-proxy-rs` 已提交 `6481003 fix lane classification for default 4096 nonstream output`：`max_tokens=4096` 的小非流式请求不再被误分到 `long_output` lane。
- 重新构建 V4.110 release 包：`/tmp/zen-proxy-rs-v4110` SHA256 `c768a71c928c97e5e9c0839c0eb2bb155ad50312aec9dbf90413d67023dcdd74`；`/tmp/zen-proxy-rs-v4110.xz` SHA256 `056b179f4fb54e5dc057f448678ee6001a6a5b32938689eb5c2f508001f0a074`。
- panda 只读检查确认第三个部署项：nginx 仍为 `worker_connections 768`，错误日志在 17:59-18:00 CST 有大量 `768 worker_connections are not enough`，同时影响 public NewAPI 8081 和内部 ZenProxy 4000 转发。

验证：
- `free-model-client-rs` targeted golden `claude_code_anthropic_non_stream_no_tool_retries_no_forwardable_reasoning_with_disabled_thinking` 通过。
- `zen-proxy-rs` targeted test `routes_default_4096_output_small_nonstream_to_short_lane` 通过。
- V4.110 release build 通过，strip 和 xz 包均完成。
- panda 只读 health/models：4001/4002/4004/4000 `/v1/models` 均 HTTP 200。

结论：
- V4.110 本地修复包已准备好，但尚未部署。
- 当前统一部署应包含三件事：V4.110 二进制、滚动 readiness gate、nginx worker connection 上限调整。
- 这轮仍不改 NewAPI、ClaudeCode、cc-switch，不裁剪上下文，不限制输出，不注入提示词。

下一步：
- 用户确认部署窗口后，通过临时中转或 SSH 上传 V4.110 包，校验 hash 后滚动重启三实例。
- 每个实例重启后不能只看 `/health`，必须等资源池 active/dispatch 达标再切下一个。
- 同一部署窗口内备份并调整 nginx `worker_connections`，`nginx -t` 通过后 reload。
- 部署后复查 NewAPI channel 69 和 nginx error log，重点确认 `empty_output`、`lane is saturated`、`do request failed`、`bad response status code 500`、`worker_connections are not enough` 是否消失或显著下降。
