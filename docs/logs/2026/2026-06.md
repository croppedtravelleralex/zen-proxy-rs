# 2026-06 工作记录

## 2026-06-01 17:20

目标：
继续处理 panda NewAPI 下 ClaudeCode huge_context 空跑、300s timeout、context_drift 问题，只从 ZenProxy/free-model-client 源头修复。

已做：
- 构建并部署包含 latest-user final-anchor 的 `zen-proxy-rs` release 到 panda 三实例。
- 线上二进制从 `94872cd91a558ec431e176af5a1fa8f257219c9518df3f729e7e5645b5cbb937` 更新为 `3a27f2c7cda56119b32dfc42738b06f3f1e08155a0ff89c48daca6ddc8aed1d4`。
- 保留备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-final-anchor-20260601-94872cd`。
- 在 panda 本机通过 NewAPI `/v1/messages` 做约 1.0MB huge stream source-side smoke。

验证：
- 三实例 `/health` 均 200，池 `total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 返回 `OK`。
- huge stream `deepseek-v4-flash` 3/3 返回 `HUGE_OK`。
- huge stream `deepseek-v4-flash-lite` 3/3 返回 `HUGE_OK`。
- ZenProxy 日志确认 `source_client=claude-code`、`compacted_messages=1`、`appended_latest_user_anchor=true`。

结论：
source-side final-anchor 已在 panda 生产链路生效，旧的 “git diff / inspect current state” 语义漂移未在本次 panda 本机 huge smoke 中复现。

未完成：
- 这不是四客户端真实 dry run，不能替代 Windows ClaudeCode、WSL ClaudeCode、WSL Hermes、WSL OpenClaw 验收。
- 日志仍可见上游偶发空输出，当前由 buffered retry 兜底，正式 dry run 需要统计重试率和尾延迟。

下一步：
重新跑 panda-only dry run，并重点观察 ClaudeCode huge_context、lite huge_context、OpenClaw subagent、Hermes 慢路径和上游 502/524/stream decode 分类。

## 2026-06-01 19:45

目标：
执行 panda-only 四客户端 dry run，验证 final-anchor 部署后的真实客户端表现。

已做：
- WSL ClaudeCode dry 50。
- WSL Hermes dry 50。
- WSL OpenClaw dry 50。
- Windows ClaudeCode dry 50，使用 Windows 原生 Python/PowerShell 启动 runner，不再把 WSL interop 结果计入正式 dry run。
- 追加脱敏报告 `docs/reports/panda-dry-run-20260601.md`。

验证：
- 总轮次 200，API OK 200/200，semantic OK 186/200。
- Windows ClaudeCode：50/50 API ok，43/50 semantic ok；6 个 huge_context `context_drift`，1 个 `subagent_not_triggered`。
- WSL ClaudeCode：50/50 API ok，44/50 semantic ok；6 个 huge_context 全部 `context_drift`。
- WSL Hermes：50/50 API ok，50/50 semantic ok；P50/P90/P99 total 约 54.3s/69.5s/103.5s。
- WSL OpenClaw：50/50 API ok，49/50 semantic ok；1 个 lite long_context `context_drift`，subagent 5/5 observed。
- panda 三实例 dry run 后健康：`total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`。

结论：
链路层稳定性明显好于之前：没有认证/模型/API 层失败，没有 300s timeout，runner summary 未观察到 502/504/协议 400。full run 仍不能启动，阻塞点已从链路可用性转为 ClaudeCode huge_context 语义漂移、Hermes 慢路径和 Windows runner 路径问题。

未完成：
- ClaudeCode 真实客户端 huge_context 仍会读取 transcript、git 状态或继续旧任务，source-side final-anchor 还不够。
- Windows ClaudeCode runner 使用 `\\wsl.localhost` UNC 工作目录，CMD fallback 到 Windows 目录，影响 subagent 结果。
- Hermes 功能通过但延迟不达标。

下一步：
先修 ClaudeCode huge_context 的真实客户端 prompt/compactor 策略和 Windows runner 工作目录，再复跑 dry run；不要启动 4 x 500 full run。
