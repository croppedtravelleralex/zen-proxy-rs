# 2026-06 工作记录

## 2026-06-04 15:49

目标：
把包含 `finish_reason` 透传、non-stream cache usage 透传和 ClaudeCode 中等工具历史压缩的最新 `zen-proxy-rs` release 部署到 panda，并做最小生产链路验收。

已做：
- 确认 `/home/lenovo/zen-proxy-rs` 通过 path 依赖引用 `../free-model-client-rs`。
- 在 WSL 本地构建 `zen-proxy-rs` release，并生成未 strip hash `9b68db105aaad2c1014899d00122accf3a21109a26054f68ce0d612f152b5839`。
- strip 产物后得到部署 hash `694036f6a130e8211b998a5b58eff36105fb48fb866ec57ebbb2c03ccfb5f3d7`。
- 通过 SSH 流式传输把新二进制上传到 panda，备份旧线上二进制 `0f6cdf6e5cd2dd1946a69707c97591cca865b47178ff63846f04bbdf283f2314` 到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-v46-20260604-153327-0f6cdf6e5cd2`。
- 替换 `/opt/zen-proxy-rs/zen-proxy-rs` 并重启 `zen-proxy-rs@1/@2/@3`。
- 用有效 `vip` 组 token 对 panda `8081` 做 `/v1/models`、OpenAI 非流式、Anthropic 流式、Anthropic 非流式探针 smoke。

验证：
- 三实例 `systemctl is-active` 均为 `active`。
- 4000/4001/4002/4004 `/health` 均返回 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- `/metrics` 记录 smoke 后 `status=200` 为 2、`status=5xx` 为 1、`stream=2`、`non_stream=1`、`model=deepseek-v4-flash` 为 3。
- `curl --noproxy '*' http://100.69.228.93:8081/v1/models` 返回 200，包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 返回 `PONG`，usage `prompt_tokens=87`、`completion_tokens=27`。
- Anthropic 流式 `deepseek-v4-flash` 返回 `PONG`，usage `input_tokens=87`、`output_tokens=27`。
- Anthropic 极短非流式探针 `reply PONG only` 仍可能返回 502：`upstream retry budget exhausted ... last_error=empty_output`；ZenProxy 日志显示其被归类为 `internal_claude_code_probe`，并在上游连续空输出时耗尽 provider retry。

结论：
- 这轮 release 已成功进入 panda 生产三实例，OpenAI 非流式和 Anthropic 流式真实链路可用。
- `finish_reason` 透传、cache usage 透传和中等工具历史压缩已不是“仅源码修复”，而是线上代码事实。
- 当前残留主要是 Anthropic 极短非流式 probe 的空输出兜底仍不完整；它会污染 smoke/管理探针，但不等于真实流式用户链路失败。

未完成：
- 用真实 ClaudeCode 长会话复验半截输出体感和 `finish_reason=max_tokens` 可见性。
- 在 NewAPI 展示层确认 cache 字段是否开始出现。
- 补 Anthropic `internal_claude_code_probe` 的 non-stream 本地兜底或更细分类，避免极短探针继续打出 502。

下一步：
继续做真实 ClaudeCode 长会话复验，并单独修 Anthropic 极短 non-stream probe 的空输出兜底。

## 2026-06-01 17:20

目标：
继续处理 panda NewAPI 下 ClaudeCode huge_context 空跑、300s timeout、context_drift 问题，只从 ZenProxy/free-model-client 源头修复。

已做：
- 构建并部署包含 latest-user final-anchor 的 `zen-proxy-rs` release 到 panda 三实例。
- 线上二进制从 `94872cd91a558ec431e176af5a1fa8f257219c9518df3f729e7e5645b5cbb937` 更新为 `3a27f2c7cda56119b32dfc42738b06f3f1e08155a0ff89c48daca6ddc8aed1d4`。
- 保留备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-final-anchor-20260601-94872cd`。
- 在 panda 本机通过 NewAPI `/v1/messages` 做约 1.0MB huge stream source-side smoke。

验证：
- 三实例 `/health` 均 200，池 `total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 返回 `OK`。
- huge stream `deepseek-v4-flash` 3/3 返回 `HUGE_OK`。
- huge stream `deepseek-v4-flash-lite` 3/3 返回 `HUGE_OK`。
- ZenProxy 日志确认 `source_client=claude-code`、`compacted_messages=1`、`appended_latest_user_anchor=true`。

结论：
source-side final-anchor 已在 panda 生产链路生效，旧的 “git diff / inspect current state” 语义漂移未在本次 panda 本机 huge smoke 中复现。

未完成：
- 这不是四客户端真实 dry run，不能替代 Windows ClaudeCode、WSL ClaudeCode、WSL Hermes、WSL OpenClaw 验收。
- 日志仍可见上游偶发空输出，当前由 buffered retry 兜底，正式 dry run 需要统计重试率和尾延迟。

下一步：
重新跑 panda-only dry run，并重点观察 ClaudeCode huge_context、lite huge_context、OpenClaw subagent、Hermes 慢路径和上游 502/524/stream decode 分类。

## 2026-06-01 19:45

目标：
执行 panda-only 四客户端 dry run，验证 final-anchor 部署后的真实客户端表现。

已做：
- WSL ClaudeCode dry 50。
- WSL Hermes dry 50。
- WSL OpenClaw dry 50。
- Windows ClaudeCode dry 50，使用 Windows 原生 Python/PowerShell 启动 runner，不再把 WSL interop 结果计入正式 dry run。
- 追加脱敏报告 `docs/reports/panda-dry-run-20260601.md`。

验证：
- 总轮次 200，API OK 200/200，semantic OK 186/200。
- Windows ClaudeCode：50/50 API ok，43/50 semantic ok；6 个 huge_context `context_drift`，1 个 `subagent_not_triggered`。
- WSL ClaudeCode：50/50 API ok，44/50 semantic ok；6 个 huge_context 全部 `context_drift`。
- WSL Hermes：50/50 API ok，50/50 semantic ok；P50/P90/P99 total 约 54.3s/69.5s/103.5s。
- WSL OpenClaw：50/50 API ok，49/50 semantic ok；1 个 lite long_context `context_drift`，subagent 5/5 observed。
- panda 三实例 dry run 后健康：`total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`。

结论：
链路层稳定性明显好于之前：没有认证/模型/API 层失败，没有 300s timeout，runner summary 未观察到 502/504/协议 400。full run 仍不能启动，阻塞点已从链路可用性转为 ClaudeCode huge_context 语义漂移、Hermes 慢路径和 Windows runner 路径问题。

未完成：
- ClaudeCode 真实客户端 huge_context 仍会读取 transcript、git 状态或继续旧任务，source-side final-anchor 还不够。
- Windows ClaudeCode runner 使用 `\\wsl.localhost` UNC 工作目录，CMD fallback 到 Windows 目录，影响 subagent 结果。
- Hermes 功能通过但延迟不达标。

下一步：
先修 ClaudeCode huge_context 的真实客户端 prompt/compactor 策略和 Windows runner 工作目录，再复跑 dry run；不要启动 4 x 500 full run。

## 2026-06-03 11:40

目标：
解决 panda NewAPI channel 69 测试报 `502 upstream returned no assistant content or tool call` 的问题，只从 ZenProxy/free-model-client 源头修复。

已做：
- 在 `free-model-client-rs` 修复空内容无工具健康探测：OpenAI/Anthropic 小探测短路返回本地 `ok`。
- 修正 ClaudeCode huge buffered stream 触发条件：只在修复前估算输入 >= 50k tokens 时启用，不再因 `max_tokens <= 512` 误伤小请求。
- 补充 kernel golden：OpenAI/Anthropic 空 stream probe 短路、ClaudeCode 小低 `max_tokens` 流式请求不进入 huge buffer retry。
- 构建 `zen-proxy-rs` release 并部署到 panda 三实例，保留线上备份。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy -D warnings`、`cargo test` 通过；库测试 61 条、kernel golden 62 条。
- panda 线上二进制 hash 更新为 `28b928472d2abc7be036cdc2796915865bd4a5b083ee2dd1dab46b1ddd0e2633`。
- panda 三个 ZenProxy systemd 实例为 `active`。
- 2026-06-03 11:26 NewAPI 日志出现 channel 69 成功消费记录：`/v1/messages`、`stream_status=ok`、`frt=76ms`。
- 有效 `vip` 组 token 下，panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- 有效 `vip` 组 token 下，OpenAI 非流式 `deepseek-v4-flash` 200，返回 `CHANNEL69_OPENAI_OK`。
- 有效 `vip` 组 token 下，Anthropic 流式 `deepseek-v4-flash` 200，返回 `CHANNEL69_ANTHROPIC_OK`。

结论：
channel 69 本身可用，当前核心问题已修复。此前继续报错的主要误区是把已删除/错误分组 token 当作验收 key，或把 `token_name=ds` 当作明文 API key。

未完成：
- 仍需重新跑 panda-only smoke/dry，验证四客户端真实链路是否还存在 huge_context 语义漂移、Hermes 慢路径、lite 长上下文漂移。
- `.codex_tmp/`、`configured`、`panda`、异常字符文件等历史临时项仍不应提交。

下一步：
只用有效 `vip` 组 token 进入 panda-only smoke/dry；dry run 通过前不启动 4 客户端 x 500 full run。

## 2026-06-03 12:18

目标：
继续修复 NewAPI 管理端“测渠道”仍偶发 `upstream returned no assistant content or tool call`，但普通调用可用的问题。

已做：
- 抓取 panda NewAPI 日志，确认管理端测渠道请求为 `/api/channel/test/69?model=deepseek-v4-flash&stream=true`，内部生成 OpenAI 流式小探测，转到 ZenProxy 后走 `/v1/messages`，`body_size` 约 112-114。
- 确认失败为偶发：11:46:40 一次测渠道失败，11:46:54 和 11:47:44 同接口又成功；普通调用后续多次成功。
- 在 `free-model-client-rs` 增加第二层 fallback：无工具、流式、单用户消息、`max_tokens <= 64` 的 `hi/hello/test/echo hi` 类测渠道探测，如果上游空输出，则返回本地 `ok`，不裸透空输出错误。
- 补充 OpenAI/Anthropic 两条 kernel golden，模拟 `echo hi` 上游空流。
- 构建并部署新的 `zen-proxy-rs` 到 panda，备份旧二进制。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy -D warnings`、`cargo test` 通过；库测试 61 条，kernel golden 64 条。
- panda 线上二进制从 `28b928472d2abc7be036cdc2796915865bd4a5b083ee2dd1dab46b1ddd0e2633` 更新为 `942e69091ef424d92a3e2f4d30d54ea708e46e88cbe782ede789f7b6d77849e1`。
- 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-channel-probe-fallback-20260603-120851-28b9284`。
- panda 4000/4001/4002/4004 `/health` 均 `status=ok`，池约 `total=90/91`、`dead=0`、`ratelimited=0`。
- panda NewAPI 有效 `vip` 组 token 下，`echo hi`、`max_tokens=16`、stream 探测 10/10 HTTP 200，`empty_error=0`。
- ZenProxy 日志确认这 10 轮上游实际为空流，但已被 `short channel-test probe received empty upstream; returning local ok` 兜底。

结论：
普通调用原本可用；管理端测渠道的剩余失败点是 NewAPI 自带极短探测碰到上游空流。当前已从源头兜住该请求形态。

未完成：
- 我不能用 API token 直接调用 NewAPI 管理端 `/api/channel/test/69`，该接口要求网页登录态；最终 UI 按钮结果仍需用户点击一次确认。

下一步：
用户在 NewAPI 管理端再点 channel 69 测试后，继续看 5 分钟 NewAPI/ZenProxy 日志，确认不再出现该错误。

## 2026-06-03 19:30

目标：
排查 panda CPU 突然爆满/短尖峰来源，避免无用探活和空输出重试继续放大；同时补源码层非流式渠道探针兜底。

已做：
- 备份 panda crontab 和 `/usr/local/bin/check-socks5-proxies.sh`。
- 将旧 socks5 每 5 分钟探活 cron 禁用，旧脚本会因为单一 `httpbin.org/ip` 失败而批量 `docker restart`。
- 替换为安全版 socks5 探活脚本：`flock`、3 个探测端点、2/3 成功判定、连续失败阈值 3、单轮最多重启 1 个、重启冷却 30 分钟、跳过新启动容器。
- 确认当前生产 NewAPI/ZenProxy channel 69 不使用这些本地 socks5 容器，ZenProxy 使用 `/opt/zen-proxy-rs/nodes.json` 里的 Webshare 节点。
- 在源码层补非流式渠道探针兜底：`echo hi`/`hi`/`hello`/`test` 类极短无工具非流式探针，只有在上游连续空输出后才返回本地 `ok`；普通请求仍返回结构化空输出错误。
- 同步 `docs/02-current-state.md` 和 `docs/05-ai-maintenance-playbook.md`，明确该源码补丁尚未部署到 panda。

验证：
- panda 19:00 后没有新的 socks5 Docker restart/task-delete 事件；`socks5-check.log` 最后一条为 18:59:46 手动安全探活，4 个容器均 `OK successes=3/3`。
- panda 19:27 load average 为 `0.12/0.11/0.16`。
- ZenProxy 4000 `/health` 正常，nginx 后端采样确认流量分到 `4001/4002/4004` 三实例。
- ZenProxy `/metrics` 当前有历史 `5xx=2`、`retry_budget_exhausted=2`、`client_gone=1`，但并发和 RPM 很低：`active_concurrency=1`、`rpm=2`。
- 本机 Windows Rust 工具链验证通过：`cargo fmt --check`、`cargo test`；结果为库测试 61 条、kernel golden 70 条、doc tests 0 条。

结论：
CPU 持续占用并不高，主要风险是短尖峰：旧 socks5 cron 每 5 分钟因误判批量重启容器是明确放大源，已经禁用并替换为安全脚本。ZenProxy 三实例在跑，当前负载正常。源码层非流式渠道探针补丁已验证，但尚未进入 panda 线上二进制。

未完成：
- 非流式渠道探针源码补丁未部署到 panda：panda 没有 Rust 工具链，本机 WSL rustup 下载 stable toolchain 卡住后已中止；未在生产机强行编译。
- 还未实现更完整的“连续空输出短冷却/熔断”机制；当前只修了渠道探针形态。
- 还未重新跑 NewAPI 管理端真实测渠道按钮，因为该接口需要网页登录态。

下一步：
先准备可控 Linux release 构建环境，再部署非流式渠道探针补丁；随后点 NewAPI channel 69 管理端测试并观察 5 分钟 NewAPI/ZenProxy 日志。若还出现空输出尖峰，再做 per-client/model/request-shape 的短窗口空输出冷却。

## 2026-06-03 20:31

目标：
部署非流式渠道探针兜底，并排查 70k 输入、cache 近乎为 0、ClaudeCode CLI Markdown/表格/代码块/列表格式错乱。

已做：
- 在 `free-model-client-rs` 收窄 body/toolset 客户端识别：普通正文出现 OpenClaw/Hermes 不再作为客户端身份信号；`web_fetch`/`web_search` 不再单独触发 OpenClaw；OpenClaw 仍由 `subagents`、`sessions_*`、`memory_*`、`openclaw*` 等专属工具识别。
- 在 `zen-proxy-rs` 外层 `src/v4/provider.rs` 做同样收窄，避免外层先把 ClaudeCode 请求标成 OpenClaw 后传入内核。
- 新增回归：ClaudeCode + `Task/TodoWrite` + `web_fetch/web_search` 必须识别为 ClaudeCode；普通 OpenClaw/Hermes 文本引用不能覆盖 ClaudeCode 工具信号；Web-only 工具不能识别为 OpenClaw。
- 构建 Linux release，strip 后上传 panda；因 Windows/WSL scp 走 SFTP 很慢，最终使用 gzip + `scp -O` 上传，远端解压后校验 hash。
- 部署到 panda 三实例并滚动重启。

验证：
- `free-model-client-rs`：`cargo fmt -- --check`、`cargo test` 通过；库测试 64/64，kernel golden 70/70，doc tests 0。
- `zen-proxy-rs`：`cargo fmt -- --check`、`cargo test` 通过；主单元测试 129/129，e2e 26/26。
- release hash：未 strip `7ff411a5ae72aeb267044cdbb4aa09b7522891f56a3cb7924153cbbd6ce97f4c`；strip 部署 hash `e47e2c89d7c0c497daa2cd49a9d135a8b695928e951f81a22630b206a0e2ab51`。
- panda 旧 hash：`cdafae89c2f7f3ec7d4b453dd14555c41e9456f5a0d5730456b80ed64fe3c19f`。
- panda 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-profile-format-20260603-202631-cdafae8`。
- panda 新 pid：`zen-proxy-rs@1=365354`、`@2=365378`、`@3=365392`；4001/4002/4004 和 4000 `/health` 均 `status=ok`，dispatch=90、dead=0、ratelimited=0。
- panda `/v1/models` 200，只暴露 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- 受控 `Task + web_fetch` `/v1/messages` 请求进入新 pid 后日志为 `source_client=claude-code`，证明格式误伤的核心识别路径已修正。

结论：
- 70k/60k 输入不是 NewAPI 随机行为，主要来自默认流式 compactor：超过约 80k tokens 后压到约 60k tokens；部署前 ClaudeCode 被误识别为 OpenClaw 时，会错过 ClaudeCode huge-context 的更激进压缩策略。
- cache 近乎为 0 是因为当前上游 usage 基本不返回 provider cache 字段；ZenProxy 当前只转发 `cache_creation_input_tokens`、`cache_read_input_tokens` 或 `cached_tokens`，不伪造缓存命中。
- ClaudeCode CLI 格式错乱的一个明确源头是 profile 误伤：一旦被判成 OpenClaw/Hermes，流式内容会走 Markdown 修复/脱敏路径，不再逐字保留 Markdown 和空白。该识别误伤已修复并部署。

未完成：
- 还没有拿 Windows ClaudeCode 真实终端重新跑格式全集；若 raw SSE 正确但终端仍乱，再归类为 CLI 渲染问题。
- 受控小请求由于上游慢路径 60 秒超时，未作为模型质量验收，只用于 ingress profile 验证。
- 未重新跑四客户端 dry/full 压测。

下一步：
让 Windows ClaudeCode 真实跑一轮 Markdown 格式全集，同时抓 ZenProxy raw SSE 和新 pid `source_client`；若 `source_client=claude-code` 且 raw SSE 正确，则不再从 ZenProxy 修改格式。

## 2026-06-03 21:05

目标：
对 WSL ClaudeCode、WSL Hermes、WSL OpenClaw 做小样本对比，验证 profile 区分是否正确，并收集后续 per-client 适配数据。

已做：
- 从 panda NewAPI token 表选择有效 `vip` 组 token id=38/name=ds，仅写入本地临时 `/tmp/panda_newapi_key.env`，未写入仓库，未输出明文 key。
- 跑 `panda_pressure_runner.py --mode preflight --clients wsl-claudecode,wsl-hermes,wsl-openclaw --models deepseek-v4-flash`。
- 分别跑 WSL Hermes smoke、WSL OpenClaw smoke；三客户端混合 smoke 因 WSL ClaudeCode 连续 timeout 被中止，保留 partial raw results。
- 追加报告 `docs/reports/client-profile-smoke-20260603.md`。

验证：
- preflight 通过：models 200；chat 200；返回 `OK`。
- WSL ClaudeCode partial：2/2 API 路径进入 CLI，但 2/2 `client_timeout`，每条约 90s；runner 只捕获到 system init。
- WSL Hermes smoke：5/5 API ok，5/5 semantic ok；P50 33.7s，P90 44.0s；工具 2/2。
- WSL OpenClaw smoke：5/5 API ok，0/5 semantic ok；错误为 semantic mismatch、tool runtime、context drift、subagent not triggered；输出前缀集中为 `HEARTBEAT_OK`，stderr 有 local secrets gateway 1006 warning。
- ZenProxy 20:50 后识别分布：`claude-code=133`、`hermes=13`、`openclaw=5`。
- ZenProxy metrics：162 个请求均为 `200/success`，`5xx=0`、`timeout=0`、`429=0`，dispatch=90、dead=0、ratelimited=0。

结论：
三类客户端已经能被 ZenProxy 正确区分。当前问题不再是 OpenClaw/Hermes 适配打到 ClaudeCode 上，而是：
1. ClaudeCode 链路存在 CLI timeout，需要单独抓 raw SSE 和 CLI 输出。
2. Hermes 可用但慢，需要分段耗时。
3. OpenClaw API 通但本地 agent/gateway/提示形态不合格，需要先修 OpenClaw harness/config，再谈模型适配。

下一步：
先修 OpenClaw 本地 secrets gateway/config/agent harness 的 `HEARTBEAT_OK` 偏移；同时为 ClaudeCode 单独做 Markdown 格式全集和 timeout 复现。

## 2026-06-03 21:31

目标：
继续复测 WSL ClaudeCode / Hermes / OpenClaw，收集 raw API、CLI、ZenProxy ingress 三侧证据，判断 ClaudeCode 格式错乱和 timeout 是否仍来自 ZenProxy profile 误伤。

已做：
- 通过 panda NewAPI 重新跑 preflight，`/v1/models` 与最小 chat 均通过。
- 跑三客户端 smoke：`20260603-210902`。
- 发现该轮 WSL ClaudeCode 是 root 进程，5/5 被 ClaudeCode 拒绝 `--dangerously-skip-permissions`，该段只作为 runner 环境错误记录。
- 切换到 WSL `lenovo` 用户重跑 WSL ClaudeCode：`20260603-211511`，short prompt 90s timeout，stdout 只到 system init。
- 直接 raw POST panda NewAPI：`/v1/chat/completions` 与 `/v1/messages` 的 stream/non-stream 均能返回 `OK`。
- 用 `x-fmc-client=claude-code` raw `/v1/messages` 跑 Markdown 格式检查，返回表格、代码块、引用块、列表均完整。
- 统计 21:08 后 ZenProxy ingress body 分布，确认三类客户端被正确分开。
- 确认本地无残留 runner/claude/openclaw/hermes 进程；ZenProxy active_concurrency 后续自然回到 0。

验证：
- raw `/v1/messages`：非流式约 2.8s，流式约 3.7s，均返回 `OK`。
- raw Markdown `/v1/messages`：约 31.5s 返回完整 Markdown，`has_table/has_fence/has_quote/has_list` 均为 true。
- 21:08 后 ZenProxy ingress：`claude-code=182`、`hermes=20`、`openclaw=5`。
- ClaudeCode body size：min 118，p50 144533，max 463984；46 个请求超过 200KB。
- Hermes body size：p50 479541，20 个里 14 个超过 200KB。
- OpenClaw body size：5 个均在 50KB-200KB。
- 空目录 ClaudeCode short prompt 仍会产生约 291KB-305KB 请求，估算 56k-59k tokens；还出现约 467KB / 72k tokens 请求；`max_tokens=32000` 被 ZenProxy cap 到 1024。
- 空目录测试中出现 body 342 非流式请求，多次 `non-stream upstream returned empty output` retry。

结论：
- profile 分离已经生效，ClaudeCode 新请求没有被 OpenClaw/Hermes 适配覆盖。
- ZenProxy raw 响应没有破坏 Markdown；若 ClaudeCode 终端仍显示错乱，优先排查 CLI 渲染、流式事件消费、插件/skills/工具 schema 造成的大上下文请求形态。
- ClaudeCode timeout 不是 panda NewAPI/ZenProxy `/v1/messages` 基础链路不可用，而是 ClaudeCode CLI 实际发送的请求远大于表面 prompt，并伴随小非流式空输出重试。
- Hermes 可用但慢，且 tool_read 有 90s timeout。
- OpenClaw API 通但语义不通，仍集中为 `HEARTBEAT_OK` 和 local secrets gateway 1006 warning。

下一步：
专项抓 ClaudeCode 完整 raw SSE 与终端渲染；拆分 ClaudeCode 300KB+ 请求体来源；识别 body 342 非流式空输出请求用途并在 ZenProxy/free-model-client-rs 源头适配；OpenClaw 先修 gateway/harness 后再做模型适配。

## 2026-06-03 22:14

目标：
拆分 ClaudeCode 300KB+ 请求体来源，识别 body 342 非流式空输出请求用途，并检查 Windows ClaudeCode CLI 是否存在输入 token 限制或链路混淆。

已做：
- 读取当前维护文档、backlog 和 21:31 复测记录，确认本轮只做证据采集和待办编排，不改运行代码。
- 拉取 panda 21:35-21:50 ZenProxy 日志并统计 `source_client/body_size/context_action/stream`。
- 阅读 `free-model-client-rs` 的流式 compactor、流式输出 cap、非流式空输出 retry、短探针 fallback 代码。
- 检查 Windows ClaudeCode CLI 版本、路径、环境变量、`settings.json`、插件配置和 cc-switch 监听进程。

## 2026-06-04 16:40

目标：
检查 NewAPI 为什么读不到/不显示 cache，并定位 ClaudeCode 使用中的“输出被截断”到底属于哪一层。

已做：
- 复查维护文档、`src/proxy/openai.rs`、`src/proxy/anthropic.rs`、`src/protocol/translate.rs`、`src/zen/client.rs`、`src/client_profile.rs`、`src/proxy/markdown.rs`。
- 确认非流式 cache 丢失的源头：OpenAI 非流式响应未带 `cache_*`，Anthropic 非流式正文/工具调用把 `cache_*` 写死为 `0`。
- 修复 OpenAI/Anthropic 非流式正文和工具调用两条分支的 cache usage 透传。
- 新增 kernel golden 4 条回归：OpenAI/Anthropic 非流式正文、工具调用均检查 `cache_creation_input_tokens/cache_read_input_tokens` 保留。
- 读取 ClaudeCode 终端样本，确认 `… +N lines (ctrl+o to expand)` 是客户端工具输出折叠，不是 ZenProxy 截断正文。
- 对照源码确认当前流式输出 cap：估算 prompt `>=50k/100k/200k` 时，流式 `max_tokens` 会分别被 cap 到 `1024/768/512`。

验证：
- WSL `lenovo` 用户下执行 `cargo fmt -- --check` 通过。
- WSL `lenovo` 用户下执行 `cargo test -q` 通过：库测试 71 条、kernel golden 82 条。
- WSL `lenovo` 用户下执行 `cargo clippy --all-targets -- -D warnings` 通过。

结论：
- NewAPI cache 不可见至少有一部分是源头问题，现已在源码层修复；但还未部署到 panda，线上不应视为已恢复。
- ClaudeCode 的“输出截断感”当前最明确的源头是服务端流式输出 cap，不是 `ctrl+o` 这类客户端折叠展示，也不是 Markdown fence 修复。

未完成：
- 本轮没有部署到 panda，也还没有拿真实 NewAPI 线上记录复验 cache 展示。
- 还没有调整 ClaudeCode 大流式 1024/768/512 硬 cap；当前只完成根因定位，没有改策略。

下一步：
- 如继续推进，先部署 cache usage 修复到 panda，再用真实 NewAPI 日志确认显示是否恢复。
- 对 ClaudeCode 长会话补 `requested_max_tokens/effective_max_tokens` 观测，再决定是否把流式 cap 改为可配置或按 profile 分桶。

## 2026-06-04 16:55

目标：
继续排查 ClaudeCode 使用 ZenProxy deepseek 时“原本 3 段只输出 1.5 段”的真实原因，不能再用客户端折叠展示解释。

已做：
- 读取 panda 最近两小时 ZenProxy 日志和 NewAPI `logs` 表，按 channel 69 统计 prompt/completion tokens。
- 确认 14:56-14:58 多条 ClaudeCode 工程请求为 26k-40k prompt tokens，并未全部触发 50k+ 输出 cap；大量样本 completion tokens 只有 100-700。
- 确认同窗口存在长输出成功样本：`prompt_tokens=11906`、`completion_tokens=6985`、耗时约 82s，说明模型/链路不是整体不能长输出。
- 确认源码已解析上游 `finish_reason`，但 OpenAI/Anthropic 响应此前固定写成 `stop/end_turn`，会隐藏上游 `length`。
- 确认近期 ClaudeCode 工程请求常见 `last_user_tokens=3`、最大旧消息约 26k、工具 schema 6k-7k，旧工具历史会淹没当前短指令。
- 修复 OpenAI/Anthropic 非流式、流式和 buffered 流式的 `finish_reason/stop_reason` 透传。
- 增加 ClaudeCode 中等工具历史压缩：消息很多、消息 token 24k+、单条旧输出 12k+、最新用户指令 <=1024 tokens 时触发。

验证：
- 新增 OpenAI 非流式/流式 `finish_reason=length` 回归。
- 新增 Anthropic 非流式/流式 `length -> max_tokens` 回归。
- 新增 ClaudeCode 中等工具历史折叠回归。
- WSL `lenovo` 用户下 `cargo fmt -- --check` 通过。
- WSL `lenovo` 用户下 `cargo test -q` 通过：库测试 71 条、kernel golden 87 条。
- WSL `lenovo` 用户下 `cargo clippy --all-targets -- -D warnings` 通过。

结论：
- 用户描述的半截输出不是客户端 `ctrl+o` 折叠能解释的。
- 当前最明确的两个源头是：上游提前结束原因被吞掉；中等 ClaudeCode 工具历史未压缩导致当前短指令被旧输出淹没。
- 50k+ 输出 cap 仍是风险，但不是这批样本的唯一原因。

未完成：
- 本轮仍未部署到 panda。
- 部署后必须用真实 ClaudeCode 工程会话复验 stop_reason、completion tokens 和体感输出完整度。

下一步：
- 部署包含 cache usage、finish_reason 透传和中等工具历史压缩的二进制到 panda。
- 部署后抓 NewAPI 和 ZenProxy 日志，重点看是否出现 `stop_reason=max_tokens`、completion tokens 是否上升、半截输出是否减少。
- 读取 cc-switch SQLite DB 的表结构、provider endpoint、proxy config 和最近 Claude 请求日志；API key/token 均未写入仓库，报告中不记录明文。
- 更新 `docs/04-improvement-backlog.md` 和 `docs/05-ai-maintenance-playbook.md`。

验证：
- 21:35-21:50 ZenProxy ingress 共 38 条：`claude-code=2`、`hermes=36`。
- 21:44 ClaudeCode 两条请求：`body_size=472161/474175`，`context_action=pass`，`stream=true`；free-model-client-rs 日志显示只做输出 cap：`prompt_tokens=72826/73017`，`max_tokens=32000 -> 1024`。
- 21:37-21:48 Hermes 多条请求：`before_tokens≈104k-113k`，压缩后 `after_tokens≈88k-97k`，并将 `max_tokens=32000 -> 768`；这与 NewAPI 截图里 88k-90k input tokens 对得上。
- body 342 线索：21:28:16 出现 `source_client=claude-code`、`stream=false`、`body_size=342` 的 `/v1/messages`，随后 `non-stream upstream returned empty output` retry 三次；21:28:38 又开始下一轮非流式空输出 retry。
- Windows ClaudeCode：版本 `2.1.138`；当前 `ANTHROPIC_BASE_URL=http://127.0.0.1:15721`；端口 15721 由 `D:\SelfMadeTool\CCSwitch-v3.16.0\cc-switch.exe` 监听。
- Windows ClaudeCode settings：启用 `CLAUDE_CODE_EFFORT_LEVEL=max`、`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`、`ENABLE_TOOL_SEARCH=true`，并启用 `glm-plan-bug`、`glm-plan-usage` 插件；本机还安装了 context7、playwright、security-guidance、typescript-lsp、everything-claude-code 等插件。
- cc-switch proxy config：Claude proxy 开启，监听 `127.0.0.1:15721`，日志开启，`max_retries=6`，stream first-byte timeout 90s，non-stream timeout 600s。
- cc-switch 最近 Claude 请求 provider 为 `closedeepseek -> https://sub2api.closeapi.top`；`LocalNewapi -> http://127.0.0.1:4000/v1` 存在但不是最近记录里的 provider。
- cc-switch 最近 20 条 Claude 日志出现 `input_tokens=76728`、多条约 40k，也有 20/27/166/174 等小请求；未发现 cc-switch 层存在 90k 输入 token 墙字段。

结论：
- NewAPI 里看到 70k-90k input tokens 不是已确认的输入墙。至少本轮有两类来源：Hermes/默认流式 compactor 将 104k+ 压到 88k-97k；ClaudeCode 72k-73k 则是未压缩输入，只有输出 max_tokens 被 cap。
- Windows ClaudeCode 最近并非直接打 panda NewAPI channel 69，而是先打 cc-switch；cc-switch 最近又路由到 closeapi 直连 provider。因此 Windows 本机使用日志和 panda NewAPI channel 69 截图不能直接合并归因。
- ClaudeCode 300KB+ 请求体大概率由 ClaudeCode 自带系统提示、工具 schema、plugins/skills、agent 信息、历史上下文和模型别名共同组成；当前还缺少脱敏 request-shape 采样，不能精确给出每项占比。
- body 342 非流式请求基本可以定位为 ClaudeCode 内部小请求或探测/摘要类请求，但当前日志没有保存脱敏形态，不能确认具体用途。

下一步：
实现脱敏 request-shape 采样和小非流式请求分类；为 ClaudeCode profile 输出 `system/messages/tools/largest_message` token 分布；确认 Windows cc-switch 当前 active provider 后，再分别做 `Windows ClaudeCode -> cc-switch` 和 `panda NewAPI channel 69 -> ZenProxy` 两条链路的独立验收。

## 2026-06-03 22:54

目标：
落地第一阶段脱敏 request-shape 采样和 ClaudeCode 小非流式空输出请求分类，先增强源头观测，不调整 compactor 阈值，不新增普通请求 fallback。

已做：
- 在 `src/protocol/translate.rs` 新增 `RequestShape` 和 `ShortNonStreamRequestKind`。
- 在 OpenAI/Anthropic 入口统一记录脱敏 shape：`system_tokens/messages_tokens/tools_tokens/tool_count/message_count/largest_message_tokens/last_user_tokens/estimated_total_tokens/stream/max_tokens/tool_choice_present/prompt_hash/source_client/profile_source`。
- `prompt_hash` 使用消息增量 hash，不再为了 hash 拼出完整 prompt 字符串，降低大上下文请求的额外分配。
- 在非流式空输出 retry 日志增加 `short_request_kind/prompt_hash/prompt_tokens/message_count/max_tokens/source_client`。
- 保持现有 fallback 边界：只有 `hi/hello/test/echo hi` 等 channel-test probe 在上游连续空输出后返回本地 `ok`；普通短请求不被误短路。
- 补单元测试和 kernel golden，覆盖不泄露原文、工具 schema token 计数、channel-test 分类、普通短请求分类、ClaudeCode 小非流式非探针不返回本地 `ok`。
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo fmt -- --check`：通过。
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo clippy --all-targets -- -D warnings`：通过。
- `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo test`：通过，库测试 69 条、kernel golden 71 条、doc tests 0 条。

结论：
- 第一阶段源码已完成：下次看到 `body_size=342` 或 300KB+ ClaudeCode 请求，可以用脱敏 shape 和 `short_request_kind` 做归因，不再依赖原始 prompt。
- 当前改动没有部署到 panda；线上日志暂时不会出现这些新字段。
- 当前没有改变 compactor 阈值，也没有对 `internal_claude_code_probe` 自动返回本地 `ok`，避免误伤真实用户短请求。

未完成：
- 部署到 panda 后复测真实 ClaudeCode/Hermes/OpenClaw 请求，确认 shape 字段可采集。
- 真实样本确认后，再决定是否给 ClaudeCode 内部小探测加安全 fallback 或短冷却。
- Windows ClaudeCode 仍需按独立链路 `ClaudeCode -> cc-switch(15721) -> provider` 做 raw/CLI 对照，不和 panda channel 69 统计混用。

下一步：
提交前复查 git diff 和未跟踪文件；如要上线，先构建 Linux release，上传 panda 灰度部署，再从 NewAPI/ZenProxy 日志验证新 shape 字段。

## 2026-06-03 23:45

目标：
将 request-shape 观测改动随 `zen-proxy-rs` release 部署到 panda，并验证 NewAPI -> ZenProxy -> free-model-client-rs 链路可用。

已做：
- 确认 `zen-proxy-rs` 通过 path dependency 嵌入本地 `../free-model-client-rs`。
- 本地构建 `zen-proxy-rs` release，并生成 stripped 部署包。
- 上传时普通 `scp` 在 tailnet 上中断，远端只留下 3MB 残片；已删除残片，改用 gzip + rsync `--partial --inplace` 续传成功。
- 备份旧二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-request-shape-20260603-232757-e47e2c8`。
- 替换 `/opt/zen-proxy-rs/zen-proxy-rs`，滚动重启 `zen-proxy-rs@1/@2/@3`。
- 用 panda Postgres 中 token id `38`/name `ds`/group `vip` 做本机 NewAPI smoke；未输出明文 key。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy --all-targets -- -D warnings`、`cargo test` 通过；库测试 69 条、kernel golden 71 条。
- `zen-proxy-rs`：`cargo test` 通过，单元测试 129 条、e2e 26 条；`cargo build --release` 通过。
- 本地未 strip release hash：`1e77a544b5c444b4d626b14327862259cfe1b26221ffbd6f87778c1afc321376`。
- 部署 stripped hash：`28b25370925835bb33aa4142208a5a20f0cf4dcb74ad3ae74c3808d3c2761e2b`。
- 旧线上 hash：`e47e2c89d7c0c497daa2cd49a9d135a8b695928e951f81a22630b206a0e2ab51`。
- 三实例新 pid：4001=`462903`、4002=`462918`、4004=`462981`；三实例和 nginx 均 active，`/health` 200。
- `/metrics`：`zen_proxy_requests_total{status="200"} 6`，`5xx=0`，`timeout=0`，`active_concurrency=0`，dead=0，ratelimited=0。
- panda NewAPI 8081 smoke：HTTP 200，返回 `OK`，总耗时约 4.01s。
- NewAPI 最近日志：id `109472`，channel id `69`，token id `38`，token name `ds`，model `deepseek-v4-flash`，non-stream，use_time `4`，prompt tokens `93`，completion tokens `47`。
- ZenProxy 日志已出现新字段：小非流式样本 `short_request_kind="internal_claude_code_probe"`，并在 retry 日志里带 `prompt_hash/prompt_tokens/message_count/max_tokens`；真实 ClaudeCode 787KB 流式样本记录 `message_count=703`、`system_tokens=5773`、`messages_tokens=78859`、`tools_tokens=12700`、`tool_count=10`、`estimated_total_tokens=97332`。

结论：
- request-shape 观测已在线上生效，能拆出 ClaudeCode 大请求里的 system/messages/tools 占比。
- `body_size=342` 同类小非流式请求现在可通过 `short_request_kind` 归因；当前仍只是观测，不自动本地 `ok`。
- 之前 WSL 侧 502 是本机 proxy 环境误走代理导致，请求没有到 ZenProxy；清空 proxy env 或在 panda 本机测试即可避免误判。

未完成：
- 继续观察更多真实 `internal_claude_code_probe` 样本，判断 retry 是否经常成功、是否需要短冷却或安全 fallback。
- 若后续要从本机 WSL 直测 panda，必须清空 `http_proxy/https_proxy/ALL_PROXY` 并使用 `--noproxy "*"`.

下一步：
跑一小段真实 ClaudeCode/WSL/Hermes/OpenClaw 混合 smoke，基于新 shape 字段分析请求构成和首字/重试关系。

## 2026-06-04 00:12

目标：
排查用户反馈的 “ClaudeCode CLI 用 deepseek-v4-flash 一直反复做”，并解释 NewAPI 中看到 200k+ prompt tokens 记录是否代表上下文未截断。

已做：
- 拉取 panda 当前时间、服务状态、NewAPI channel 69 最近日志和 ZenProxy 三实例 journal。
- 对齐 NewAPI 记录与 ZenProxy `v4 ingress request`、`compacted streaming anthropic context before upstream`、`desensitized request shape before upstream` 日志。
- 阅读 `src/proxy/anthropic.rs`、`src/proxy/openai.rs` 和 `src/protocol/translate.rs`，确认流式/非流式 compactor 边界。
- 检查 panda 当前 CPU/负载和 `/metrics`，确认排查时不是服务器 CPU 打满。

验证：
- 2026-06-03 23:01-23:46 panda channel 69 出现一串 ClaudeCode 大流式请求：body 从约 674KB 增到 788KB，`before_tokens` 约 97k-110k，流式 compactor 后 `after_tokens` 约 66k-79k，`max_tokens` 被 cap 到 1024 或 768。
- 部署 request-shape 后的真实样本显示：`message_count` 从 670 增到 705，`tools_tokens=12700`，`tool_count=10`，`last_user_tokens` 多数只有 36-96，`estimated_total_tokens` 约 96k-98k。
- NewAPI channel 69 最近窗口共 61 条：57 条流式、4 条非流式；53 条 prompt tokens 在 70k-90k，2 条 prompt tokens 超过 200k，2 条 prompt tokens 为 0 且内容是 `status_code=500, upstream returned no assistant content or tool call`。
- 200k+ 两条不是常规流式轮次：id `109370` 为非流式 213248 prompt tokens，id `109461` 为非流式 225416 prompt tokens。23:39:05 对应 ZenProxy 日志显示 `stream_seen_by_zenproxy=false`、`max_tokens=Some(1024)`、`message_count=701`、`largest_message_tokens=26563`、`estimated_total_tokens=128229`。
- 23:38:49 流式请求先出现 NewAPI id `109459`，prompt tokens 记 0，内容为 `upstream returned no assistant content or tool call`；随后 23:39:05 出现非流式大请求 id `109461` 并成功返回。
- 代码确认：`compact_stream_context_with_policy` 只在 `stream_requested=true` 时调用；非流式路径只调用 `non_stream_output_policy` cap 输出 tokens，不压输入上下文。
- panda 排查时负载约 `0.12/0.13/0.10`，ZenProxy `/metrics` 为 `5xx=0`、`timeout=0`、active concurrency 0，问题不是当前服务器 CPU 满载。

结论：
- “200k 记录”不是证明流式路径已经完整吃下 200k 上下文，而是 ClaudeCode 在流式空输出后重发了一条非流式大请求；非流式路径目前没压输入，所以 NewAPI 账面会看到 200k+。
- “一直反复做”的直接证据是同一会话持续追加历史：`message_count` 增长到 700+，最新用户请求只有几十 tokens；当前 compactor 主要压单条大文本，无法折叠大量旧短消息和工具历史，因此 active anchor 仍可能被旧任务轨迹淹没。
- 这不是 profile 误伤导致的 OpenClaw/Hermes 策略打到 ClaudeCode；日志中 `source_client=ClaudeCode`、`profile_source=Header`。

下一步：
从源头修 ClaudeCode huge-context 策略：增加旧短轮次历史折叠/裁剪、给大非流式 fallback 加输入保护、统计 `stream empty -> non-stream fallback` 链路。修复前不要把 70k-90k 或 200k NewAPI 数字当成稳定上下文质量验收。

## 2026-06-04 00:24

目标：
阅读用户提供的 ClaudeCode CLI 实际输出片段，确认 “一直反复做” 是否只是体感，还是可由日志文本证明。

已做：
- 读取用户贴文 `pasted-text.txt`，不复述其中本地 Bearer token。
- 对贴文做机械计数，统计重复动作和错误形态。
- 拉取 panda NewAPI channel 69 最新记录和 00:00 后 ZenProxy 日志，确认贴文末尾后 panda 侧只有一次小 `channel_test`，没有新的大上下文轮次。

验证：
- 贴文中同一目标被重复执行：`开始导出` 出现 11 次，`检查` 16 次，`清理` 7 次，`重启` 10 次。
- 运行状态反复恶化：任务从 `running=23` 增长到 `running=53/74/96/117`，说明模型没有执行 single-flight/幂等保护，重复提交了导出任务。
- QCE/NapCat 状态处理失控：`502` 出现 29 次，`timeout` 出现 29 次，`Interrupted` 出现 7 次，`Invalid tool parameters` 2 次，`API Error: Failed to parse JSON` 1 次。
- 贴文显示模型在扫码成功/即将成功后仍继续执行重启/杀进程命令，导致登录态被破坏；这属于工具执行计划失控，不是单纯 API 不通。
- panda 00:03 仅有小非流式 `channel_test` 请求，`short_request_kind="channel_test"`，上游连续空输出后由探针 fallback 处理；贴文的大量导出/重启循环主要对应更早 23 点段的 ClaudeCode 大上下文链路。

结论：
- 用户贴文直接证实：当前问题是 “长会话上下文漂移 + 工具执行幂等缺失 + 上游空输出/非流式 fallback 膨胀” 叠加，不是简单首字慢或单次请求失败。
- 仅靠保留 200k 上下文不会解决，反而可能保留更多旧错误动作；必须在 ZenProxy/free-model-client-rs 源头对 ClaudeCode 长会话做旧工具历史折叠、最新目标置顶和大非流式 fallback 输入保护。

下一步：
实现 ClaudeCode huge-session compactor，而不是继续扩大上下文窗口：折叠旧 Bash 输出和旧工具结果、保留最新用户目标和最近有效状态、压制非流式大 fallback 对旧历史的二次放大。

## 2026-06-04 00:36

目标：
从源头解决 ClaudeCode deepseek 长会话反复执行旧任务、非流式 200k+ fallback 放大旧历史的问题。

已做：
- 在 `src/protocol/translate.rs` 新增 `compact_claude_code_huge_session_context`。
- huge-session compactor 先执行原有大文本 compactor，再在消息数过多或压缩后仍过大时折叠旧短轮次历史。
- 折叠策略保留系统消息、最近 48 条消息、最新用户 anchor 和少量脱敏状态信号；旧 Bash/工具输出不会整段复制。
- Anthropic/OpenAI 两个入口都接入 ClaudeCode huge-session compactor，覆盖 stream 和 non-stream。
- 非流式输出 cap 改为使用压缩前原始 prompt token 口径，避免大 fallback 压缩后误放宽输出上限。
- 新增回归测试：700 条短历史必须折叠；Anthropic 非流式大 fallback 进上游前必须压缩。
- 构建 `zen-proxy-rs` release，部署到 panda 三实例。

验证：
- `free-model-client-rs`：`cargo fmt -- --check` 通过；`cargo clippy --all-targets -- -D warnings` 通过；`cargo test` 通过，库测试 69 条、kernel golden 73 条。
- `zen-proxy-rs`：`cargo test` 通过，主单测 129 条、e2e 26 条；`cargo build --release` 通过。
- 本地未 strip release hash：`408fa673aef439e849ca9e24d41576810c122faa71724581ce8067e10c04fc80`。
- 部署 stripped hash：`96b954a81978e9348f26341d68626d0a98682c6971611d7802a0850ef771d815`。
- 旧线上 hash：`28b25370925835bb33aa4142208a5a20f0cf4dcb74ad3ae74c3808d3c2761e2b`。
- 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-huge-session-20260604-003040-28b2537`。
- 新 pid：4001=`498728`、4002=`498733`、4004=`498734`；三实例和 nginx active，4000/4001/4002/4004 `/health` 均 200。
- panda 非流式 smoke：517KB 请求、`before_tokens=123371`，线上日志显示 `compacted non-stream anthropic context before upstream`，压到 `after_tokens=9139`、`message_count=51`；NewAPI id `109585` 为 HTTP 200，`prompt_tokens=5647`。
- panda 流式 smoke：522KB 请求、`before_tokens=124597`，线上日志显示 `compacted streaming anthropic context before upstream`，exact-anchor 后 shape 为 `message_count=1`、`estimated_total_tokens=51`；NewAPI id `109593` 为 HTTP 200，`prompt_tokens=125`。

结论：
- 源头修复已部署：ClaudeCode 长会话不再只压单条大文本，700+ 旧短消息会被折叠；非流式大 fallback 也会压输入，不再让 NewAPI 看到同类 200k+ 脏历史。
- 这仍需真实 Windows/WSL ClaudeCode 长会话复测，因为 smoke 证明压缩链路生效，但不能完全等同于真实导出/扫码任务不会再被模型搞乱。

下一步：
用真实 ClaudeCode 长会话复测用户贴文中的导出/扫码/等待外部动作场景；重点验收不重复提交导出、不在用户扫码后继续重启/杀进程、不再出现 200k+ 非流式 fallback 放大旧历史。

## 2026-06-04 10:55

目标：
按严格验收复测 panda NewAPI channel 69，并修复短 non-stream smoke 仍可能 502 的问题。

已做：
- 重新运行 `free-model-client-rs` 的 `cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`。
- 重新运行 `zen-proxy-rs` 的 `cargo test` 和 `cargo build --release`。
- 在 panda 本机用有效 NewAPI token 测 `/v1/models`、OpenAI `/v1/chat/completions`、Anthropic `/v1/messages`，覆盖两个 deepseek 模型、stream/non-stream。
- 发现并修复：极短 non-stream smoke 经 NewAPI 转为 ClaudeCode/Anthropic 小请求后，上游连续空输出会在 `internal_claude_code_probe` 分类下裸透 502。
- 新增 `short_no_tool_empty_fallback_text`，只对显式测试探针兜底；普通 ClaudeCode 短输入仍不兜底。
- 部署新 stripped 二进制到 panda 三实例。

验证：
- 修复前：panda 本机 NewAPI 短 non-stream `deepseek-v4-flash` 返回 502，错误为 `upstream retry budget exhausted ... last_error=empty_output`。
- 修复后 `free-model-client-rs` 测试通过：库测试 70 条，kernel golden 75 条。
- 修复后 `zen-proxy-rs` 测试通过：主单测 129 条，e2e 26 条；release build 通过。
- 新部署 hash：`0f1d7a36fdc7142e1acd9670301e7277ca6805e47899490958a2c390c619cea5`。
- 旧线上 hash：`96b954a81978e9348f26341d68626d0a98682c6971611d7802a0850ef771d815`。
- 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-strict-smoke-20260604-105132-96b954a`。
- panda 本机 NewAPI `/v1/models` 返回 200，模型数 8，包含 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- 两个 deepseek 模型的 OpenAI/Anthropic、stream/non-stream 共 8 条 smoke 全部 HTTP 200，内容摘要均为 `PASS`。
- 耗时：non-stream 总耗时约 4.8-5.5s；stream 首内容约 2.0-2.3s。
- 线上日志显示短探针仍会先尝试上游，连续空输出后走本地兜底，不再裸透 502。
- WSL 到 `100.69.228.93:8081/v1/models` 直连返回 200；Windows `curl --noproxy '*'` 直连返回 200。
- Windows `Invoke-RestMethod` 走环境变量 `HTTP_PROXY=http://127.0.0.1:7897` 时仍返回 502，这是 Windows 代理路径问题，不是 panda 本机链路问题。

结论：
- 本轮严格验收暴露的 deepseek channel 69 短 non-stream 502 已从源头修复并部署。
- NewAPI -> ZenProxy -> free-model-client-rs 对两个 deepseek 模型的最小 OpenAI/Anthropic、stream/non-stream 链路已通过。
- Windows 客户端后续验收必须显式确认 no-proxy/Tailscale bypass，否则可能把本机代理 502 误判为 ZenProxy 失败。

## 2026-06-04 11:14

目标：
解释 NewAPI 日志中偶发 `status_code=500, upstream returned no assistant content or tool call` 的含义，并把问题记录为后续待办；同时检查 `deepseek-v4-flash` web search 能力边界。

已做：
- 对齐 panda NewAPI 日志和 `zen-proxy-rs@1/@2/@3` journal。
- 阅读 `free-model-client-rs` 的空上游错误、Anthropic stream/non-stream、ClaudeCode huge buffered stream 和输出 cap 代码。
- 统计近 4 小时 NewAPI `deepseek-v4-flash` 成功/失败样本。
- 搜索项目文档和源码中的 `web_search/web_fetch` 相关逻辑。
- 回写 `docs/04-improvement-backlog.md` 和 `docs/05-ai-maintenance-playbook.md`。

验证：
- 近 4 小时 `deepseek-v4-flash` 成功消费 133 条；空 assistant 错误事件 4 次，分别在 10:43、10:58、11:03、11:04。
- 失败样本主要是 ClaudeCode Anthropic `/v1/messages` 流式请求，`source_client=ClaudeCode`，大上下文加工具 schema。
- 失败样本的 `max_tokens=32000` 被 cap 到 768 或 1024；当前 ClaudeCode huge buffered retry 只覆盖 `max_tokens <= 512`，因此这些请求绕过 buffered retry。
- 代码确认：普通流式分支在上游结束后若 `text` 为空且 `tool_calls` 为空，会发送 `upstream returned no assistant content or tool call`。
- 后续非流式 fallback 有成功样本，说明不是 channel 69 整体不可用。
- 现有文档显示：OpenClaw `web_fetch` 小矩阵曾通过；Hermes web 用例命令成功但返回 `WEB_FAIL`，仍需拆分是未触发工具、工具执行失败、工具结果未被使用还是模型判断错误。

结论：
- 该 500 的根因是大 ClaudeCode 流式请求偶发上游空输出且未进入 buffered retry；NewAPI 只是记录了这个错误。
- `deepseek-v4-flash-free` 不能被视为“原生支持联网搜索”的模型；它最多通过 tools/function calling 请求外部搜索工具，真正联网动作必须由客户端/MCP/后端执行。
- 本仓库当前职责是协议规范化和工具调用转发，不自带搜索引擎；web search 用不了时必须按三段链路验收，而不是只问模型是否支持。

未完成：
- 扩大 ClaudeCode huge stream buffered retry 覆盖 768/1024 cap 桶。
- 给普通流式空输出补更细结构化日志。
- 为 ClaudeCode/Hermes/OpenClaw 分别做 web 工具定义、tool call、工具结果使用的最小验收。

下一步：
先修 ClaudeCode 大流式空 assistant 500；随后用 panda NewAPI 对三客户端 web 工具链路做脱敏采样和分段验收。

## 2026-06-04 12:24

目标：
执行 ClaudeCode 大流式空 assistant 500 修复，并把 ClaudeCode/Hermes/OpenClaw/web-search 的真实边界测清楚。

已做：
- 修复 `src/proxy/anthropic.rs`：移除 `handle_stream` 内部多余的 `max_tokens <= 512` 二次门槛，让外层判定的大 ClaudeCode 流式请求直接进入 buffered retry。
- 新增 kernel golden：`claude_code_huge_stream_uses_buffer_retry_after_1024_output_cap`，覆盖 `max_tokens=32000 -> 1024`、第一次上游空输出、第二次 retry 成功。
- 构建 `zen-proxy-rs` release，上传并部署到 panda 三实例。
- 用有效 panda NewAPI key/base URL 做 preflight、Windows ClaudeCode、Hermes、OpenClaw、直连 web tool 和 Windows ClaudeCode WebSearch 对照。
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy -D warnings`、`cargo test` 通过；库测试 71 条、kernel golden 76 条。
- `zen-proxy-rs`：主单测 129 条、e2e 26 条、release build 通过。
- 本地未 strip release hash：`bcedd35fa53e16eecbad76725aef6ae62aa625290ba129976229e0543017fded`。
- panda stripped hash：`7a8f4e5dc99e8ccf1aaf6562519d8353dc4ba5205e5e55f521c265b0760ed66e`；旧线上 hash `117b3cbfaf058fbbeb258f98542afc09a097e763359f34d174414b47dfd11aff` 已备份。
- panda health：`zen-proxy-rs@1/@2/@3` active；`/health` 返回 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`。
- NewAPI preflight：`/v1/models` 200，模型数 8；最小 `deepseek-v4-flash` chat 200，返回 `OK`。
- Windows ClaudeCode 显式 panda base/key：5/5 通过，P50 约 4.3s，P90 约 7.8s，tool 2/2。
- WSL Hermes：5/5 通过，P50 约 34.7s，P90 约 38.9s，tool 2/2。
- WSL OpenClaw：API 5/5 通，但 semantic 0/5，输出固定 `HEARTBEAT_OK`，stderr 为 local secrets gateway `1006 abnormal closure`。
- WSL ClaudeCode：当前 launcher 实际启动 `/root/.clawgod/cli.cjs`，不是有效 ClaudeCode；测试进程挂住后已清理。
- 直连 web tools：清空 WSL proxy env 后，Anthropic 和 OpenAI 两种协议均返回 `web_search` tool call。
- Windows ClaudeCode WebSearch：`--tools WebSearch,WebFetch` 下 `tools_seen=[]`，模型只输出普通文本伪 function call；`--tools default` 只有 `Bash/Edit/PowerShell/Read`。

结论：
- `upstream returned no assistant content or tool call` 的 768/1024 大流式绕过 retry bug 已从源头修复并部署。
- panda NewAPI -> ZenProxy -> free-model-client-rs 链路当前可用；Windows ClaudeCode 和 Hermes 能通过显式 panda base/key 工作。
- WSL ClaudeCode 不是有效客户端；OpenClaw 当前是本地 gateway/harness 问题；ClaudeCode WebSearch 不可用是客户端没有注册 WebSearch/WebFetch，不是 ZenProxy 不会转发工具调用。

未完成：
- 观察新 hash 后 NewAPI 是否还出现同类空 assistant 500。
- 修复 WSL ClaudeCode launcher 或换成真实 ClaudeCode CLI。
- 修复 OpenClaw local secrets gateway / agent harness 的 `HEARTBEAT_OK` 问题。
- 若要 ClaudeCode 真实 web search，需要接入可执行 WebSearch/WebFetch 的 MCP 或使用 Bash/PowerShell/curl 类工具。

下一步：
先处理客户端侧两个阻塞：WSL ClaudeCode 可执行文件和 OpenClaw gateway/harness；随后重跑四客户端 smoke，再进入 dry/full 压测。

## 2026-06-04 14:10

目标：
纠正 ClaudeCode WebSearch 误判，并修复 ZenProxy/free-model-client-rs 路径下工具名不匹配导致的 WebSearch/Task 不执行问题。

已做：
- 用户截图证明：Windows ClaudeCode 在官方 Claude 模型路径下可以真实执行 `WebSearch/WebFetch`。此前“ClaudeCode 没注册 WebSearch/WebFetch”的说法只适用于当时那次受控 ZenProxy 样本，不能作为 ClaudeCode 能力结论。
- 定位到 free-model-client-rs 出口问题：上游可能返回 `web_search/task` 等小写或下划线工具名，旧代码在 OpenAI/Anthropic stream/non-stream 出口原样返回；ClaudeCode 注册工具名是 `WebSearch/Task`，名称不一致时不会执行工具或 subagent。
- 在 `src/synthesis/tool.rs` 新增 tool call name canonicalization：按原始请求 `tools` 表用去符号、小写归一匹配，把 `web_search -> WebSearch`、`task -> Task` 回填成客户端注册名。
- 在 `src/proxy/openai.rs`、`src/proxy/anthropic.rs` 的 stream/non-stream/buffered stream 工具调用出口接入 canonicalization。
- 新增 kernel golden：
  - `claude_code_anthropic_stream_canonicalizes_web_tool_name`
  - `claude_code_anthropic_nonstream_canonicalizes_task_tool_name`
- 同步 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- 先跑新增测试，确认旧行为失败：返回 `web_search/task`，断言 `WebSearch/Task` 失败。
- 修复后新增测试通过。
- `free-model-client-rs`：
  - `cargo fmt -- --check` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。
  - `cargo test` 通过：库测试 71 条、kernel golden 78 条。
- `zen-proxy-rs` path 依赖重新编译后：
  - `cargo test` 通过：主单测 129 条、e2e 26 条。
  - `cargo build --release` 通过。

部署：
- 用户确认“部署”后，将 `/home/lenovo/zen-proxy-rs/target/release/zen-proxy-rs` 重新 strip、上传到 panda，并安装到 `/opt/zen-proxy-rs/zen-proxy-rs`。
- 远端 stripped hash：`0f6cdf6e5cd2dd1946a69707c97591cca865b47178ff63846f04bbdf283f2314`。
- 备份路径：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-tool-canon-20260604-142705-0f6cdf6`。
- `zen-proxy-rs@1/@2/@3` 重启后均为 `active`。
- `/health`：4000/4001/4002/4004 均返回 `status=ok`，dispatch 90、dead 0、ratelimited 0、upstream backoff false。
- `/metrics`：`zen_proxy_requests_total{status="5xx"} 0`，`active_concurrency 0`。

线上 smoke：
- 直连 panda ZenProxy `/v1/messages`，`x-fmc-client=claude-code`，工具表只注册 `WebSearch`：HTTP 200，`tool_use_names=WebSearch`，`stop_reason=tool_use`。
- 强制 `tool_choice=WebSearch` 的 smoke 返回上游 400：`Thinking mode does not support this tool_choice`，因此真实 smoke 改为 auto tool。该 400 是上游 DeepSeek 限制，不是部署失败。
- 直连 panda ZenProxy `/v1/messages`，工具表只注册 `Task`：HTTP 200，`tool_use_names=Task`，`stop_reason=tool_use`；期间上游空输出重试后成功。

结论：
- 这次不是 ClaudeCode 不能 WebSearch；是 ZenProxy/free-model-client-rs 对非 Claude 上游返回的工具名没有回填为 ClaudeCode 注册名。
- 官方 Claude 能跑，是因为它原生按 ClaudeCode 期望返回 `WebSearch/WebFetch`。DeepSeek/free 上游更容易返回 `web_search/task` 或伪调用，ZenProxy 必须做协议保真适配。
- 修复已部署到 panda，直连 ZenProxy 层的 WebSearch/Task 工具名 canonicalization 已验收通过。

未完成：
- 通过用户本机 Windows ClaudeCode -> NewAPI -> ZenProxy 的完整交互再跑一轮真实 WebSearch/WebFetch。
- 如果模型仍输出普通文本伪函数调用，而不是 tool call，下一步再做“伪函数调用文本 -> 受控 tool_use 修复”的风险评估；当前未实现该高风险转换。

## 2026-06-04 17:01

目标：
执行输出限制、缓存/截断、客户端适配分流的剩余源头改动；用 subagent 检查“为什么要输出限制、能不能不加限制”，并统一把结论落到源码和维护文档。

已做：
- 拉起两个 `gpt-5.5/xhigh` subagent：A 检查输出限制必要性，B 检查 ClaudeCode/Hermes/OpenClaw 识别与格式误伤边界。第三个 subagent 因线程 agent 数上限未创建，本地接手缓存/截断/websearch 归因。
- 修复 WSL rustup 半安装状态：`stable-x86_64-unknown-linux-gnu` 重新安装成功；后续验证使用 WSL 原生路径和 `/tmp/free-model-client-rs-target`。
- 新增 `ClientProfile::effective_for_model`：识别 profile 和有效策略 profile 分离，日志保留真实 `source_client`，同时记录 `effective_client`。
- 模型策略分流：`deepseek-v4-flash/deepseek-v4-flash-free` 不再应用 Hermes/OpenClaw 兼容策略，只保留 ClaudeCode 策略；`deepseek-v4-flash-lite/big-pickle` 不再应用 ClaudeCode 策略，只保留 Hermes/OpenClaw 策略。
- 调整流式输出策略：非流式 guard 保留；流式显式 `max_tokens` 不再因输入达到 50k/100k/200k 被硬降到 1024/768/512。
- 收窄 ClaudeCode Anthropic buffered stream：`max_tokens <= 512`、50k+ 输入且 `max_tokens <= 2048`、exact-output fallback 仍可 retry；20k/32k 长输出直接流式返回，避免完整缓冲后一次性回放。
- 增加 `reply PONG only` 短 smoke 兜底，只在无工具、`max_tokens <= 64` 且上游连续空输出时生效。
- 回写 `docs/02-current-state.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：
- 定向测试通过：
  - `stream_output_policy_preserves_explicit_max_tokens_by_prompt_size`
  - `deepseek_flash_*`
  - `deepseek_flash_lite_*`
- `kernel_golden` 通过：90/90。
- 最终完整验证通过：
  - `cargo fmt -- --check`
  - `cargo clippy --all-targets -- -D warnings`
  - `cargo test`：库测试 77 条、kernel golden 90 条、doc tests 0 条。

结论：
- 该轮临时结论是保留非流式 guard、流式尊重显式大输出；17:55 后已被最新决策覆盖，当前维护文档以“输出限制完全取消、真实 panda policy 验收待跑”为准。
- ClaudeCode 格式问题的主要风险仍是 profile 误判或模型策略误套；本轮已把真实识别和有效策略拆开，避免 Hermes/OpenClaw 适配继续污染 flash 的 ClaudeCode 使用。

未完成：
- 尚未把本轮 free-model-client-rs 改动构建进 `zen-proxy-rs` release 并部署到 panda。
- 尚未用用户侧 Windows ClaudeCode -> NewAPI -> ZenProxy 完整链路复验长输出、WebSearch/WebFetch、cache 展示和 `finish_reason`。
- OpenClaw local gateway/harness `HEARTBEAT_OK` 阻塞仍未解决。

下一步：
如需要上线，先在 `zen-proxy-rs` path dependency 重新 build release，部署 panda 三实例；随后跑最小 smoke：models、OpenAI/Anthropic PONG、ClaudeCode 长流式 20k/32k、WebSearch/Task 工具名、NewAPI cache/finish_reason 展示。

## 2026-06-04 18:01

目标：
按最新要求彻底取消输出限制；flash/free 只保留 ClaudeCode 深度适配并取消输入墙；lite/big-pickle 只保留 Hermes/OpenClaw 适配；补 cache 四态和 provider usage 信号；补 policy harness；处理 ZenProxy 外层 compactor 是否仍压 flash 输入。

已做：
- 新建分支 `codex/v47-client-split-cache-harness`。
- `free-model-client-rs`：输出策略改为 `Option<u64>` 原样透传；缺省 `max_tokens` 不再补 1024/2048；显式 `max_tokens` 不再按输入桶 cap。
- `free-model-client-rs`：`deepseek-v4-flash/deepseek-v4-flash-free` 取消 Hermes/OpenClaw effective policy，只保留 ClaudeCode；`deepseek-v4-flash-lite/big-pickle` 取消 ClaudeCode effective policy，只保留 Hermes/OpenClaw。
- `free-model-client-rs`：flash/free 输入只做 request-shape 观测，不再走内核 compactor。
- `free-model-client-rs`：新增 cache 观测四态 `attempted/accepted/rejected/ignored`，并记录 provider response/header/body usage 信号。
- `scripts/panda_pressure_runner.py`：新增 `policy-smoke` / `policy-dry`，覆盖 input/output wall、provider header/body usage、cache 四态和 lite effective profile。
- `zen-proxy-rs`：取消 non-stream output guard 的改写/拒绝；外层 V4 context compactor 对 flash/free 改为只 warn/pass，不 compact、不 token-target reject；lite 仍保留 compactor。
- `zen-proxy-rs` e2e：新增 flash 大输入不压缩测试，保留 lite 大输入会 compact 的测试；mock upstream body limit 调整为 8MB，避免测试 harness 自己误报 413。
- 维护文档同步：current state、roadmap、backlog、playbook、panda 压测方案、客户端策略方案已写入最新边界和剩余验收。

验证：
- `free-model-client-rs` 已通过：`python3 -B -m unittest discover -s tests -p test_panda_pressure_runner.py`，4/4。
- `zen-proxy-rs` 已通过：`cargo test -q v4::context::tests`，12/12；`cargo test --test e2e_integration -- --test-threads=1 --nocapture`，27/27。
- subagent Rawls 独立确认外层风险真实存在，并复跑 flash pass-through、lite compactor、nonstream guard 相关测试通过。

结论：
- 本轮源码层已经取消输出墙；flash/free 的输入墙已从 `free-model-client-rs` 和 `zen-proxy-rs` 外层同时取消。
- 这不是全局关闭 compactor：lite/big-pickle 仍保留大上下文压缩保护。
- 真实 panda `policy-smoke/policy-dry` 尚未执行，不能写成生产已验证；输出限制取消后的上游 413、超时、空输出、延迟和成本风险必须靠 panda policy/dry 数据确认。

未完成：
- 还未部署本轮 v47 到 panda。
- 还未跑真实 panda `policy-smoke/policy-dry`。
- 还未跑四客户端 dry/full 压测。
- OpenClaw local gateway/harness `HEARTBEAT_OK` 和 WSL ClaudeCode launcher 问题仍是后续正式压测阻塞。

下一步：
先完成本地全量验证、git diff 审查和提交；如用户批准部署，再构建 `zen-proxy-rs` release 部署 panda，并先跑 `policy-smoke/policy-dry`，通过后再进入四客户端 dry/full。

## 2026-06-04 18:58

目标：
排查用户反馈的 ClaudeCode 反复声称“上下文被压缩/我看不到上一轮指令”的问题；确认 v47 是否已解决，若未部署则部署到 panda 并做最小验证。

已做：
- 通过用户截图定位到模型回复中直接引用了 `[free-model-client-rs context compactor: folded stale ClaudeCode tool/session history]`，确认这不是用户手动 compact，而是旧版 `free-model-client-rs` 自动 ClaudeCode compactor 注入的折叠提示。
- 本地代码确认 v47 已在 `deepseek-v4-flash/deepseek-v4-flash-free` 路径调用 `model_disables_input_compaction`，只做 `observe_context`，不再调用 `compact_claude_code_huge_session_context`；ZenProxy 外层也对 flash/free 只 warn/pass。
- 远端部署前日志确认旧线上仍在压缩和 cap：18:19-18:21 仍有 `compacted streaming anthropic context before upstream` 与 `capped streaming anthropic max_tokens ... effective_max_tokens=512`，模型为 `deepseek-v4-flash-free`。
- 构建 `/home/lenovo/zen-proxy-rs` release，依赖本地 `free-model-client-rs` v47 commit `619f2f0` 和 `zen-proxy-rs` commit `21feba2`。
- Windows `scp` 大文件上传在 8-12MB 处卡住，改为本地 strip 后 512KB base64 分片上传；远端文件 hash 校验通过。
- 部署 panda `/opt/zen-proxy-rs/zen-proxy-rs`，旧 hash `694036f6a130e8211b998a5b58eff36105fb48fb866ec57ebbb2c03ccfb5f3d7`，新 stripped hash `99424602ce7c076671579abf48ca0d27367ac126e514efe4403d902d5caecd78`，备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-v47-20260604-185423-694036f`。
- 滚动重启 `zen-proxy-rs@1/@2/@3`，新 pid 为 `1093754/1093766/1093777`。

验证：
- 三实例 `/health` 均返回 `status=ok`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 短请求返回 `V47_SHORT_OK`，HTTP 200，约 3.9s。
- OpenAI 非流式 `deepseek-v4-flash` 大上下文请求：361 条消息、请求体约 560KB、`max_tokens=32000`，返回 `V47_NO_COMPACTOR_OK`，HTTP 200，约 7.0s，NewAPI usage 约 `prompt_tokens=97836`。
- 部署后日志 grep `compacted .*context|capped .*max_tokens|context compactor|effective_max_tokens` 无命中。
- 大请求日志显示 `context_action=pass`、`effective_body_size=560709`、`max_tokens=Some(32000)`，证明 flash/free 线上不再被 ZenProxy 外层或 free-model-client-rs 内核折叠，也不再 cap 输出。

结论：
- 用户看到的“被压缩过了/不记得上一轮具体内容”是旧线上自动 compactor 的真实副作用，不是用户手动触发 compact。
- v47 已部署并通过手工 smoke 证明 flash/free 这条链路不再注入 `context compactor` 折叠块，`max_tokens=32000` 已原样透传。
- cache 本轮大请求记录为 `cache_observation="rejected"` 且 `provider_body_cached_tokens=Some(0)`，说明 provider 返回 usage/cache 信号但未命中缓存。

未完成：
- 真实 panda `policy-smoke/policy-dry` 仍未跑。
- 四客户端 dry/full 压测仍未跑。
- 用户本机 Windows ClaudeCode 真实长会话还需要继续观察是否还有模型自身短视或上游空输出问题；若日志不再出现 compactor/cap，则下一步应看上游质量、cache、工具调用和 ClaudeCode 客户端状态。

## 2026-06-05 00:58

目标：
落地 V4.98 cache-friendly session，尽量提高长会话 provider cache 命中机会，同时不降低输入大小、不裁剪上下文、不影响 ClaudeCode 输出质量。

已做：
- 复核 panda 真实数据：最近约 70 分钟 `deepseek-v4-flash` `/v1/messages` 77/77 流式，prompt tokens P50 约 326k、P90 约 331k，cache hits 0，`frt` P50 约 3.8s、P90 约 5.1s，总耗时 P50 约 14s、P90 约 39s。
- 确认 330k 不是输入 token 墙：ZenProxy 线上日志为 `context_action=pass`，`effective_body_size=body_size`；free-model-client-rs `messages_tokens` 连续增长到 331k+。
- 确认 cache 不是完全不可用：8 小时窗口内存在 `prompt_tokens=286975/cache_tokens=462592`、`prompt_tokens=439847/cache_tokens=439808` 等命中。
- 定位 cache 命中差异：完全相同 `prompt_hash` 的重试样本能命中；长会话尾部持续增长的样本不命中。
- 写入失败证明：旧逻辑下，同一 1.2MB 前缀只追加尾部对话会导致 `x-opencode-session` 变化。
- 修复 `src/zen/client.rs`：大请求 session scope 改为 `large_prefix_v498`，使用稳定前缀 hash + tools hash + tool_choice hash；保留模型、api key hash、时间桶隔离。
- 扩展 `RequestShape` 和日志：新增 `prefix_4k_hash/prefix_32k_hash/prefix_128k_hash/prefix_256k_hash/cache_material_bytes`，同时出现在 request-shape 和 cache observation 日志。
- 新增回归：大前缀稳定只追加尾部时，session 与 prefix hash 保持稳定；前缀变化时 prefix hash 变化。
- 同步 `docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

当前验证：
- `cargo test -q opencode_session` 通过：4/4。
- `cargo test -q request_shape_prefix_hashes_stay_stable_when_large_tail_grows` 通过：1/1。
- `cargo fmt -- --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- `cargo test` 通过：库测试 83 条、kernel golden 95 条、doc tests 0 条。
- `zen-proxy-rs` 集成验证通过：`cargo test -q v4::context::tests` 12/12，`cargo build --release` 成功，确认 V4.98 path dependency 能编进最终二进制。

结论：
- V4.98 是 cache/session 友好优化，不是上下文裁剪策略。
- 该改动不修改请求正文、消息顺序、`max_tokens` 或用户提示词，因此理论上不应降低回答质量。
- 真实 cache 命中提升需要部署 panda 后用同一 ClaudeCode 长会话 A/B 观察，不能只凭源码测试下生产结论。

未完成：
- 尚未部署 panda。
- 尚未做部署前后 cache hit rate、`frt`、总耗时、工具错误和回答质量对比。
