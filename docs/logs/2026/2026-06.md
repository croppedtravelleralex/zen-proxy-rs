# 2026-06 工作记录

## 2026-06-01 17:20

目标：
继续处理 panda NewAPI 下 ClaudeCode huge_context 空跑、300s timeout、context_drift 问题，只从 ZenProxy/free-model-client 源头修复。

已做：
- 构建并部署包含 latest-user final-anchor 的 `zen-proxy-rs` release 到 panda 三实例。
- 线上二进制从 `94872cd91a558ec431e176af5a1fa8f257219c9518df3f729e7e5645b5cbb937` 更新为 `3a27f2c7cda56119b32dfc42738b06f3f1e08155a0ff89c48daca6ddc8aed1d4`。
- 保留备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-final-anchor-20260601-94872cd`。
- 在 panda 本机通过 NewAPI `/v1/messages` 做约 1.0MB huge stream source-side smoke。

验证：
- 三实例 `/health` 均 200，池 `total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 返回 `OK`。
- huge stream `deepseek-v4-flash` 3/3 返回 `HUGE_OK`。
- huge stream `deepseek-v4-flash-lite` 3/3 返回 `HUGE_OK`。
- ZenProxy 日志确认 `source_client=claude-code`、`compacted_messages=1`、`appended_latest_user_anchor=true`。

结论：
source-side final-anchor 已在 panda 生产链路生效，旧的 “git diff / inspect current state” 语义漂移未在本次 panda 本机 huge smoke 中复现。

未完成：
- 这不是四客户端真实 dry run，不能替代 Windows ClaudeCode、WSL ClaudeCode、WSL Hermes、WSL OpenClaw 验收。
- 日志仍可见上游偶发空输出，当前由 buffered retry 兜底，正式 dry run 需要统计重试率和尾延迟。

下一步：
重新跑 panda-only dry run，并重点观察 ClaudeCode huge_context、lite huge_context、OpenClaw subagent、Hermes 慢路径和上游 502/524/stream decode 分类。

## 2026-06-01 19:45

目标：
执行 panda-only 四客户端 dry run，验证 final-anchor 部署后的真实客户端表现。

已做：
- WSL ClaudeCode dry 50。
- WSL Hermes dry 50。
- WSL OpenClaw dry 50。
- Windows ClaudeCode dry 50，使用 Windows 原生 Python/PowerShell 启动 runner，不再把 WSL interop 结果计入正式 dry run。
- 追加脱敏报告 `docs/reports/panda-dry-run-20260601.md`。

验证：
- 总轮次 200，API OK 200/200，semantic OK 186/200。
- Windows ClaudeCode：50/50 API ok，43/50 semantic ok；6 个 huge_context `context_drift`，1 个 `subagent_not_triggered`。
- WSL ClaudeCode：50/50 API ok，44/50 semantic ok；6 个 huge_context 全部 `context_drift`。
- WSL Hermes：50/50 API ok，50/50 semantic ok；P50/P90/P99 total 约 54.3s/69.5s/103.5s。
- WSL OpenClaw：50/50 API ok，49/50 semantic ok；1 个 lite long_context `context_drift`，subagent 5/5 observed。
- panda 三实例 dry run 后健康：`total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`。

结论：
链路层稳定性明显好于之前：没有认证/模型/API 层失败，没有 300s timeout，runner summary 未观察到 502/504/协议 400。full run 仍不能启动，阻塞点已从链路可用性转为 ClaudeCode huge_context 语义漂移、Hermes 慢路径和 Windows runner 路径问题。

未完成：
- ClaudeCode 真实客户端 huge_context 仍会读取 transcript、git 状态或继续旧任务，source-side final-anchor 还不够。
- Windows ClaudeCode runner 使用 `\\wsl.localhost` UNC 工作目录，CMD fallback 到 Windows 目录，影响 subagent 结果。
- Hermes 功能通过但延迟不达标。

下一步：
先修 ClaudeCode huge_context 的真实客户端 prompt/compactor 策略和 Windows runner 工作目录，再复跑 dry run；不要启动 4 x 500 full run。

## 2026-06-03 11:40

目标：
解决 panda NewAPI channel 69 测试报 `502 upstream returned no assistant content or tool call` 的问题，只从 ZenProxy/free-model-client 源头修复。

已做：
- 在 `free-model-client-rs` 修复空内容无工具健康探测：OpenAI/Anthropic 小探测短路返回本地 `ok`。
- 修正 ClaudeCode huge buffered stream 触发条件：只在修复前估算输入 >= 50k tokens 时启用，不再因 `max_tokens <= 512` 误伤小请求。
- 补充 kernel golden：OpenAI/Anthropic 空 stream probe 短路、ClaudeCode 小低 `max_tokens` 流式请求不进入 huge buffer retry。
- 构建 `zen-proxy-rs` release 并部署到 panda 三实例，保留线上备份。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy -D warnings`、`cargo test` 通过；库测试 61 条、kernel golden 62 条。
- panda 线上二进制 hash 更新为 `28b928472d2abc7be036cdc2796915865bd4a5b083ee2dd1dab46b1ddd0e2633`。
- panda 三个 ZenProxy systemd 实例为 `active`。
- 2026-06-03 11:26 NewAPI 日志出现 channel 69 成功消费记录：`/v1/messages`、`stream_status=ok`、`frt=76ms`。
- 有效 `vip` 组 token 下，panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash` 和 `deepseek-v4-flash-lite`。
- 有效 `vip` 组 token 下，OpenAI 非流式 `deepseek-v4-flash` 200，返回 `CHANNEL69_OPENAI_OK`。
- 有效 `vip` 组 token 下，Anthropic 流式 `deepseek-v4-flash` 200，返回 `CHANNEL69_ANTHROPIC_OK`。

结论：
channel 69 本身可用，当前核心问题已修复。此前继续报错的主要误区是把已删除/错误分组 token 当作验收 key，或把 `token_name=ds` 当作明文 API key。

未完成：
- 仍需重新跑 panda-only smoke/dry，验证四客户端真实链路是否还存在 huge_context 语义漂移、Hermes 慢路径、lite 长上下文漂移。
- `.codex_tmp/`、`configured`、`panda`、异常字符文件等历史临时项仍不应提交。

下一步：
只用有效 `vip` 组 token 进入 panda-only smoke/dry；dry run 通过前不启动 4 客户端 x 500 full run。

## 2026-06-03 12:18

目标：
继续修复 NewAPI 管理端“测渠道”仍偶发 `upstream returned no assistant content or tool call`，但普通调用可用的问题。

已做：
- 抓取 panda NewAPI 日志，确认管理端测渠道请求为 `/api/channel/test/69?model=deepseek-v4-flash&stream=true`，内部生成 OpenAI 流式小探测，转到 ZenProxy 后走 `/v1/messages`，`body_size` 约 112-114。
- 确认失败为偶发：11:46:40 一次测渠道失败，11:46:54 和 11:47:44 同接口又成功；普通调用后续多次成功。
- 在 `free-model-client-rs` 增加第二层 fallback：无工具、流式、单用户消息、`max_tokens <= 64` 的 `hi/hello/test/echo hi` 类测渠道探测，如果上游空输出，则返回本地 `ok`，不裸透空输出错误。
- 补充 OpenAI/Anthropic 两条 kernel golden，模拟 `echo hi` 上游空流。
- 构建并部署新的 `zen-proxy-rs` 到 panda，备份旧二进制。

验证：
- `free-model-client-rs`：`fmt --check`、`clippy -D warnings`、`cargo test` 通过；库测试 61 条，kernel golden 64 条。
- panda 线上二进制从 `28b928472d2abc7be036cdc2796915865bd4a5b083ee2dd1dab46b1ddd0e2633` 更新为 `942e69091ef424d92a3e2f4d30d54ea708e46e88cbe782ede789f7b6d77849e1`。
- 备份：`/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-channel-probe-fallback-20260603-120851-28b9284`。
- panda 4000/4001/4002/4004 `/health` 均 `status=ok`，池约 `total=90/91`、`dead=0`、`ratelimited=0`。
- panda NewAPI 有效 `vip` 组 token 下，`echo hi`、`max_tokens=16`、stream 探测 10/10 HTTP 200，`empty_error=0`。
- ZenProxy 日志确认这 10 轮上游实际为空流，但已被 `short channel-test probe received empty upstream; returning local ok` 兜底。

结论：
普通调用原本可用；管理端测渠道的剩余失败点是 NewAPI 自带极短探测碰到上游空流。当前已从源头兜住该请求形态。

未完成：
- 我不能用 API token 直接调用 NewAPI 管理端 `/api/channel/test/69`，该接口要求网页登录态；最终 UI 按钮结果仍需用户点击一次确认。

下一步：
用户在 NewAPI 管理端再点 channel 69 测试后，继续看 5 分钟 NewAPI/ZenProxy 日志，确认不再出现该错误。
