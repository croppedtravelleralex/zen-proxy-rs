# 2026-06 工作记录

## 2026-06-01 17:20

目标：
继续处理 panda NewAPI 下 ClaudeCode huge_context 空跑、300s timeout、context_drift 问题，只从 ZenProxy/free-model-client 源头修复。

已做：
- 构建并部署包含 latest-user final-anchor 的 `zen-proxy-rs` release 到 panda 三实例。
- 线上二进制从 `94872cd91a558ec431e176af5a1fa8f257219c9518df3f729e7e5645b5cbb937` 更新为 `3a27f2c7cda56119b32dfc42738b06f3f1e08155a0ff89c48daca6ddc8aed1d4`。
- 保留备份 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-final-anchor-20260601-94872cd`。
- 在 panda 本机通过 NewAPI `/v1/messages` 做约 1.0MB huge stream source-side smoke。

验证：
- 三实例 `/health` 均 200，池 `total=90`、`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- panda NewAPI `/v1/models` 200，包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- OpenAI 非流式 `deepseek-v4-flash` 返回 `OK`。
- huge stream `deepseek-v4-flash` 3/3 返回 `HUGE_OK`。
- huge stream `deepseek-v4-flash-lite` 3/3 返回 `HUGE_OK`。
- ZenProxy 日志确认 `source_client=claude-code`、`compacted_messages=1`、`appended_latest_user_anchor=true`。

结论：
source-side final-anchor 已在 panda 生产链路生效，旧的 “git diff / inspect current state” 语义漂移未在本次 panda 本机 huge smoke 中复现。

未完成：
- 这不是四客户端真实 dry run，不能替代 Windows ClaudeCode、WSL ClaudeCode、WSL Hermes、WSL OpenClaw 验收。
- 日志仍可见上游偶发空输出，当前由 buffered retry 兜底，正式 dry run 需要统计重试率和尾延迟。

下一步：
重新跑 panda-only dry run，并重点观察 ClaudeCode huge_context、lite huge_context、OpenClaw subagent、Hermes 慢路径和上游 502/524/stream decode 分类。
