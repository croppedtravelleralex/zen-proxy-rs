# 2026-05 工作日志

## 2026-05-30

目标：继续执行当前待办，先把 `free-model-client-rs` 维护入口补齐，再推进 panda NewAPI / Hermes / OpenClaw 真实链路验证。

已做：

- 确认当前分支为 `codex/v46-quality-nonstream-gates`。
- 确认已有代码改动覆盖：`x-api-key` 认证、64MB 请求体上限配置、非流式输出 cap、短指令保留、工具历史修复、SSE 错误分类。
- 修复 `src/zen/client.rs` 的 clippy `large_enum_variant`：`ParsedZenFrame::Event` 改为装箱，流式输出时解箱。
- 补齐维护文档主档：项目章程、当前状态、roadmap、backlog、AI 维护手册。

验证：

```bash
cd /home/lenovo/free-model-client-rs
CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo fmt -- --check
CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo clippy --all-targets -- -D warnings
CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/free-model-client-rs-target cargo test
```

结果：`fmt`、`clippy`、`cargo test` 全通过。测试数量：库测试 56 条，集成测试 39 条。

结论：代码层 V4.6 质量和非流式保护改动当前可编译、可测试；运行层在当日尚未完成，后续 2026-05-31 已完成 panda NewAPI / Hermes / OpenClaw 小矩阵，详见下文。

未完成：

- panda NewAPI OpenAI/Anthropic 最小请求。
- Hermes 临时接入 panda NewAPI 测试。
- OpenClaw 隔离 Node 和 panda NewAPI 测试。
- 根目录 README 与当前能力同步。

下一步：从 panda NewAPI `/v1/models`、OpenAI `/v1/chat/completions`、Anthropic `/v1/messages` 最小请求开始，记录脱敏结果。

## 2026-05-31

目标：整理当前 goal 的最终目标、剩余待办和 panda 小矩阵实测结果，更新维护文档，便于下一轮直接进入压测准备。

已做：

- 更新 `docs/README.md`：补充当前最终目标、完成标准、P0 已完成项和剩余待办。
- 更新 `docs/02-current-state.md`：记录 panda NewAPI、WSL Hermes、WSL OpenClaw 小矩阵结果。
- 更新 `docs/03-roadmap.md`：把 panda/Hermes/OpenClaw 小矩阵移动到已完成阶段，把当前主线改为文档/README 同步、临时产物归类和压测入口整理。
- 更新 `docs/04-improvement-backlog.md`：将 P0 状态改为已完成小矩阵，并补充压测矩阵、README 同步、临时产物归类。
- 更新 `docs/05-ai-maintenance-playbook.md`：补充 Hermes/OpenClaw 临时接入方式和 panda-only 四客户端压测目标。
- 更新根目录 README：补充 `FREE_MODEL_REQUEST_BODY_LIMIT_MB`、`ZEN_UPSTREAM_SESSION_TTL_SECS`、非流式输出保护、空上游错误行为和当前测试数量。

验证：

- 当前文档依据来自实际命令结果：panda `/v1/models`、OpenAI `/v1/chat/completions`、Anthropic `/v1/messages` 均返回 200。
- Hermes 临时 panda 链路：短回复和文件/终端工具通过；web 用例命令成功但返回 `WEB_FAIL`。
- OpenClaw 临时 panda 链路：隔离 Node 22.21.1、config validate、models list、infer、agent 文件工具、agent web_fetch 均通过。

结论：

- P0 小矩阵已经完成；剩余主线转为 README 同步、临时产物归类、git 状态清理和 panda-only 四客户端各 500 次压测准备。

未完成：

- `.codex_tmp/`、`configured`、`panda`、异常字符文件等未跟踪项仍需归类。
- panda-only 四客户端 500 次压测脚本、报告模板和验收标准仍需落地。

下一步：

- 整理压测脚本入口和临时产物归类；提交前确认不包含密钥、临时输出和大日志。

## 2026-05-31 收尾更新

目标：把剩余待办和最终目标固化成下一轮可直接执行的压测入口。

已做：

- 新增 `docs/06-panda-pressure-test-plan.md`：明确 panda-only 四客户端各 500 次混合压力测试、阶段安排、采集字段、错误分类、通过门槛和报告模板。
- 更新 `docs/README.md`：把压测方案加入维护文档阅读顺序。
- 更新 `docs/02-current-state.md`：补充 `.codex_tmp/`、Hermes/OpenClaw 临时目录、根目录 0 字节未跟踪文件的归类和提交原则。
- 更新 `docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`：把“方案/报告模板”标记为已落地，把“无密钥执行器和真实压测”保留为下一步。
- 更新根目录 README：key 默认值不再直接展示测试 key 字符串，避免文档误泄漏。

验证：

- 仍需在本次文档修改后执行一致性搜索、`git diff --check` 和 Rust 验证命令。

结论：

- 当前剩余目标已经收敛为：落地无密钥 panda 压测执行器，跑 smoke/dry run/full run，生成脱敏报告，再按结果回写维护文档。

未完成：

- 真实 4 客户端 x 500 压测尚未执行。
- `.codex_tmp/client-matrix/run_matrix.py` 尚未提炼成可提交的无密钥执行器。
- 根目录 0 字节未跟踪文件来源未确认，保持不删除、不提交。

下一步：

- 执行文档一致性和格式校验；之后进入无密钥执行器整理。

## 2026-05-31 客户端策略隔离方案

目标：针对 Hermes/OpenClaw 适配后 ClaudeCode 体感变差的问题，先落地文档方案，后续先做 90 分实现，拿数据后再做 99+。

已做：

- 新增 `docs/07-client-profile-policy-plan.md`。
- 明确根因风险：tools 请求默认禁用 thinking、流式空白 delta 过滤、工具历史修复过宽，都会通过共享路径影响 ClaudeCode。
- 明确 90 分方案：新增 `x-fmc-client` 识别头和 `ClientProfile`，按 ClaudeCode / Hermes / OpenClaw / unknown 拆 thinking、whitespace、tool-history policy。
- 明确 99+ 后续：基于真实数据做动态 profile、per-client 指标、灰度和回滚。
- 同步 `docs/README.md`、`docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`。

验证：

- 本次只落地文档，尚未改 Rust 代码。

结论：

- 下一步代码主线应先实现客户端识别和策略隔离，再进入四客户端压测；否则压测数据会混入 ClaudeCode 被兼容策略误伤的噪声。

未完成：

- `ClientProfile`、`x-fmc-client`、per-client policy、profile 维度测试尚未实现。

下一步：

- 先做 90 分实现，跑 ClaudeCode/Hermes/OpenClaw 小矩阵拿数据，再决定 99+ 动态 profile 范围。

## 2026-05-31 客户端策略隔离 90 分实现

目标：实现 `docs/07-client-profile-policy-plan.md` 的 90 分版本，先解除 ClaudeCode 被 Hermes/OpenClaw 兼容策略误伤的问题。

已做：

- 新增 `src/client_profile.rs`：定义 `ClientKind`、`ClientProfileSource`、`ClientProfile`。
- 支持显式识别头 `x-fmc-client`，并支持 UA/body 工具名兜底识别。
- OpenAI/Anthropic chat 路径已传入 profile。
- ClaudeCode 策略：tools 请求不默认禁用 thinking；流式空白 delta 保留；工具历史走 strict/minimal 修复。
- Hermes/OpenClaw 策略：保留 compat 工具历史和工具请求禁用 thinking 的兼容路径。
- unknown 策略：不禁用 thinking，只做最小协议修复。
- 根 README 补充 `x-fmc-client` 行为说明。

验证：

- 新增失败证明后转绿：`claude_code_tools_do_not_disable_thinking`。
- `cargo test --test kernel_golden` 通过：43 条。

结论：

- 90 分代码实现已完成到内核回归层；还需要真实 panda 小矩阵验证 ClaudeCode 体感和 Hermes/OpenClaw 兼容性。

未完成：

- 未跑真实 ClaudeCode/Hermes/OpenClaw 小矩阵。
- 未做 99+ 动态 profile、per-client metrics、灰度和回滚。

下一步：

- 跑 ClaudeCode/Hermes/OpenClaw 小矩阵，确认输出格式、Task/subagent、工具调用和 web_fetch 是否恢复，再进入 99+。

## 2026-05-31 panda 部署客户端策略隔离实现

目标：把当前 `free-model-client-rs` 的 90 分 `ClientProfile` / `x-fmc-client` 策略隔离实现部署到 panda 生产链路。

已做：

- 确认 panda 生产实际运行的是 `zen-proxy-rs`，且依赖本机源码路径 `../free-model-client-rs`，因此部署目标是重建后的 `zen-proxy-rs` 二进制，不是单独启动 `free-model-client-rs`。
- 在 `/home/lenovo/zen-proxy-rs` 执行 release 构建，确认依赖 `free-model-client-rs = { path = "../free-model-client-rs" }`。
- 构建得到本地 release hash：`65e1ee22dd1f392b566e2af8664308cea044599294f93d228da1082f9b1639f9`。
- 因 SSH 上传链路抖动，改为 `strip` 后 gzip 上传；实际部署二进制 hash：`35feea8bba79412e0b323a494ebd5450e8a21bfd9769395091eb8c6c0d87165a`。
- 替换前备份旧二进制到 `/opt/zen-proxy-rs/backups/zen-proxy-rs.pre-client-profile-20260531-131038`。
- 滚动重启 `zen-proxy-rs@1`、`zen-proxy-rs@2`、`zen-proxy-rs@3`。

验证：

- 三实例均 active，监听 `127.0.0.1:4001`、`127.0.0.1:4002`、`127.0.0.1:4004`。
- 三实例 `/health` 均返回 200，`status=ok`，池 `total=90`、`dispatch=90`。
- 部署后 10 分钟窗口内三实例 `journalctl -p warning` 无记录。
- panda 本机 NewAPI `/v1/models` 返回 200，模型列表包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`。
- panda 本机 NewAPI `/v1/chat/completions` 使用 `deepseek-v4-flash` 非流式小请求返回 200，内容为 `OK`，耗时约 2.37s。

结论：

- 当前 90 分客户端策略隔离实现已经部署到 panda 的 `NewAPI 8081 -> zen-proxy-rs 4001/4002/4004 -> free-model-client-rs kernel -> upstream` 链路。
- 部署后基础模型发现和最小聊天调用通过；还不能等同于 ClaudeCode/Hermes/OpenClaw 全矩阵通过。

未完成：

- 需要在真实 panda 链路上补跑 ClaudeCode/Hermes/OpenClaw 小矩阵，确认 `x-fmc-client` 透传或自动识别是否按预期生效。
- 需要继续落地无密钥压测执行器，并跑 4 客户端各 500 次混合压力测试。

## 2026-05-31 panda 压测执行器与 dry-run 闸口

目标：按推荐路径落地正式 panda-only 压测执行器，先跑 preflight/smoke/dry run，再判断是否能进入 4 客户端 x 500 full run。

已做：

- 新增并完善 `scripts/panda_pressure_runner.py`，作为长期复用的无密钥压测入口。
- runner 只从环境变量读取 key，默认 panda NewAPI `http://100.69.228.93:8081`，默认拒绝 localhost。
- 支持 Windows ClaudeCode、WSL ClaudeCode、WSL Hermes、WSL OpenClaw。
- Hermes/OpenClaw 大 prompt 改为文件背书，避免 Linux `Argument list too long`。
- 补强错误分类：进程返回 0 但内容是 timeout/API error 的情况不再记为 `ok`；semantic false 拆为 `context_drift`、`tool_runtime_error`、`subagent_not_triggered` 等。
- 拉取 panda 侧健康和日志：三实例 active，监听 `4001/4002/4004`，NewAPI Docker 监听 `8081`。

验证：

- `python3 -m py_compile scripts/panda_pressure_runner.py` 通过。
- Windows Python `panda_pressure_runner.py --help` 通过。
- panda preflight 通过：`/v1/models` 200，包含 `deepseek-v4-flash`、`deepseek-v4-flash-lite`；最小聊天 200，返回 `OK`。
- WSL ClaudeCode dry 50：49/50 API ok，47/50 semantic ok；失败集中在 `deepseek-v4-flash-lite` huge_context 和一次 `503 system cpu overloaded`。
- WSL Hermes dry 50：50/50 API ok，50/50 semantic ok；P90 总耗时约 47s，subagent 当前不支持观测。
- WSL OpenClaw dry 50：50/50 API ok，48/50 semantic ok；subagent 出现一次 timeout 和一次 direct-read fallback。
- Windows ClaudeCode partial dry 22：21/22 API ok，21/22 semantic ok；huge_context 出现 310s 级客户端非零退出和一次 `context_drift`，因此主动中止。
- panda ZenProxy `/metrics` 显示三实例池仍为 `dispatch=90`、`dead=0`、`ratelimited=0`；NewAPI/docker 日志中另有上游 Cloudflare 502/524、stream JSON 截断、client_gone，需要和 ZenProxy 归因分开。

结论：

- 正式 runner 已可用，但 dry-run 闸口未通过。
- 当前不能直接启动 4 客户端 x 500 full run；必须先处理 huge_context lane、OpenClaw subagent 稳定性、`deepseek-v4-flash-lite` 长上下文语义漂移和上游 overload/502/524 归因。

未完成：

- 未执行 full run。
- 未把 huge_context 从 full run 中降级或隔离。
- 未完成 OpenClaw subagent 单独复现和 Hermes subagent 支持状态最终判定。

下一步：

- 先按 `docs/06-panda-pressure-test-plan.md` 的 dry-run 红旗列表修复或降级，再重新跑四客户端 dry run。

## 2026-05-31 OpenClaw profile 误识别修复

目标：修复 OpenClaw 请求被 ClaudeCode 共用工具名误识别的问题，解决 subagent 长时间 timeout，并避免 Hermes/OpenClaw 兼容策略继续误伤 ClaudeCode。

已做：

- `free-model-client-rs` 增强 `src/client_profile.rs`：OpenClaw body marker 和专属工具集优先于 `read/write/edit` 等共用工具名。
- `zen-proxy-rs` 增强 `src/v4/provider.rs`：显式头优先，其次 body/toolset 推断，再看 SDK/UA；向 free-model kernel 传递 `ClientProfile`。
- 修复 `zen-proxy-rs` e2e 启动旧二进制和 clippy `items_after_test_module` 问题。
- 构建、strip 并部署到 panda `/opt/zen-proxy-rs/zen-proxy-rs`，实际 hash 为 `b9441bce94180aed6aae7dd6af79da618382a2a1f49ee292620b08cd2cd357fd`。

验证：

- `free-model-client-rs`：`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test` 全通过；库测试 60 条、kernel golden 46 条。
- `zen-proxy-rs`：`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test` 全通过；unit 125 条、e2e 26 条。
- panda 三实例 `4001/4002/4004` `/health` 均 200，`dispatch=90`、`dead=0`、`ratelimited=0`、`upstream.backoff=false`。
- WSL OpenClaw-only smoke：5/5 API ok，5/5 semantic ok；tool 2/2，subagent 1/1，subagent 用例约 20.1s 成功。
- WSL ClaudeCode/Hermes/OpenClaw smoke：15/15 API ok，15/15 semantic ok；三客户端均 5/5。

结论：

- OpenClaw subagent 的主要阻塞已从 profile 误识别层修复，smoke 级别通过。
- Hermes P90 仍高，huge_context 和 `deepseek-v4-flash-lite` 长上下文语义漂移仍是 full run 前的红旗。

未完成：

- 尚未跑修复后的 dry run。
- Windows ClaudeCode 仍需在 Windows 原生 shell 或可用的测试执行环境中复测。
- `.codex_tmp/`、根目录 0 字节未跟踪文件和 raw logs 仍不能提交。

下一步：

- 先跑修复后的 WSL 三客户端 dry run；如果通过，再处理 Windows ClaudeCode 原生执行与 full run 放量。
