# V4.109 Dynamic Free-Model Promotion Goal

## Final Goal

V4.109 turns V4.108's admin-only dynamic model discovery into a controlled
free-model lifecycle system.

The target behavior is:

```text
opencode /zen/v1/models
-> ZenProxy discovery worker
-> candidate registry
-> bounded model probes
-> canary model registry
-> active public model registry for the test channel
-> optional production promotion after explicit approval
```

V4.109 must let newly supported free models be discovered, probed, exposed, and
retired without modifying NewAPI, ClaudeCode, cc-switch, or the production
channel 69. The existing stable public aliases remain mandatory:

```text
deepseek-v4-flash      -> deepseek-v4-flash-free
deepseek-v4-flash-lite -> big-pickle
```

The first production rule is negative: unknown models must never become public
just because the provider listed them. They become public only after passing
explicit safety, protocol, performance, and compatibility gates.

## Current Baseline

V4.108 is deployed to panda as a separate test instance:

```text
service:  zen-proxy-rs-v4108-test.service
port:     http://172.17.0.1:4010
NewAPI:   channel 82, token sk-v4108test, group v4108-test
state:    DYNAMIC_MODEL_DISCOVERY_ENABLED=false
public:   only deepseek-v4-flash and deepseek-v4-flash-lite
```

V4.108 code already has:

- dynamic discovery config;
- opencode model list parser;
- candidate/ignored/missing states;
- admin-only visibility;
- public `/v1/models` still backed by the static registry;
- tests proving discovered candidates are not public.

V4.109 starts from commit `293786a` on branch
`codex/v4109-dynamic-model-promotion-plan`.

## Non-Goals

V4.109 must not:

- change NewAPI source code;
- change ClaudeCode, cc-switch, Hermes, or OpenClaw clients;
- overwrite NewAPI channel 69;
- route production traffic to unprobed models;
- expose paid-looking or unknown models;
- apply ClaudeCode-specific request shaping to every unknown model;
- hide model failures behind generic 502/503 errors;
- use discovery probes aggressively enough to drain proxy or token budgets.

## Architecture Contract

The model subsystem is split into four layers:

```text
DiscoverySource
  fetches raw provider model list

DynamicModelRegistry
  stores discovered, missing, ignored, canary, active, retired, quarantined

ProbeEngine
  validates model behavior through bounded OpenAI/Anthropic, stream/non-stream,
  tool-call, and minimal context probes

EffectiveModelRegistry
  merges static aliases with approved dynamic models for `/v1/models` and
  request resolution
```

The request data plane must depend only on `EffectiveModelRegistry`, not on raw
provider discovery. This keeps routing deterministic and testable.

## Model State Machine

Required states:

```text
ignored
candidate
probe_pending
canary
active
missing
retired
quarantined
```

Required transitions:

```text
provider listed free-looking model -> candidate
candidate selected by probe scheduler -> probe_pending
probe success quorum met -> canary
canary traffic success quorum met -> active
provider list absent for grace window -> missing
missing beyond retirement window -> retired
hard protocol or repeated empty-output failure -> quarantined
manual/admin rollback -> candidate or retired
```

Hard safety rules:

- `ignored`, `candidate`, `probe_pending`, `missing`, `retired`, and
  `quarantined` are not public and not routable.
- `canary` is public only when `DYNAMIC_MODEL_PUBLIC_MODE=canary_or_active`
  and only on the test instance.
- `active` can be public for test instances.
- production promotion requires an explicit deploy step and separate approval.

## Public Model Modes

Add a runtime switch:

```text
DYNAMIC_MODEL_PUBLIC_MODE=static_only|canary_or_active|active_only
```

Expected behavior:

```text
static_only:
  /v1/models returns only the two stable aliases.

canary_or_active:
  /v1/models returns stable aliases plus canary and active dynamic models.
  Intended only for NewAPI test channel 82 or a later isolated test channel.

active_only:
  /v1/models returns stable aliases plus active dynamic models.
  Candidate production mode after soak.
```

Default must be `static_only`.

## Probe Requirements

A model can move from `candidate` to `canary` only after all required probes
pass.

Required probe set:

```text
metadata:
  model id is free-looking and not denylisted

openai_nonstream_minimal:
  /v1/chat/completions stream=false returns assistant content

openai_stream_minimal:
  /v1/chat/completions stream=true returns valid SSE and real content

anthropic_nonstream_minimal:
  /v1/messages stream=false returns content

anthropic_stream_minimal:
  /v1/messages stream=true returns valid events and real content

tool_history_minimal:
  accepts repaired OpenAI/Anthropic tool history without missing tool_call_id,
  missing tool_use_id, orphan tool result, or invalid assistant message

empty_output_guard:
  no assistant content/tool-call is retried or classified, not exposed as a
  raw ambiguous 502

format_guard:
  model response must not break OpenAI or Anthropic response schema
```

Optional probes after canary:

```text
claudecode_tool_smoke
medium_context_50k
cache_signal_observation
long_stream_stability
```

Probe scheduler constraints:

```text
max concurrent probe models: 1 by default
max probe requests per interval: configurable, default 20
probe traffic lane: model_probe
probe budget: separate from user lanes
probe timeout: bounded and recorded per probe type
probe failures: never bury proxy nodes unless transport evidence proves node fault
```

## Compatibility Policy

All dynamic models share the existing protocol guard and free-model-client
kernel path, but client-specific compatibility remains profile-gated.

Required profiles:

```text
static_flash:
  deepseek-v4-flash only, ClaudeCode deep compatibility allowed

static_flash_lite:
  deepseek-v4-flash-lite only, Hermes/OpenClaw compatibility allowed

dynamic_generic:
  newly discovered models before model-family confidence is known

dynamic_claudecode_compatible:
  only after ClaudeCode probe data proves tool-call compatibility

dynamic_restricted:
  models that can answer simple chat but fail tools or stream correctness
```

Hard rule:

```text
No dynamic model inherits deepseek-v4-flash ClaudeCode deep adaptation just by
name. It must earn a compatible profile through probes.
```

## Admin And Observability APIs

V4.109 must expose enough data to debug model lifecycle without reading logs.

Required admin fields:

```text
model id
upstream id
state
public
routable
profile
first_seen_unix
last_seen_unix
last_probe_unix
last_success_unix
last_failure_unix
last_failure_code
last_failure_message
probe_attempts_total
probe_success_total
probe_failure_total
canary_requests_total
canary_success_total
canary_failure_total
active_requests_total
active_success_total
active_failure_total
consecutive_probe_successes
consecutive_probe_failures
missing_rounds
retirement_reason
promotion_reason
rollback_reason
```

Required endpoints:

```text
GET /admin/models
GET /admin/models/{model_id}
GET /admin/models/{model_id}/probes
GET /admin/models/{model_id}/traffic
POST /admin/models/{model_id}/promote
POST /admin/models/{model_id}/demote
POST /admin/models/{model_id}/quarantine
POST /admin/models/refresh
```

Admin mutation endpoints must require `ADMIN_API_KEY` and must be disabled or
unauthorized when the key is missing.

## Task Plan

### T0 Git And Baseline

1. Keep current V4.108 commit as rollback anchor.
2. Work on `codex/v4109-dynamic-model-promotion-plan`.
3. Do not stage `target-1.86` deletes or unrelated temporary directories.
4. Record the current panda production and test topology in docs.

Acceptance:

```text
git branch is codex/v4109-dynamic-model-promotion-plan
commit 293786a remains reachable
git status staged set contains only intentional source/docs/tests
```

### T1 Config And Modes

Implement:

- `DYNAMIC_MODEL_PUBLIC_MODE`;
- probe scheduler config;
- free/deny model allow rules;
- model discovery admin key visibility requirements.

Acceptance:

```text
default config keeps public models static
invalid public mode fails config validation or falls back safely with warning
unit tests cover all public modes
```

### T2 Registry State Machine

Implement:

- new lifecycle states;
- transition functions;
- persisted in-memory snapshot behavior;
- tombstones for missing/retired models;
- denylist/allowlist filters.

Acceptance:

```text
candidate cannot resolve in data plane
canary resolves only in canary_or_active mode
active resolves in active_only and canary_or_active modes
missing/retired/quarantined never resolve
static aliases always resolve
```

### T3 Effective Model Registry

Implement:

- merged static + dynamic public list;
- request resolution through `EffectiveModelRegistry`;
- `/v1/models/{id}` detail for dynamic public models;
- admin detail for non-public candidates.

Acceptance:

```text
/v1/models static_only returns exactly two stable aliases
/v1/models canary_or_active includes approved dynamic models
unknown dynamic candidate request returns model_not_found
static alias mapping remains unchanged
```

### T4 Probe Engine

Implement:

- model probe lane;
- OpenAI stream/non-stream probes;
- Anthropic stream/non-stream probes;
- tool-history minimal probe;
- empty-output classification;
- per-probe metrics and admin history.

Acceptance:

```text
probe never consumes normal short/long user lanes
probe failure records model failure, not proxy-node death, unless transport fault proves it
probe timeout is bounded
probe results are visible through admin APIs
```

### T5 Promotion And Demotion

Implement:

- automatic `candidate -> canary` after probe quorum;
- optional `canary -> active` after canary traffic quorum;
- admin promote/demote/quarantine;
- missing/retired handling with grace windows.

Acceptance:

```text
one probe success is not enough for active
repeated provider absence moves model toward missing/retired
hard protocol failure quarantines model
admin demotion removes model from /v1/models immediately
```

### T6 Compatibility Profiles

Implement:

- profile registry for static and dynamic models;
- dynamic generic profile by default;
- ClaudeCode-compatible promotion only after tool probes;
- no cross-contamination from Hermes/OpenClaw profile into ClaudeCode profile.

Acceptance:

```text
deepseek-v4-flash keeps ClaudeCode behavior
deepseek-v4-flash-lite keeps Hermes/OpenClaw behavior
new dynamic model starts generic
dynamic model cannot enable ClaudeCode deep profile without probe evidence
tool-call schema remains valid across profiles
```

### T7 Observability And Error Mapping

Implement:

- model lifecycle counters;
- per-model probe and canary traffic counters;
- classified errors for unsupported, missing, quarantined, probe_failed,
  provider_empty_output, provider_invalid_tool_history, provider_decode_error;
- admin and audit visibility.

Acceptance:

```text
NewAPI-facing error body is concise and classified
admin detail includes root cause
no raw provider implementation detail leaks into user-facing messages unless safe
model-level failure is distinguishable from proxy pool exhaustion
```

### T8 Local Test Harness

Implement:

- mock opencode model source;
- mock upstream model behavior matrix;
- e2e tests for discovery, probe, promotion, demotion, missing, retirement;
- static production regression tests.

Acceptance:

```text
cargo fmt --check
cargo check
cargo test v4::model_discovery
cargo test v4::model_registry or equivalent
cargo test dynamic model e2e cases
full cargo test passes
```

### T9 Panda Test Deployment

Deploy only as an isolated test service, not production channel 69.

Required shape:

```text
new service or upgraded test service on non-production port
NewAPI test channel only
test token only
DYNAMIC_MODEL_PUBLIC_MODE=canary_or_active or active_only only for test
production 69 remains unchanged
```

Acceptance:

```text
channel 69 base_url and quota path unchanged
test channel model list shows approved dynamic models
static aliases still work
failed candidate does not appear in model list
GitHub transfer artifacts cleaned after deployment
```

### T10 Real Client Smoke

Run narrow but real smokes through NewAPI test token:

```text
OpenAI-compatible curl
Anthropic-compatible curl
ClaudeCode minimal chat
ClaudeCode tool-call smoke
stream and non-stream
each approved dynamic model
```

Acceptance:

```text
HTTP 200 for approved model smokes
protocol errors: 0
invalid JSON / failed to parse JSON: 0
upstream returned no assistant content or tool call: 0 for smoke prompts
no proxy resources available: 0 during smoke
static aliases unaffected
```

### T11 Soak And Promotion Decision

Soak on test channel before any production decision.

Required evidence:

```text
per-model request count
per-model success rate
per-model protocol error count
per-model empty output count
per-model stream decode error count
per-model first_content_ms P50/P90/P95
per-model total latency P50/P90/P95
proxy pool impact
probe budget used
cache signal if available
```

Acceptance:

```text
minimum 100 successful requests per promoted model or explicit waiver
success rate >= 99%
protocol error count = 0
tool-call smoke pass rate >= 95% for ClaudeCode-compatible profile
ZenProxy process RSS increase <= 50MB from baseline
steady probe CPU overhead <= 2% on panda-class host
normal short-request P90 does not regress by more than 10%
```

## Strict Final Acceptance

V4.109 is complete only if all of these are true:

```text
1. Production channel 69 was not modified during implementation or test deploy.
2. Default runtime still exposes exactly two static models.
3. Dynamic discovery can be enabled on a test instance without making candidates public.
4. A mock discovered free model can move candidate -> canary -> active through probes.
5. A mock paid/unknown model remains ignored and unroutable.
6. A missing model becomes missing/retired and disappears from public model lists.
7. Admin APIs show full lifecycle and probe evidence.
8. Public /v1/models includes dynamic models only in configured test/public mode.
9. Unknown or failed dynamic model requests return classified model errors, not ambiguous 502.
10. ClaudeCode compatibility remains profile-gated and does not leak to all models.
11. deepseek-v4-flash and deepseek-v4-flash-lite behavior and mappings are unchanged.
12. Probe traffic cannot saturate normal user lanes.
13. Full Rust test suite passes.
14. Panda test deployment passes model probe and minimal chat for each approved dynamic model.
15. Rollback is one config/service action and does not require NewAPI source changes.
```

## Stop Lines

Pause implementation and report instead of pushing forward if any of these
happen:

```text
opencode model source requires scraping or anti-bot bypass
provider list does not expose stable model ids
candidate probes require production channel 69
test channel cannot be isolated from production users
dynamic model breaks static alias behavior
probe traffic starts burying healthy proxy nodes
ClaudeCode tool calls regress on existing deepseek-v4-flash
NewAPI has to be patched to make the design work
```

## Rollback Plan

Test deployment rollback:

```text
stop/disable V4.109 test service
disable NewAPI test channel or test token
set DYNAMIC_MODEL_PUBLIC_MODE=static_only
set DYNAMIC_MODEL_DISCOVERY_ENABLED=false
remove test-only firewall rule if no longer needed
```

Production rollback, if a later production promotion is approved:

```text
set DYNAMIC_MODEL_PUBLIC_MODE=static_only
restart ZenProxy instances
verify /v1/models returns only the two stable aliases
leave channel 69 unchanged
```

## Expected Outcome

After V4.109, adding a newly supported free opencode model should be an
operational action instead of a code rewrite:

```text
discover -> probe -> canary -> observe -> active -> retire when gone
```

The model can share the stable ZenProxy/free-model-client protocol guard,
transport, proxy pool, retry, telemetry, and ClaudeCode-compatible formatting
where it has earned the matching profile. Unknown models remain generic and
bounded until proven safe.
