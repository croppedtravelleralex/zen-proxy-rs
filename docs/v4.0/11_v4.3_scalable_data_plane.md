# V4.3 Scalable Data Plane

## Goal

V4.3 changes the performance target from "more ZenProxy processes" to a
capacity model that can scale under long streaming, large context, and tool
heavy workloads on small VPS profiles such as 1 vCPU / 2 GB RAM.

The main objective is not short non-stream RPM. Short RPM is only a gateway hot
path proof. The primary workloads are:

- long streaming chat completions;
- 8-32 MB large-context streaming requests;
- 32-64 MB huge-context requests with strict isolation;
- Claude Code / CherryStudio tool-call sessions where many requests belong to
  one development workflow.

## Capacity Formula

Use this formula when estimating real capacity:

```text
effective_rpm =
healthy_nodes
* safe_concurrency_per_node
* 60
/ avg_duration_seconds
* upstream_success_ratio
```

For long streaming traffic, concurrency is the main capacity signal:

```text
stream_rpm = stable_stream_concurrency * 60 / avg_stream_seconds
```

Example:

```text
128 stable streams * 60 / 120 seconds = 64 RPM
```

This can be a healthier system than a short-prompt benchmark reporting 2000 RPM,
because it matches the actual interactive workload.

## 1 vCPU / 2 GB Starting Budget

Default V4.3 lane targets for the current local/VPS class:

```text
short_nonstream: 32
normal_stream: 96
large_context: 16
huge_context: 2
large_context_body_mb: 8
huge_context_body_mb: 32
lane_wait_timeout_ms: 1000
```

Expected practical ranges after the data plane is fully optimized:

| Workload | Stable Concurrency | Equivalent RPM |
|---|---:|---:|
| small/medium long streams | 64-128 | 20-120 |
| 8-16 MB large-context streams | 16-32 | 5-30 |
| 24-32 MB large-context streams | 4-8 | 1-8 |
| 32-64 MB huge-context requests | 1-3 | below 1-3 |
| tool-heavy development sessions | 8-24 sessions | workflow-based |

## Landed Slice: Lane Isolation

V4.3 introduces a process-local lane limiter in `src/lanes.rs`.

Runtime behavior:

- `/admin/*`, `/health`, `/metrics`, and model discovery bypass data-plane lane
  limits.
- `/v1/chat/completions` and `/v1/messages` are classified by body size and
  streaming flag.
- Each response gets an `x-zen-lane` header.
- A lane permit is attached to the response extensions so streaming responses
  hold capacity until the response body is dropped.
- If lanes are disabled, the old `V1_MAX_CONCURRENT_REQUESTS` value is still
  used as the single data-plane limit.

Environment switches:

```text
V43_LANES_ENABLED=false
V43_SHORT_NONSTREAM_CONCURRENCY=32
V43_STREAM_CONCURRENCY=96
V43_LARGE_CONTEXT_CONCURRENCY=16
V43_HUGE_CONTEXT_CONCURRENCY=2
V43_LARGE_CONTEXT_BODY_MB=8
V43_HUGE_CONTEXT_BODY_MB=32
V43_LANE_WAIT_TIMEOUT_MS=1000
```

Runtime inspection:

```text
GET /admin/runtime
```

The response includes `v43_lanes.runtime` with max and in-flight counts for each
lane.

## Landed Slice: Async Collector

V4.3 adds an optional async collector wrapper around the existing
`DefaultCollector`.

Runtime behavior:

- request handlers enqueue telemetry with `try_send`;
- one background task drains the queue into the existing collector, WAL, audit
  log, ring buffer, and aggregators;
- if the queue is full, request telemetry falls back to synchronous recording so
  important audit records are not lost;
- lower-value pool, probe, schedule, and system events are lossy under collector
  overload to protect data-plane latency;
- admin reads still use the existing collector query APIs.

Environment switches:

```text
V43_ASYNC_COLLECTOR_ENABLED=false
V43_COLLECTOR_QUEUE_CAPACITY=8192
```

Default remains disabled until release-mode soak tests prove that eventual
telemetry visibility is acceptable for the operator experience.

## Landed Slice: Safe Request History

The request history ring no longer uses `UnsafeCell` with a manual `Sync`
implementation. V4.3 replaces it with a bounded `VecDeque` protected by a
mutex and monotonic sequence cursors.

This is intentionally conservative:

- async collector mode makes request history mostly single-writer in normal
  operation;
- admin reads and writes are now memory-safe under concurrent access;
- the bounded capacity still protects the 1 vCPU / 2 GB profile from unbounded
  request history growth.

## Landed Slice: Transport Registry

`DispatchPool` no longer creates or stores a `reqwest::Client` for every node.
The selected node is still owned by pool state, but transport clients are owned
by `TransportRegistry` and created lazily when a request or probe actually needs
egress.

This removes the first duplicate client owner:

```text
before: DispatchPool client cache + PoolManager client cache
after:  TransportRegistry client cache only
```

`/admin/runtime` now exposes transport cache metrics under
`data_plane.transport`, including current per-node client count and whether the
direct client has been initialized.

## Landed Slice: Sampled Dispatch Fast Path

`DispatchPool` now tries a bounded sampled selection before falling back to the
older whole-pool weighted scan.

Fast path behavior:

- sample up to 8 candidate nodes;
- ignore candidates that fail local budget or concurrency checks;
- choose the best sampled score;
- acquire local and global budget;
- if sampling cannot find or admit a node, use the previous full scan path.

This makes the healthy-pool common case cheaper while preserving the old
availability behavior when many nodes are cooling down, rate-limited, or dead.

## Landed Slice: Node Registry

`PoolManagerImpl` no longer owns an ad-hoc `HashMap` directly. Node identity and
lookup now live behind `src/pool/node_registry.rs`.

Why this matters:

- dispatch, active, rate-limited, dead, and probe logic share one node lookup
  boundary;
- runtime can expose node-registry size without coupling admin code to manager
  internals;
- later sharding can move behind registry/pool boundaries without changing API
  handlers.

Runtime inspection:

```text
GET /admin/runtime
```

The response includes:

```text
data_plane.node_registry.nodes
data_plane.transport.node_client_count
data_plane.transport.direct_client_initialized
```

## Landed Slice: Sharded Dispatch

`DispatchPool` now stores ready nodes in configurable shards instead of one
global vector.

Environment switch:

```text
V43_DISPATCH_SHARDS=16
```

Dispatch behavior:

- node add/remove/sticky/release are routed to the node's shard;
- normal acquire starts at a random shard and walks shards only until a node is
  admitted;
- inside a shard, the sampled fast path remains the common path;
- fallback weighted selection is now shard-local instead of whole-pool first.

This is not a full lock-free queue, but it removes the single ready-list lock as
the default contention point and keeps behavior compatible with the previous
pool contracts.

## Landed Slice: AIMD Node Concurrency

Per-node `max_concurrent` is now adaptive.

Environment switches:

```text
V43_NODE_MIN_CONCURRENCY=1
V43_NODE_MAX_CONCURRENCY=16
V43_AIMD_SUCCESS_STEP=1
V43_AIMD_FAILURE_PERCENT=50
V43_AIMD_SLOW_LATENCY_MS=30000
```

Behavior:

- repeated successes gradually raise node concurrency;
- rate-limit, transport error, and slow responses reduce concurrency;
- local budget detail exposes each node's current `max_concurrent`.

This protects weak proxy nodes from being hammered while letting stable nodes
absorb more work.

## Landed Slice: Redis Budget Hot-Path Reduction

Global Redis budget now reuses one connection per ZenProxy instance and drops it
only after an error. This removes per-request TCP connection setup from the
budget hot path.

Environment switch:

```text
V43_GLOBAL_BUDGET_MODE=sync_redis
```

Set `V43_GLOBAL_BUDGET_MODE=off` only for a single-process/single-host profile
where cross-instance budget coordination is intentionally disabled. The current
multi-instance local runtime should keep `sync_redis`.

## Still Required For Full V4.3 Soak

The code path now contains the core scalable data-plane pieces. Remaining work
is release-mode evidence, not another major architecture rewrite:

- a release-mode benchmark matrix for 1C2G.
- a conservative production soak before enabling async collector by default;
- real long-stream and large-context results for the active proxy pool.

Use:

```text
scripts/v43_smoke_matrix.sh
RUN_LARGE=1 LARGE_MB=8 scripts/v43_smoke_matrix.sh
```

## Acceptance

V4.3 data-plane slice is accepted when:

- `cargo check` passes;
- `cargo test` passes;
- `cargo build --release` passes;
- `/admin/runtime` exposes lane configuration and runtime counts;
- `/admin/runtime` exposes `data_plane.node_registry` and
  `data_plane.transport`;
- stream, large-context, and short non-stream requests get distinct
  `x-zen-lane` values;
- saturating one lane does not block admin endpoints;
- a low huge-context limit prevents 32-64 MB requests from overwhelming memory.
- node budget details show adaptive `max_concurrent`;
- Redis budget mode is visible and configurable.
